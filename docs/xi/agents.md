# Xi Agent — Knowledge Base

- Agent name: xi
- MCP agent-chat available for inter-agent messaging
- Never use Plan mode or Select mode in tmux multi-agent environment
- Team: user (总负责人), wang (架构师, lead), yu (执行者), xi (协助开发)
- All xi outputs go to `docs/wang/` per wang's instruction, not `docs/xi/` (except agent state files)
- Project: LLM-empowered RTS AI copilot system for OpenRA. Three-tier: Kernel(rules) / TaskAgent(LLM) / Job(traditional AI)
- Current stage: design complete, preparing for implementation
