## Current
Execute Phase 0.1 cleanup by deleting Wang-approved legacy files and repairing any resulting broken imports without touching xi-owned directories.

## Queue
1. Execute Phase 0.1b by migrating required NLU rule assets into `nlu_pipeline/rules/`, updating imports off `the_seed`, and removing the `the-seed` submodule and `.gitmodules` references.
