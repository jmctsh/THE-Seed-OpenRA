## Current
Testing reform / signal quality: layered backend gate, direct-entry startup smoke, E2E classification guardrails, pytest-backed `__main__` runners, live-runner diagnostics consumption, deploy short-circuit truth hardening, live task-surface requirements, shared triage detail, coordinator/base-readiness projection parity, first-pass frontend triage surfacing, runtime reset on `session_clear`/restart, stale-world failure detail surfacing across task/debug panels, direct tests for `GameLoop.reset_runtime_state()` plus `session_clear -> sync_runtime` unregister behavior, Adjutant/coordinator stale-world detail wording, unit-pipeline stale-world detail in request/reservation payloads, replay preservation of those stale-world unit-pipeline details, fallback-branch coverage for replay `live_runtime_facts` / `runtime_state` reservation recovery, ordinary-task reservation triage/replay reason alignment, the current capability truth-boundary fixes (unsupported allied fail-close, off-roster queue-block filtering, ready-power low-power suppression), the operator-surface truth bridge for `capability_truth_blocker`, the live task-payload/diagnostics truth bridge for that blocker, the top-level `world_snapshot` / diagnostics / operations truth surfacing for unsupported factions, the `DiagPanel` live replay refresh on world-only truth/freshness changes, live-runner debug-context truth surfacing, and a WS-backed mock-integration smoke that asserts emitted `world_snapshot` / `task_update` truth are now in place. The current focus remains the capability/diagnostics yellow zone, but the next slices should stay narrow: keep turning remaining prompt/runtime drifts into explicit runtime truth, then pin each one with direct tests instead of trusting ad-hoc E2E memory.

## Queue
Runtime disconnect degradation: the refresh-storm fix, `session_clear`/restart runtime reset, stale failure detail surfacing in task/diagnostics panels, Adjutant/coordinator stale-world detail wording, unit-pipeline stale-world detail, and replay preservation for new sessions are landed. Remaining follow-up is now mostly consistency/UX: old replay sessions still cannot reconstruct refresh-health detail unless it was logged explicitly, and there is still a naming split between raw `world_snapshot` health fields and task-triage shorthand.
Outdated backend self-checks: `test_backend.sh` now reflects the current runtime path; any further changes should preserve the layered gate instead of reintroducing a single opaque smoke check.
Frontend task panel collapse: per-task expand/collapse and default collapsed completed-expert sections are now in place. Remaining follow-up is optional polish only (for example, whether replay/diagnostics should adopt the same “completed section folded” pattern).
Capability prompt truth alignment: make capability-facing context distinguish prereq-satisfied from buildable-now, and surface stale-world, disabled-gateway, low-power, and queue-blocked truth as first-class blockers instead of burying them in generic headers.
Unit-reservation contract surfacing: promote `UnitReservation` state from kernel bookkeeping into capability/runtime/debug surfaces, and tighten cancel/fulfill semantics without removing the current fast-path bootstrap path.
Task triage/debug bundle follow-up: replay bundles now carry a `replay_triage` fallback shaped like `TaskTriageSnapshot`, `DiagPanel` renders it when live `current_runtime.triage` is absent, ordinary managed tasks keep reservation blocker/detail semantics instead of collapsing everything into generic `unit_reservation`, and `DiagPanel` now refreshes the selected live replay when `world_snapshot` truth/freshness signals change so “Current Runtime” no longer lags behind the top banner. Remaining follow-up is refinement rather than absence: improve how much capability/runtime detail is preserved, not whether historical tasks have any structured triage at all.
Operator truth surfacing: `capability_truth_blocker` is now bridged through Adjutant/coordinator `base_readiness`/`alerts`/`status_line`, live task payloads inject capability `runtime_facts` so task cards/diagnostics surface the same blocker, and `world_snapshot` now carries the minimal truth surface (`player_faction`, `capability_truth_blocker`) so top-level diagnostics/operations no longer hide unsupported-faction fail-close. Remaining follow-up is broader runtime truth surfacing of similar hidden blockers, not this specific gap.
Capability truth boundary follow-up: the first Mendel batch is now closed. Capability runtime/context now fails closed on known-but-unsupported allied roster states, nested `queue_blocked_items` no longer leak off-roster ready items into blocker text, and low-power nudges are suppressed when a ready/unplaced `powr/apwr` already exists. Remaining follow-up is narrower: better faction-aware long-term capability support instead of the current fail-closed guard, plus any other prompt/runtime drift that still survives outside these boundary cases.
Knowledge/planner truth cleanup: actor `unit_type` normalization, undeployed-MCV guard, aircraft counter category normalization, counter-prerequisite truth, and the second-MCV expansion false-positive are now aligned with the normalized demo capability truth. Remaining follow-up is narrower soft-strategy review only; the planner no longer overclaims immediate producibility for counters that still miss tech prerequisites or mistakes expansion MCVs for undeployed openers.
Historical task debug bundle follow-up: one task can now be triaged from `summary + replay_triage + blockers/highlights` instead of only raw log scrolling. Remaining work is optional enrichment, such as richer replay-specific highlights or parity with live task-level chips.
Test-signal audit follow-up: broader capability/diagnostics E2E coverage still remains, but the startup smoke, direct-entry smoke, layered backend gate, bootstrap smoke, E2E classification guardrails, pytest-backed `__main__` runners, replay fallback blind spots, several stale assertion surfaces, the live-runner debug-context truth gap, and the missing WS-backed emitted-payload truth smoke are now closed.
Docs hygiene follow-up: top-level `docs/yu` now retains only `agents.md`, `plan.md`, `progress.md`, and the still-referenced knowledge table. Remaining work is occasional maintenance, not backlog cleanup.

## Blocked (optional)
