## Current
Session-level diagnostics now have both a selected-session rollup, list-level scan hints, direct session highlights, and basic trace compaction instead of only `task_count`/`record_count`: persisted sessions backfill a structured `task_rollup`, the current live session overlays real in-memory task counts, `DiagPanel` renders that rollup next to session metadata, the session selector itself now surfaces compact `fault/stale/sync/slow` health hints alongside `nt/failed/partial/aborted`, the selected session exposes 2-3 clickable high-signal task highlights, the `Live Runtime` block now consumes `unit_pipeline_focus` rather than stopping at preview text, and the trace stream collapses consecutive duplicate entries into `×N` groups. This closes the narrow “I still have to click every session and then manually hunt the bad task” gap and trims one of the biggest scan-cost sources inside a selected task. The next yellow area on this diagnostics line is broader publish/debug ergonomics rather than missing basic browseability, session-discovery truth, or top-level live pipeline focus.
Player-reply routing follow-up: the old `Adjutant` TODO around multi-question replies is now closed for the safe deterministic subset. Explicitly segmented replies like `放弃；否` can bypass LLM classification entirely, match pending questions in priority order, and deliver multiple `PlayerResponse`s in one turn, while ambiguous single replies still keep the previous highest-priority fallback. The boundary contracts are now pinned as well: segment-count overflow and unmatched segments must fail closed, the narrow synonym bridge (`继续` -> `继续进攻`) is covered, and `_handle_reply()` without a target must still prefer safe multi-reply routing before collapsing back to single-question fallback. Remaining work on this line is richer open-ended multi-reply semantics only if they prove necessary; the basic explicit split-reply gap is no longer open.
Background publish fault-surfacing follow-up: `DashboardPublisher.schedule_publish()` no longer leaves failures to the event loop’s generic `Task exception was never retrieved` path, and `publish_all()` no longer lets one crashing stage abort the rest of the same fanout cycle. Scheduled publish tasks now retrieve/log their own exceptions and clear the held task reference, while per-stage execution logs structured `dashboard_publish_stage_failed` errors and continues through later stages (`notifications/logs/benchmarks`). The scheduled-path regression and the fail-open stage-order regression are both pinned. Remaining work on this line is richer fault history/persistence, not the earlier hidden-live-fault/operator-surfacing gap.
Runtime fault surfacing follow-up: that broader operator-facing gap is now closed at the dashboard boundary too. `RuntimeBridge` reuses one shared world-sync probe result while building the live payload, `world_snapshot` now carries the merged `runtime_fault_state`, and both `OpsPanel` and `DiagPanel` render runtime degradation explicitly instead of falling back to a false-healthy top line. The contract is now pinned end to end: probe failures surface on `sync_request`, publisher-stage failures surface on the next snapshot, publisher fault state clears after a clean publish, and the scheduled done-callback `dashboard_publish_task_failed` branch is tested as well. Remaining work on this line is fault history/persistence or richer operator aggregation, not hidden live-runtime degradation.
Session fault-history follow-up: that persistence half is now closed for a first useful slice as well. Probe faults now emit a deduplicated structured `runtime_probe_fault` log, persisted sessions now carry a compact `runtime_fault_summary`, older sessions backfill the same summary from `main.jsonl` / `dashboard_publish.jsonl`, current-session catalog payloads overlay the live `runtime_fault_state`, `DiagPanel` renders the selected session’s last fault summary next to `world_health`, and `task_replay` now injects the same selected-session runtime fault into `bundle.session_context` so focused replay/debug consumers no longer have to cross-reference the separate session strip. Remaining work on this line is richer counts/timelines or broader fault taxonomy, not “session looked healthy unless you opened raw logs” or “task replay lost session degradation context”.
Coordinator unit-pipeline follow-up: the top-level `Adjutant` coordinator no longer collapses unit-pipeline truth to a bare reservation count when high-signal `unit_pipeline_focus` data already exists. Coordinator snapshots now carry the same focused pipeline payload used by `world_snapshot`, reservation-wait alerts prefer `task_label + detail` over generic counts, and the top-level `status_line` now appends that focused detail instead of stopping at generic capability-phase text. Remaining work on this line is broader operator copy/aggregation polish, not this specific reservation-surfacing blind spot.
Live harness build acceptance follow-up: phase-B structure-build checks no longer accept “task succeeded” as a proxy for real world-state success. `_wait_for_structure_result()` now requires task surface up front and fails closed if the task reaches any terminal state before the observed structure count actually increases; the runner-level contract is pinned so future pre-E2E greens cannot silently regress back to workflow-only acceptance. Remaining work on this line is broader live-flow semantics, not this specific structure-build false-green.

## Queue
Runtime lifecycle follow-up from the 2026-04-12 sidecar audit: the `ApplicationRuntime.start()` partial-failure regression and post-start fatal wait-path regression are now pinned with real `ApplicationRuntime` startup, the startup smoke no longer drops out-of-order WS payloads or inherit previous-test log backlog, and `tests/test_game_control.py::test_main_entry_direct_start_smoke_covers_enable_voice_and_task_message_publish` already exercises the direct `main()` entry path with websocket/voice/task-message publish enabled. The remaining work on this line is broader end-to-end lifecycle coverage and runtime fault surfacing, not a missing `__main__` smoke.
Historical diagnostics follow-up: persisted task catalogs now recover summary text from `task_message_registered` when structured `content` exists, session catalogs now also surface task-status rollups for both historical and live sessions, and non-terminal historical tasks now carry a narrow derived `triage` (`status_line` plus waiting/blocking hints) when persisted warning/info/high-signal expert events exist. Remaining work on this line is broader browseability and aggregation, not the earlier empty-summary / no-session-rollup / no-running-task-triage gaps.
Unit-request lifecycle semantics follow-up: single-request semantics are now pinned end to end, including register-time `start_released` + task-actor handoff. The remaining ambiguity on this line is deliberate design rather than a silent bug: for multiple blocking requests on the same task, current kernel semantics remain task-level gated (ready requests are not released while any blocking wait remains), and the helper-level order contract is now pinned as `handoff -> resume/push -> sync`.
Test-stack hardening from the 2026-04-12 audit: the live `command_submit` round-trip smoke, the live `session_clear` and `game_restart` mutating-control smokes, the true `main.py` `__main__` / shutdown-wrapper coverage, the frontend selector fallback/merge-precedence blind spots, and the newest sequence-only `command_submit` smoke false negative are now closed. There is also now a curated fast-path gate in [`test_signal_stack.sh`](/Users/kamico/work/theseed/THE-Seed-OpenRA/test_signal_stack.sh) for the highest-signal runtime/operator truths, so the mainline no longer relies only on a very broad green suite or the layered backend gate. Remaining test work is broader regression coverage rather than a known high-signal false-green gap.
Real Adjutant submit-path follow-up: the last fake-adjutant hole in the live startup/runtime tests is now closed too. A dedicated websocket/runtime smoke keeps the real `ApplicationRuntime` Adjutant, sends a deterministic economy command (`建造电厂`), and proves the command is merged into the existing capability task rather than creating a fake managed task, while also pinning that the adjutant LLM provider stays unused on this deterministic fast path. Remaining work here is no longer “does live `command_submit` hit the real adjutant path”, but broader live semantics and operator-facing truth for other routes.
Operator-control live-path follow-up: the first real player reply contract is now pinned too. `tests/test_game_control.py` covers both the successful `question_reply` round-trip and the `task_mismatch` failure path over a real websocket/runtime stack, and the smoke now explicitly drains background log backlog before asserting on the reply payload so we do not mistake websocket noise volume for a missing control-plane response. Remaining work on this line is the other mutating control paths (`command_cancel` live round-trip and similar) plus the frontend transport/app wiring, not this specific reply-route blind spot.
Operator-control live-path follow-up II: `command_cancel` success is now pinned at the same level as well. The live smoke no longer stops at the bridge notification; it requires the websocket notification, the per-task update, and the next task-list truth surface to all converge on `aborted` for the cancelled task. Remaining work on this line is the frontend transport/app wiring itself and other control flows where the UI could still look successful while the runtime/dashboard truth drifts.
Frontend transport follow-up: the first fake-bus hole is now closed. `web-console-v2/src/composables/__tests__/useWebSocket.spec.js` pins real `useWebSocket` semantics instead of relying on `App.spec.js`'s full mock: connect/reconnect must auto-send `sync_request`, reconnect must clear buffered messages, `send()` must fail closed when the socket is not open, and `on()`/off plus unmount/disconnect must not leak duplicate handlers or reconnection. Remaining work here is one level up the stack: `ChatView` / `App` still need direct tests for `question_reply`, `session_cleared`, and chat-history cleanup wiring.
Frontend transport follow-up II: `ChatView` now has direct tests for the two highest-signal operator paths it owns: task-question option buttons must emit the correct `question_reply` payload exactly once and become disabled after answering, and `theseed:clear-ui` plus unmount must clear chat/sessionStorage state and unregister websocket handlers so old messages cannot keep leaking into a dead view. Remaining work here is the top-level `App` bridge for `session_clear` / `session_cleared`, not the basic chat reply/cleanup wiring itself.
Frontend transport follow-up III: the top-level `App` bridge is now pinned as well. Clicking “清空界面” must send `session_clear`, but `theseed:clear-ui` may only fire after the websocket `session_cleared` event arrives; the UI can no longer falsely look cleared just because the request was emitted. Remaining work here is no longer the base transport/app chain itself, but adjacent operator flows or higher-level E2E semantics.
Mutating-control refresh-truth follow-up: the earlier live smokes for `session_clear` and `game_restart` no longer stop at `session_cleared` / restart notifications. `session_clear` must now republish a post-reset `world_snapshot` / `task_list` where transient pre-clear tasks are gone and capability/runtime state is rebuilt, and `game_restart` must republish a post-restart `world_snapshot` / `task_list` where transient pre-restart tasks are no longer active and the cancelled task surfaces as `aborted`. Remaining work here is no longer these specific control/update gaps, but whatever the next distinct runtime/test yellow area is.
Live harness contract follow-up: `LiveTestRunner` is no longer allowed to mark the websocket baseline as ready on a single surface, and `send_command()` is no longer allowed to consume the first random `query_response`. The runner now waits for `world_snapshot + task_list + session_catalog`, and command replies must be `response_type=command` with matching `echo_text` when present. Remaining work here is broader live-flow semantics, not these two loose runner contracts.
Frontend diagnostics follow-up: the main runtime-truth visibility gap is now closed by the new `Live Runtime` block in `DiagPanel`, task cards can now open the diagnostics view directly through an explicit focus button plus App-level mode handoff, task selectors now surface live `triage.status_line` or persisted historical `summary` rather than only `label · raw_text`, `OpsPanel` capability/pipeline rows reuse the same focus chain to jump into the capability task, selected tasks now expose an immediate catalog strip with `status/entries/summary`, and historical task catalogs now recover the latest published `task_info` / `task_warning` when no terminal summary exists while staying compatible with older `expert_signal`-only sessions. Remaining work here is narrower UX refinement, such as richer task-history compaction or other polish once the current operator flow is stable.
Diagnostics selector polish follow-up: the selected-task strip now shares one computed catalog entry for log-path/catalog-summary/triage reads, and the `ALL -> task -> ALL` disappearance path is pinned. Remaining work here is replay/live triage priority specification and other UI-polish nits, not selector-state correctness.
Replay/live triage precedence follow-up: `DiagPanel` now has an explicit regression pinning `current_runtime.triage` over `replay_triage` when both exist in the same replay bundle. Remaining work here is further UI polish, not ambiguity about which triage the operator should trust first.
Diagnostics session-switch follow-up: the eventual `session_select -> session_task_catalog` reset path is now pinned as well, so switching sessions and receiving a catalog that no longer contains the old selected task must drive the UI back to `ALL` and drop the selected-task strip. Remaining work here is polish, not selection-state drift.
Replay/live triage edge-case follow-up: the remaining `DiagPanel` replay triage matrix is now fully pinned as well. Both `current_runtime.triage only` and `current_runtime exists but triage=null -> replay_triage fallback` are explicitly covered, so remaining work in this area is UI polish rather than data-source ambiguity.
Adjutant stale-world detail follow-up: coordinator alert/status text and stale-guard structured fields are now aligned on the same error fallback chain (`last_error` -> `last_refresh_error`). Remaining work here is broader operator copy polish, not a known text-vs-structured truth drift.
Task-triage stale-world detail follow-up: the live task payload builder now also has an explicit regression for `last_refresh_error` fallback, so stale-world detail is pinned consistently across Adjutant, task triage, and diagnostics surfaces. Remaining work here is not fallback correctness.
Outdated backend self-checks: `test_backend.sh` now reflects the current runtime path; any further changes should preserve the layered gate instead of reintroducing a single opaque smoke check.
Frontend task panel collapse: per-task expand/collapse and default collapsed completed-expert sections are now in place. Remaining follow-up is optional polish only.
Capability prompt truth alignment: the main prompt/tool contract drift is now closed as well. Capability-facing context distinguishes prereq-satisfied from buildable-now, surfaces stale-world/disabled-gateway/low-power/queue-blocked truth as first-class blockers, the prompt no longer advertises impossible actions, the capability header no longer leaks off-roster queue-blocked items through weak-reference `world_summary`, and raw `recent_signals` / `recent_events` payloads are compacted down to the same high-signal fields the LLM actually needs. Remaining follow-up is narrower: reduce any other misleading weak-reference surfaces and keep prompt text synchronized with the actual tool boundary.
Unit-reservation contract surfacing: promote `UnitReservation` state from kernel bookkeeping into capability/runtime/debug surfaces, and tighten cancel/fulfill semantics without removing the current fast-path bootstrap path. The first prompt-truth drift in this area is now closed (`_capability_runtime_facts_view` preserves reservation `reason` and `world_sync_*` fields), live task triage now also exposes request/reservation-driven `world_sync_*` detail structurally instead of only via `status_line`, and top-level `unit_pipeline_focus` no longer depends on insertion order when multiple waits coexist: `world_sync_stale` / hard blockers now beat generic waiting-delivery entries. The larger surfacing/contract cleanup is still open.
Task triage/debug bundle follow-up: replay bundles now carry a `replay_triage` fallback shaped like `TaskTriageSnapshot`, `DiagPanel` renders it when live `current_runtime.triage` is absent, ordinary managed tasks keep reservation blocker/detail semantics instead of collapsing everything into generic `unit_reservation`, `DiagPanel` refreshes the selected live replay when `world_snapshot` truth/freshness signals change so “Current Runtime” no longer lags behind the top banner, replay bundles now expose a compact `capability_truth` summary sourced from persisted context or live capability facts, active-task replay now prefers live `capability_truth` / `unit_pipeline` when live runtime facts explicitly provide them, historical session replay no longer gets contaminated by same-`task_id` live tasks, historical replay no longer inherits live `runtime_state.unit_reservations` through the final fallback path, reservation-only `world_sync_stale` replay triage now surfaces the same degraded state/detail contract as request-driven stale paths, `_task_payload_builder()`'s `runtime_state.is_capability` fallback is now pinned by a direct regression test, historical replay can now derive degraded world-sync triage directly from `runtime_facts` even when no unit pipeline exists because `compute_runtime_facts()` persists `world_sync_failure_threshold` alongside the other stale-world fields, session browsing now surfaces persisted/live `world_health` so operators no longer need raw logs just to answer whether a session was stale or slow, and focused `task_replay` consumers now receive both `session_context.runtime_fault_summary` and `session_context.world_health` so task-level debugging no longer loses session degradation context. Remaining follow-up is refinement rather than absence: richer counts/timelines and broader publish/debug ergonomics are still open, but the specific stale-health/runtime-fault cross-reference gap is closed.
Replay triage follow-up from Xi audit: the runtime-facts stale-world fallback now uses the same `world_sync_stale` reason token as the pipeline path, and terminal lifecycle (`task_completed` / terminal `expert:task_complete`) now wins over runtime-facts stale fallback instead of being masked by older stale snapshots. The specific masking/drift issues Xi flagged on `75dd99b` are closed.
Session-clear/runtime reset follow-up: the first reset/test gap is now closed as well. `GameLoop.reset_runtime_state()` stays narrow and only handles stale-world recovery bookkeeping, while the real `RuntimeBridge.on_session_clear()` chain is now pinned against an actual `GameLoop` so session clear must empty the loop-owned `_jobs` / `_agents` registries and the bridge's own registered-id sets. Remaining follow-up here is only broader runtime restart ergonomics, not this stale-registry leak or the earlier false-green around it.
Historical summary compatibility follow-up: persisted task catalogs now also recover `expert_signal(constraint_violated)` summaries for older sessions instead of dropping them on the floor. The remaining boundary here is deliberate noise control rather than missing a known high-signal expert state.
Coordinator/operator truth follow-up: the `42ef144` capability truth alignment slice is already in main, and its remaining blind spot is now closed as well: the `capability_queue_blocked` dedup branch against battlefield queue-blocked alerts is covered by a direct regression, so both dedup guards in `Adjutant._coordinator_alerts()` are now pinned instead of only the low-power half.
Operator truth surfacing: `capability_truth_blocker` is now bridged through Adjutant/coordinator `base_readiness`/`alerts`/`status_line`, live task payloads inject capability `runtime_facts` so task cards/diagnostics surface the same blocker, and `world_snapshot` now carries the minimal truth surface (`player_faction`, `capability_truth_blocker`) so top-level diagnostics/operations no longer hide unsupported-faction fail-close. Remaining follow-up is broader runtime truth surfacing of similar hidden blockers, not this specific gap.
Capability truth boundary follow-up: the first Mendel batch is now closed. Capability runtime/context now fails closed on known-but-unsupported allied roster states, nested `queue_blocked_items` no longer leak off-roster ready items into blocker text, and low-power nudges are suppressed when a ready/unplaced `powr/apwr` already exists. Remaining follow-up is narrower: better faction-aware long-term capability support instead of the current fail-closed guard, plus any other prompt/runtime drift that still survives outside these boundary cases.
Knowledge/planner truth cleanup: actor `unit_type` normalization, undeployed-MCV guard, aircraft counter category normalization, counter-prerequisite truth, and the second-MCV expansion false-positive are now aligned with the normalized demo capability truth. Remaining follow-up is narrower soft-strategy review only; the planner no longer overclaims immediate producibility for counters that still miss tech prerequisites or mistakes expansion MCVs for undeployed openers.
Historical task debug bundle follow-up: one task can now be triaged from `summary + replay_triage + blockers/highlights` instead of only raw log scrolling. Remaining work is optional enrichment, such as richer replay-specific highlights or parity with live task-level chips.
Test-signal audit follow-up: broader capability/diagnostics E2E coverage still remains, but the startup smoke, direct-entry smoke, layered backend gate, bootstrap smoke, E2E classification guardrails, pytest-backed `__main__` runners, replay fallback blind spots, several stale assertion surfaces, the live-runner debug-context truth gap, and the missing WS-backed emitted-payload truth smoke are now closed.
Docs hygiene follow-up: top-level `docs/yu` now retains only `agents.md`, `plan.md`, `progress.md`, and the still-referenced knowledge table. Remaining work is occasional maintenance, not backlog cleanup.

## Blocked (optional)
