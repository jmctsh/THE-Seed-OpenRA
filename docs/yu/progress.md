## [2026-04-12 12:03] DONE — `world_snapshot` / diagnostics / operations top-level truth now surface unsupported-faction fail-close
Closed the next visibility gap above task-level triage. After `cdf6070`, capability tasks themselves finally surfaced `capability_truth_blocker`, but the top-level `world_snapshot` channel still only carried `world_summary + runtime_state`, so `DiagPanel` and `OpsPanel` could still show a globally healthy backend while hiding the fact that capability had intentionally failed closed on an unsupported faction. I kept this slice narrow: `RuntimeBridge._build_dashboard_payload()` now adds only the minimal truth surface needed at the dashboard layer, namely `player_faction` and `capability_truth_blocker`, sourced from reduced `compute_runtime_facts("__dashboard__", include_buildable=False)` rather than shipping the whole `runtime_facts` blob. `DiagPanel` now renders a top-level `能力真值受限` summary when that blocker is present, and `OpsPanel` now renders a matching operational warning below the regular freshness status so data-staleness and capability-truth limitations stay distinct. I also extended the backend sync-request test to assert the new `world_snapshot` fields, and added focused Vue tests for both panels, plus a production build check. Verification: `python3 -m py_compile main.py tests/test_ws_and_review.py`, `python3 -m pytest tests/test_world_model.py tests/test_ws_and_review.py tests/test_adjutant_coordinator.py tests/test_runtime_views.py -q` (`128 passed`), `cd web-console-v2 && npm test -- --run src/components/__tests__/OpsPanel.spec.js src/components/__tests__/DiagPanel.spec.js` (`8 passed`), and `cd web-console-v2 && npm run build` (succeeded).

## [2026-04-12 11:16] DONE — Live task cards/diagnostics now surface capability truth-blockers instead of showing a false-idle capability task
Closed the remaining live-surface gap in the capability truth chain. After the earlier fail-close and operator-surface slices, `capability_truth_blocker=faction_roster_unsupported` already existed in `runtime_facts` and was visible inside capability prompt context plus Adjutant/coordinator status, but the shared `task_triage` path still only consumed `runtime_state`, so the live task card/diagnostics view could keep rendering the capability task as ordinary `idle/dispatch` without any indication that capability had intentionally paused itself on an unsupported roster. I fixed that at the shared payload boundary instead of adding another one-off UI warning: `RuntimeBridge` now injects capability `runtime_facts` when building live task payloads, `TaskTriageInputs` / `build_live_task_payload()` / `build_task_triage_from_artifacts()` now carry optional `runtime_facts`, and the capability triage path now promotes `runtime_facts.capability_truth_blocker` into a real triage blocker with explicit `state=blocked`, `blocking_reason=faction_roster_unsupported`, `status_line=能力处理中：真值受限`, and a concrete `faction=allied demo capability roster 未覆盖` detail string. Xi’s audit also caught a real hidden coupling: `capability_truth_blocker` had originally only been populated under `include_buildable=True`, so I lifted that fail-close marker out of the buildability gate in `WorldModel.compute_runtime_facts()` and pinned it with a dedicated reduced-facts regression (`include_buildable=False` still exposes the blocker without leaking `buildable*`). This keeps the dashboard/diagnostics story aligned with the capability prompt and coordinator story without changing kernel arbitration or capability execution behavior. Verification: `python3 -m py_compile world_model/core.py runtime_views.py task_triage.py main.py tests/test_world_model.py tests/test_ws_and_review.py`, `python3 -m pytest tests/test_ws_and_review.py -q -k 'runtime_truth_blocker or task_payload_builder_fetches_capability_truth_blocker or capability_triage_surfaces_blocker_detail or capability_triage_surfaces_fulfilling_detail'` (`4 passed`), and `python3 -m pytest tests/test_world_model.py tests/test_ws_and_review.py tests/test_adjutant_coordinator.py tests/test_runtime_views.py -q` (`128 passed`).

## [2026-04-12 10:58] DONE — Adjutant/coordinator now surfaces capability truth-blockers to the operator layer
Closed the last “truth only visible inside capability prompt” gap from Xi’s audit. After the fail-close slice, `capability_truth_blocker=faction_roster_unsupported` existed in `runtime_facts`, but Adjutant/coordinator still built `base_readiness`, alerts, and top-level `status_line` as if nothing special had happened, so the operator layer could still miss that capability had intentionally paused itself on an unsupported allied roster. I bridged that blocker into the coordinator path instead of inventing another ad-hoc message: `_coordinator_snapshot()` now carries `faction` and `truth_blocker` through `base_state` and `capability`, `_coordinator_base_readiness()` now returns an explicit `能力层当前因阵营限制暂停（allied）`, and `_coordinator_alerts()` emits a dedicated `capability_truth_blocked` alert so the top-level `status_line` also surfaces the pause reason. Verification: `python3 -m py_compile adjutant/adjutant.py tests/test_adjutant_coordinator.py`, `python3 -m pytest tests/test_adjutant_coordinator.py -q -k 'capability_truth_blocker_to_operator or corrects_blocked_runtime_base_progression or build_context_includes_task_triage_fields'` (`3 passed`), and `python3 -m pytest tests/test_adjutant_coordinator.py -q` (`23 passed`).

## [2026-04-12 10:46] DONE — Capability context now fails closed on unsupported faction truth, stops leaking off-roster queue blockers, and suppresses false low-power nudges
Closed the next capability yellow-zone boundary instead of pushing more prompt prose. Mendel’s audit was correct on three concrete runtime/context drifts: when `faction=allied` became known, capability buildability still overclaimed the Soviet-oriented demo roster; nested `queue_blocked_items` could leak off-roster ready items such as `工程师` directly into `[队列阻塞]` and `[前置已满足但当前受阻]`; and the capability economy line still printed `⚡低电力` even when a ready-but-unplaced `powr/apwr` already made “place the power plant” the correct next action. I fixed this at the truth boundary instead of relying on prompt discipline: `WorldModel.compute_runtime_facts(..., include_buildable=True)` now fails closed with `capability_truth_blocker=faction_roster_unsupported` for known allied states, hiding `buildable / buildable_now / buildable_blocked` plus `base_progression` and disabling capability-side `produce_units` feasibility; `_demo_capability_issue_now_snapshot()` now filters nested `queue_blocked_items` through the same demo-roster filter as ready items; capability context now defensively filters top-level/nested queue-block items, renders an explicit `[能力真值]` guard for unsupported factions, and suppresses the low-power nudge when filtered ready/blocked items already include `powr/apwr`; and the ready-item filter now normalizes demo display names consistently so capability context stops mixing canonical ids with localized names. I also added two cross-layer contract tests on the real `WorldModel -> runtime_facts -> build_context_packet -> context_to_message` path, so this slice is no longer only pinned by local unit helpers. Verification: `python3 -m py_compile world_model/core.py task_agent/context.py openra_state/data/dataset.py tests/test_world_model.py tests/test_capability_task.py`, `python3 -m pytest tests/test_world_model.py tests/test_capability_task.py -q -k 'fails_closed_for_unsupported_allied_capability_roster or buildable_blocked_filters_off_roster_queue_items or filters_off_roster_queue_blocked_items or suppresses_low_power_nudge_when_ready_power_exists or surfaces_truth_guard_for_unsupported_faction_roster'` (`5 passed`), `python3 -m pytest tests/test_world_model.py -q -k 'capability_context_fails_closed_when_world_model_knows_allied_faction or capability_context_suppresses_low_power_nudge_with_ready_power_from_world_model'` (`2 passed`), `python3 -m pytest tests/test_world_model.py tests/test_capability_task.py tests/test_task_agent_policy.py -q` (`93 passed`).

## [2026-04-12 10:27] DONE — Ordinary-task reservation triage now keeps blocker semantics across live runtime, replay, and UI
Closed the next diagnostics/runtime drift in the non-capability path. Kernel/runtime state had already been exporting rich request/reservation reasons such as `missing_prerequisite`, `queue_blocked`, `world_sync_stale`, `bootstrap_in_progress`, and `waiting_dispatch`, but the shared triage path for ordinary managed tasks still collapsed any reservation-bearing task into the generic story `state=waiting_units`, `phase=reservation`, `waiting_reason=unit_reservation`. Replay had a second vocabulary split on top: it was still checking old reason codes like `pending_requests_waiting_dispatch` / `request_inference_pending`, so real request states such as `waiting_dispatch` or `inference_pending` could fall through to `blocked`. I fixed this in the shared contract rather than patching individual panels: `TaskTriageSnapshot` now carries an additive `reservation_preview`, live triage now joins a task’s first reservation back to its matching request and classifies raw unit-pipeline reasons into truthful `blocked / degraded / bootstrapping / dispatch / reservation` states, and replay now uses the same classification instead of its stale reason vocabulary. `TaskPanel` and `DiagPanel` now render the preview directly, so operators can see which unit is reserved and why it is stuck without falling back to raw JSON. Verification: `python3 -m py_compile runtime_views.py task_triage.py task_replay.py tests/test_runtime_views.py tests/test_ws_and_review.py`, `python3 -m pytest tests/test_runtime_views.py tests/test_ws_and_review.py -q -k 'task_triage_snapshot_from_mapping_normalizes_fields or surfaces_task_specific_reservation_blocker_detail or marks_request_dispatch_without_fake_blocker or derives_replay_triage_from_unit_pipeline or marks_waiting_dispatch_as_running_dispatch'` (`5 passed`), `python3 -m pytest tests/test_ws_and_review.py tests/test_runtime_views.py tests/test_adjutant_coordinator.py -q` (`82 passed`), `cd web-console-v2 && npm test -- --run src/components/__tests__/TaskPanel.spec.js src/components/__tests__/DiagPanel.spec.js` (`9 passed`), and `cd web-console-v2 && npm test && npm run build` (`11 passed`, build succeeded).

## [2026-04-12 09:46] DONE — Archived stale `docs/yu` execution notes so top-level docs stay current
Cleaned up the local execution-doc surface instead of letting old strategy notes keep competing with the active plan. I moved three historical documents that were still sitting at the top level into `docs/yu/archive/20260412_docs_cleanup/`: the old execution board, the pre-fix drift backlog, and the one-off correction report for Wang’s earlier overdesigned `design_v_next`. They are still preserved for lookup, but they are no longer mixed with the current execution surface, which now stays focused on `agents.md`, `plan.md`, `progress.md`, and the still-useful expert knowledge table. This closes the docs-hygiene backlog item down to normal maintenance instead of one-time cleanup.

## [2026-04-12 09:37] DONE — Narrowed `deploy_mcv_first` so expansion MCVs no longer freeze planner advice
Closed the one residual Xi found in the planner truth-guard slice. The first implementation correctly blocked MCV-only opener states, but it keyed only on the presence of `mcv`, which meant a legitimate mid-game “base already exists + second MCV just finished” state could still be downgraded to `hold(reason=deploy_mcv_first)`. I tightened the guard to only fire when an MCV exists without a deployed construction yard (`fact`), then added a regression proving a base with `fact + weap + dome + mcv` still emits a normal Soviet vehicle-heavy counter recommendation instead of getting stuck behind the deploy-first hold. Verification: `python3 -m pytest tests/test_planners.py -q -k 'mcv'` (`2 passed`), `python3 -m pytest tests/test_planners.py tests/test_demo_capability_truth.py -q` (`37 passed`), `python3 -m pytest tests/test_tool_handlers.py -q -k planner` (`1 passed`), and `python3 -m py_compile experts/planners.py tests/test_planners.py`.

## [2026-04-12 09:28] DONE — Historical replay now has structured triage even without live runtime
Closed another debugging gap in the replay path. Before this slice, `task_replay.py` only exposed structured triage when a task still had `current_runtime.triage`; for persisted sessions or already-finished tasks, replay collapsed back to a free-form `summary` plus raw `blockers/highlights`, which made it hard to answer the basic question “what was this task blocked on?” without reading lifecycle logs. I added a replay-local fallback object, `replay_triage`, shaped like the existing `TaskTriageSnapshot` contract and derived only from replay-local data we already had: `unit_pipeline`, `last_transition`, recent blockers, and summary text. The frontend now renders that fallback in `DiagPanel` whenever live runtime triage is unavailable, so historical tasks expose deterministic `state / phase / waiting_reason / blocking_reason / reservation_ids` instead of only prose. Verification: `python3 -m pytest tests/test_ws_and_review.py -q` (`50 passed`), `python3 -m py_compile task_replay.py tests/test_ws_and_review.py`, `npm test -- --run src/components/__tests__/DiagPanel.spec.js` (`4 passed`), `npm test` in `web-console-v2` (`3 files, 11 tests passed`), and `npm run build` (`vite build` succeeded).

## [2026-04-12 09:12] DONE — Planner truth guards now respect normalized demo prerequisites, MCV deploy state, and aircraft counter categorization
Closed a real strategy/truth gap in the deterministic planner layer. `ProductionAdvisor` was still making several recommendations from under-specified actor payloads and soft counter rules that did not respect current demo truth: `has_role()` ignored `unit_type`, so a standard `{'unit_type': 'proc'}` building payload could be misread as an empty base; an MCV-only state could still be treated as “start opening with power” instead of “deploy first”; counter recommendations could suggest `v2rl` even when the base still lacked `dome`; and MIG/YAK sightings carried through a generic `vehicle` category could miss the dedicated anti-air counter path entirely. I fixed this in the knowledge/planner layer instead of patching prompts: `has_role()` now normalizes from `unit_type/type/name/display_name`, `ProductionAdvisor` detects undeployed-MCV states and returns `hold(reason=deploy_mcv_first)` before any opening-order production advice, counter recommendations now normalize aircraft by unit identity instead of trusting raw actor category alone, and planner-side counter proposals now degrade into a truthful `tech_up` prerequisite recommendation when the suggested counter still misses required buildings. I added regressions for all four edges, including the previously wrong `proc -> empty_base -> powr` path and the `powr + proc + weap` Soviet base that used to recommend impossible `v2rl` directly. Verification: `python3 -m pytest tests/test_planners.py tests/test_demo_capability_truth.py -q` (`36 passed`), `python3 -m pytest tests/test_tool_handlers.py -q -k planner` (`1 passed`), `python3 -m py_compile experts/knowledge.py experts/planners.py tests/test_planners.py tests/test_demo_capability_truth.py`, plus direct local repros showing `proc` now yields `need_vehicle_gateway`, MCV-only now yields `deploy_mcv_first`, and missing-`dome` Soviet counter logic now yields `tech_up(dome)` instead of `produce(v2rl)`.

## [2026-04-12 08:54] DONE — TaskPanel now folds completed Experts by default inside expanded tasks
Closed one of the remaining operator-side readability issues without touching backend contracts. The task list already supported task-level expand/collapse, but once a long-running task was expanded it still rendered every finished `job` inline, so the card kept growing even when only one active Expert still mattered. I changed `TaskPanel` to split jobs into active vs terminal sets: active Experts remain visible immediately, while completed/failed/aborted Experts move behind a per-task `已完成 Experts · N` toggle that stays collapsed by default. I also reset this UI-only expansion state on global clear and added focused component tests that pin both the new default behavior and the fact that an operator’s explicit completed-section expansion survives later `task_update` merges. Verification: `npm test -- --run src/components/__tests__/TaskPanel.spec.js` (`5 passed`), `npm test` in `web-console-v2` (`3 files, 10 tests passed`), and `npm run build` (`vite build` succeeded).

## [2026-04-12 08:31] DONE — Pinned `task_replay` fallback branches so replay tests stop trusting only the happy path
Closed two remaining false-green holes in the replay test surface without changing runtime behavior. After preserving stale-world detail in replay bundles, the backend contract was still only pinned on the main persisted `context_snapshot.runtime_facts` path; there was no test for the side-specific fallback to `live_runtime_facts`, and no test for the final reservation-only fallback from `runtime_state`. I added focused `ws_and_review` tests for both branches: one proves persisted requests are not overwritten while missing reservations are filled from `live_runtime_facts`, and the other proves reservations still preserve stale-world detail when the bundle has to fall back all the way to `runtime_state["unit_reservations"]`. This turns the replay unit-pipeline coverage from “main path only” into a more truthful contract around the actual fallback structure in `task_replay.py`. Verification: `python3 -m py_compile tests/test_ws_and_review.py`, `python3 -m pytest tests/test_ws_and_review.py -q -k 'task_replay_bundle_falls_back_to_live_runtime_facts_for_unit_pipeline or task_replay_bundle_falls_back_to_runtime_state_reservations'` (`2 passed`), and `python3 -m pytest tests/test_ws_and_review.py -q` (`49 passed`).

## [2026-04-12 08:18] DONE — Preserved stale-world unit-pipeline detail in replay bundles
Closed the last obvious split between live diagnostics and historical replay in the stale-world chain. The live runtime had already been carrying `world_sync_last_error`, `world_sync_consecutive_failures`, and `world_sync_failure_threshold` through `production_readiness_for()`, unfulfilled-request payloads, unit reservations, and capability context text, but `task_replay.py` still compacted replayed `unfulfilled_requests` / `unit_reservations` down to a smaller schema that silently dropped those three fields. That meant a task could show the right stale-world cause while running, then lose the same explanation as soon as it was inspected through replay. I updated replay compaction to preserve the stale-world detail for both request and reservation entries, extended the persisted-log replay test to assert the fields survive end-to-end, and added a focused replay-bundle unit test so future changes cannot quietly reintroduce the live/replay mismatch. Verification: `python3 -m py_compile task_replay.py tests/test_ws_and_review.py`, `python3 -m pytest tests/test_ws_and_review.py -q -k 'task_replay_request_returns_persisted_task_log or task_replay_bundle_preserves_world_sync_detail_in_unit_pipeline'` (`2 passed`), and `python3 -m pytest tests/test_ws_and_review.py -q` (`47 passed`).

## [2026-04-12 07:58] DONE — Frontend panels now surface structured triage metadata and have component coverage
Closed the visibility gap after the backend truth slices. `TaskPanel` and `DiagPanel` were already receiving structured triage fields such as `waiting_reason`, `blocking_reason`, `reservation_ids`, and `world_stale`, but both panels still mostly rendered only `status_line`, which meant the new backend truth could still disappear into a single prose sentence. I added a minimal UI slice instead of redesigning the panels: `TaskPanel` now shows compact triage chips for waiting/blocker/reservation/stale state when present, and `DiagPanel` now renders the same structured fields both for the selected live task triage and the current-runtime triage inside replay summaries. I also added the missing `DiagPanel` component test and extended `TaskPanel` coverage so these structured fields are now part of the frontend contract instead of an accidental implementation detail. Verification: `npm test` in `web-console-v2` (`3 files, 6 tests passed`) and `npm run build` (`vite build` succeeded, no compiler-macro warning after removing the stale `defineProps` import from `DiagPanel.vue`).

## [2026-04-12 07:34] DONE — Coordinator projection now respects issue-now truth instead of overtrusting raw prereq buildability
Closed the next coordinator-side truth gap identified by the capability audit. The capability prompt/context already distinguished `buildable_now` from prereq-satisfied `buildable`, but Adjutant’s coordinator snapshot still overtrusted `base_progression` and a partial blocker vocabulary. In practice that meant coordinator/base-readiness could still say `下一步：矿场` even when the runtime already knew the next step was blocked right now by low power, queue blockage, or a disabled prerequisite. I fixed that in the shared projection layer instead of pushing yet another prompt tweak: `capability_blocker_status_text()` now humanizes the full blocker set (`world_sync_stale`, `deploy_required`, `disabled_prerequisite`, `low_power`, `queue_blocked`, `insufficient_funds`, etc.); `capability_coordinator_alert()` now surfaces those blockers as first-class capability alerts where appropriate; `_coordinator_snapshot()` now forwards the full blocker counters instead of only a subset; and `_coordinator_base_readiness()` now cross-checks runtime `buildable_now` / `buildable_blocked` before trusting an existing `base_progression`, correcting misleading `下一步：X` summaries into truthful blocked states such as `当前受阻：矿场（低电）`. Verification: `pytest tests/test_adjutant_coordinator.py -q` (`20 passed`), `pytest tests/test_ws_and_review.py -q` (`44 passed`), `pytest tests/test_capability_task.py -q` (`43 passed`), and `python3 -m py_compile adjutant/adjutant.py task_triage.py`.

## [2026-04-12 07:11] DONE — Shared task triage now shows concrete capability blocker/delivery detail
Closed another diagnostics gap that still made the task list feel generic even after the earlier triage cleanup. `RuntimeStateSnapshot` did not formally carry `unfulfilled_requests`, and the shared `build_task_triage()` path only surfaced capability counts such as `pending=1` or `blocker=缺少前置建筑 (1)`. That meant the live task list still could not answer the practical question “blocked on what exactly?” without dropping into raw runtime facts or replay logs. I extended the shared runtime snapshot to include `unfulfilled_requests`, synchronized that through `WorldModel.runtime_state()`, and taught capability triage to append one concrete detail line: for blocker states it now surfaces the first request-specific reason payload (for example `猛犸坦克 <- 维修厂 + 科技中心 + 战车工厂`), and for `fulfilling` it surfaces the first live reservation/delivery detail (for example `重坦×2 (partial)`). This stays in the same shared triage path used by the dashboard payload and Adjutant coordinator context, so the two surfaces do not drift. Verification: `pytest tests/test_runtime_views.py -q` (`7 passed`), `pytest tests/test_ws_and_review.py -q -k 'build_live_task_payload_uses_task_specific_message_lookup or build_live_task_payload_uses_latest_info_when_no_other_triage_signal or build_live_task_payload_capability_triage_surfaces_blocker_detail or build_live_task_payload_capability_triage_surfaces_fulfilling_detail or task_replay_bundle_prefers_live_runtime_status_line_for_active_tasks'` (`5 passed`), `pytest tests/test_adjutant_coordinator.py -q -k 'build_context_surfaces_prerequisite_gap_blocker_text or build_context_uses_capability_task_label_fallback_and_fulfilling_counts'` (`2 passed`), and `python3 -m py_compile task_triage.py runtime_views.py world_model/core.py`.

## [2026-04-12 06:53] DONE — Surfaced latest `TASK_INFO` into shared task triage when no stronger signal exists
Closed a smaller but user-visible diagnostics gap: many tasks were already emitting meaningful `TASK_INFO` messages such as “缺少战车工厂，等待能力层补前置”, but the shared triage path still fell back to generic lines like “等待调度” unless there was a warning, pending question, reservation, or active job summary. I extended the shared triage inputs to capture the latest per-task `TASK_INFO` and taught `build_task_triage()` to use it as the status line for ordinary running/waiting tasks when no stronger triage signal exists. That means the live dashboard/task payload and the coordinator context can now surface the task’s own most recent explanation directly instead of hiding it behind a generic idle/waiting label. Verification: `pytest tests/test_ws_and_review.py -q -k 'build_live_task_payload_uses_task_specific_message_lookup or build_live_task_payload_uses_latest_info_when_no_other_triage_signal or sync_request_pushes_current_state_directly or runtime_bridge_task_update_fingerprint_tracks_active_group_size'` (`4 passed`) and `pytest tests/test_adjutant_coordinator.py -q -k 'build_context_includes_task_triage_fields or build_context_uses_shared_triage_inputs_for_questions_and_warnings'` (`2 passed`).

## [2026-04-12 06:07] DONE — Hardened deploy short-circuit truth against split-query drift and unsynced snapshots
Closed a fragile truth surface in Adjutant’s deploy shortcut path. Before this slice, `_deploy_truth_snapshot()` mixed multiple read paths (`my_actors(category=mcv)`, `my_actors(type=建造厂)`, and runtime facts) and then short-circuited the player command before NLU/task routing. That meant deploy decisions could drift if those sources disagreed or if the runtime had not produced a first snapshot yet. I changed the logic to prefer one full self-actor snapshot when available, only fall back to the older split queries when the world model does not expose a general `my_actors` list, and keep the shortcut in the “ambiguous / retry” state when runtime facts disagree with the visible MCV count or when the world sync timestamp is still uninitialized. I also added focused regressions for two failure modes: an unsynced world that should not assert “already deployed,” and a split-query drift case where the full actor snapshot shows an MCV even though the legacy param-specific queries disagree. Verification: `pytest tests/test_adjutant.py -q -k 'deploy_feedback or nlu_routed_deploy or expand_mcv_uses_deploy_path'` (`11 passed`) and `pytest tests/test_live_e2e_runner.py -q` (`3 passed`).

## [2026-04-12 05:46] DONE — Hardened the live E2E runner so it consumes task updates, replay, and diagnostics instead of only task lists
Closed another false-confidence gap in the manual `live` layer: the runner used for real game-in-loop tests was still only trustworthy on a narrow subset of websocket payloads, so a backend could regress in task-update, replay, benchmark, or diagnostics flows while the live harness still looked superficially fine. I widened `LiveTestRunner` to maintain an indexed task view from both `task_list` and `task_update`, capture `task_message` / `benchmark` / `error` / `session_catalog` / `session_task_catalog` / `task_replay` payloads, and expose a `request_task_replay()` helper so the runner can actively validate replay retrieval instead of only passively observing command replies. I then added `tests/test_live_e2e_runner.py`, a fast local test file that exercises this message plumbing without requiring a real game or backend. This does not replace real live E2E, but it raises the floor for the live harness itself so the manual layer is no longer blind to most of the runtime diagnostics surfaces it depends on. Verification: `pytest tests/test_live_e2e_runner.py -q` (`3 passed`) and `pytest tests/test_harness_integrity.py -q` (`3 passed`).

## [2026-04-12 06:09] DONE — Added a minimal frontend diagnostics test shell for Task/Ops panels
Closed another remaining blind spot in the pre-E2E stack: the operator-facing Vue surfaces had already been changed several times, but `web-console-v2` still had zero component tests. I added an isolated `vitest` setup (`vitest.config.js` plus `npm test`) without touching the existing Vite app config, then added focused tests for the two most regression-prone panels. `TaskPanel` is now covered for the behaviors we just landed and rely on during live debugging: capability tasks stay pinned above ordinary tasks, terminal tasks default to collapsed details, and incremental `task_update` messages merge into the latest task view instead of leaving the panel stale. `OpsPanel` is now covered for the operator contract we explicitly want: only `game_restart` is exposed as a control, and stale-world details (`consecutive_refresh_failures`, `failure_threshold`, `last_refresh_error`) render when `world_snapshot` says the runtime is degraded. I also removed the obsolete compiler-macro imports from the two components so the new test run stays quiet instead of emitting Vue SFC warnings. Verification: `npm test` in `web-console-v2` (`4 passed`) and `npm run build` (`vite build` succeeded).

## [2026-04-12 05:58] DONE — Tightened deploy short-circuit truth and query boundaries
I re-ran the deploy/MCV path against the current code instead of trusting the earlier audit note, and there were still multiple real gaps in the pre-classification short-circuit path. First, deploy short-circuiting happens before classification/NLU, but `_looks_like_query()` did not treat sentence-final particles like `吗` / `呢` / `么` as query signals, so inputs such as `基地车展开了吗` could still be answered as an immediate deploy command result. Second, deploy truth was still partly assembled from split raw queries: `_maybe_handle_deploy_feedback()` and direct NLU deploy resolution used `_deploy_truth_snapshot()`, but `_match_deploy()` bypassed it, and the snapshot itself still depended on older split `mcv` / `建造厂` queries before later filtering. Third, malformed or stale actor payloads could still leak into deploy reasoning if they looked actor-shaped enough at the call site. I tightened the boundary in three steps: extend the lightweight query heuristic for sentence-final particles; prefer one trusted self-actor snapshot (`my_actors`) for deploy truth, falling back to split queries only if the wide snapshot is unavailable; and unify all deploy routing through the same trusted snapshot path, rejecting malformed actor ids and treating unsynced/runtime-facts-vs-query MCV mismatches as `deploy_truth_ambiguous` instead of attempting deployment. Regressions now lock the important edges: query-like deploy phrasing no longer short-circuits as a command, malformed MCV payloads are ignored, an untrusted construction-yard payload cannot force `already_deployed`, unsynced refresh-health truth returns `deploy_truth_ambiguous`, and a single atomic `my_actors` snapshot can still drive deploy routing even if the legacy split queries disagree. Verification: `pytest tests/test_adjutant.py -q -k deploy` (`9 passed` on the first slice, then `13 passed` after the atomic-snapshot follow-up), `python tests/test_adjutant.py -q -k deploy` (`9 passed`), `pytest tests/test_adjutant.py -q` (`73 passed`), and `pytest tests/test_game_control.py tests/test_ws_and_review.py -q` (`52 passed`).

## [2026-04-12 05:43] DONE — Widened the live E2E runner’s local diagnostics surface beyond `task_list`
Closed another post-start blind spot in the new testing ladder: the only live runner still treated `task_list` as its main truth source even though the runtime/frontend path already emits richer signals such as `task_update`, `task_message`, `benchmark`, `session_catalog`, `session_task_catalog`, `task_replay`, and `error`. I updated `LiveTestRunner` so it maintains a task index merged from `task_list` plus incremental `task_update`, captures those additional WS payloads locally, exposes helper accessors for replay/catalog/benchmark/error reads, and includes the new signals in its debug context. I then added `tests/test_live_e2e_runner.py`, a focused local plumbing suite that proves the runner merges incremental task updates correctly, captures diagnostics payloads instead of silently dropping them, and can request a specific task replay and wait for the matching response. Verification: `pytest tests/test_live_e2e_runner.py -q` (`3 passed`) and `pytest tests/test_harness_integrity.py -q` (`3 passed`).

## [2026-04-12 05:31] DONE — Replaced manual `__main__` test runners with pytest delegation on regular runtime test files
Closed the other large false-green channel Xi called out: a long list of test files still carried hand-maintained `if __name__ == "__main__"` runners that invoked only a subset of each file’s `test_*` functions, so `python tests/test_xxx.py` could silently skip newer regressions. Instead of adding a permanently failing completeness guard first, I replaced the `__main__` blocks on the regular runtime-facing test files with a single delegated path: `pytest.main([__file__, *sys.argv[1:]])`. That means direct file execution now uses real pytest collection instead of a stale hand-written call list. I also extended `tests/test_harness_integrity.py` to guard this convention for the converted files so the repo does not drift back into manual-call-list runners. Verification: `pytest tests/test_harness_integrity.py -q` (`3 passed`), `python tests/test_game_control.py -q` (`17 passed`), `python tests/test_world_model.py -q` (`38 passed`), `python tests/test_kernel.py -q` (`31 passed`), and `python tests/test_ws_and_review.py -q` (`35 passed`).

## [2026-04-12 05:18] DONE — Added E2E classification guardrails and a harness-integrity meta-test
Tightened the testing surface so “E2E” stops being an ambiguous label. I added explicit module-level pytest markers to the runtime-facing test files: the existing mocked runtime end-to-end files (`test_e2e_t1.py`, `test_e2e_adjutant.py`, `test_e2e_experts.py`, `test_e2e_full.py`, `test_e2e_capability_bootstrap.py`) are now all tagged `mock_integration`, while `test_live_e2e.py` is tagged `live`. I then added `tests/test_harness_integrity.py`, a small meta-test that fails if those runtime/E2E files lose their explicit layer classification or if `pytest.ini` stops declaring the runtime-layer markers at all. This does not magically make mocked E2E more truthful, but it removes a recurring false-green failure mode: an alarming-sounding `test_e2e_*` file silently living outside any declared trust layer. Verification: `pytest tests/test_harness_integrity.py tests/test_e2e_capability_bootstrap.py -q` (`3 passed`) and `pytest tests/test_e2e_t1.py tests/test_e2e_adjutant.py tests/test_e2e_experts.py -q` (`12 passed`).

## [2026-04-12 05:08] DONE — Added a real direct-entry startup smoke for `main.main(argv)` and forced publish callback coverage
Closed the largest remaining gap in the new test layering: until now, the backend gate still did not execute the real `main.py` entry path, so direct-start failures like `python main.py --enable-voice` could slip past even while `ApplicationRuntime` smoke stayed green. I added a second `@pytest.mark.startup_smoke` in `tests/test_game_control.py` that goes through `main.main(argv)` with `--enable-voice`, mock LLM providers, temporary runtime output paths, and a test runtime subclass that still uses the real runtime assembly/start path while injecting a deterministic mocked world/api surface. Inside that direct-entry runtime I now explicitly register `TASK_INFO` and `TASK_COMPLETE_REPORT` messages and force `DashboardPublisher.publish_all()`, so the smoke covers the async publish callback path rather than only the happy startup surface. The test asserts three concrete things the old startup smoke did not: CLI/entry wiring succeeds end-to-end, `enable_voice=True` really reaches `WSServerConfig.voice_enabled`, and the published task-message callback reaches Adjutant notification handlers without background crashes; when `dashscope` is missing, startup warning logs are also surfaced instead of silently skipped. Verification: `pytest tests/test_game_control.py -q -k 'startup_smoke or direct_start_smoke or published_task_messages_notify_adjutant'` (`3 passed`), `pytest -m startup_smoke tests/test_game_control.py -q` (`2 passed`), and `./test_backend.sh`.

## [2026-04-12 04:49] DONE — Replaced the backend’s single smoke check with a layered runtime gate
Converted the backend self-check from a single startup-smoke command into a layered gate that reports which runtime surface is actually green. I marked the most important runtime regression checks explicitly instead of leaving them buried inside broad files: `tests/test_world_model.py::test_connection_failure_skips_remaining_layers_and_respects_retry_backoff` and `tests/test_game_loop.py::test_worldmodel_stale_pauses_jobs_notifies_and_recovers` now carry `@pytest.mark.runtime_invariants`, while `tests/test_e2e_capability_bootstrap.py::test_capability_bootstrap_request_smoke` is marked `@pytest.mark.mock_integration` so the capability bootstrap path becomes a named trust layer instead of incidental coverage. I then expanded `test_backend.sh` into a layered runner that executes `startup_smoke`, `contract`, `runtime_invariants`, and `mock_integration` separately and states explicitly that this does not claim full live-game correctness. Verification: `pytest -m contract tests/test_game_control.py tests/test_ws_and_review.py -q` (`4 passed`), `pytest -m startup_smoke tests/test_game_control.py -q` (`1 passed`), `pytest -m runtime_invariants tests/test_world_model.py tests/test_game_loop.py -q` (`2 passed`), `pytest -m mock_integration tests/test_e2e_capability_bootstrap.py -q` (`1 passed`), and `./test_backend.sh`.

## [2026-04-12 05:20] DONE — Added a real startup-smoke gate and replaced the stale backend self-check
Promoted runtime startup from an implicit hope into an explicit test layer. I kept the existing WS-enabled runtime smoke inside `tests/test_game_control.py`, marked it as `@pytest.mark.startup_smoke`, and registered layered pytest markers in the new `pytest.ini` so startup checks stop being buried inside a large undifferentiated green count. The smoke now guards the current runtime shape directly: `ApplicationRuntime` starts with WS enabled, answers a real `sync_request`, capability wiring exposes the correct live tool surface, background dashboard publish does not explode via loop exception handling, and shutdown releases the WS port. I then replaced the old `test_backend.sh` that still referenced legacy `the_seed` config loading with a current-runtime self-check that simply runs the startup-smoke marker and documents that live game-in-loop remains a separate gate. Verification: `pytest -m startup_smoke tests/test_game_control.py -q` (`1 passed, 15 deselected`), `pytest tests/test_game_control.py tests/test_ws_and_review.py tests/test_kernel.py tests/test_kernel_task_creation.py -q` (`88 passed`), and `./test_backend.sh`.

## [2026-04-12 05:33] DONE — Quieted disconnect storms by adding world-model failure backoff and connection-fast-fail layer skipping
Audited the post-game-close logs from `Logs/runtime/session-20260411T201323Z` and confirmed the runtime degradation was structural: once `GameAPI` disconnected, `WorldModel.refresh()` still treated all layers as immediately due because failed layers never advanced any refresh timestamp, and each tick could then spend about a second on actors, another second on economy/queues, and another on map. I implemented a minimal safe fix in `world_model/core.py` instead of a larger loop/API rewrite: connection-class failures (`CONNECTION_ERROR`, `REQUEST_ID_MISMATCH`, socket-level disconnects) now trigger a per-layer retry backoff and short-circuit the remaining layers for that refresh, so one disconnection event no longer fans out into a three-layer refresh storm every tick. I added a focused regression in `tests/test_world_model.py` that asserts connection failure skips later layers and suppresses retries until the backoff window expires, then re-ran the relevant `world_model`, `game_loop`, and runtime/bridge suites. Verification: `pytest tests/test_world_model.py tests/test_game_loop.py -q` (`51 passed`) and `pytest tests/test_game_control.py tests/test_ws_and_review.py tests/test_kernel.py tests/test_kernel_task_creation.py -q` (`88 passed`).

## [2026-04-12 04:35] IN PROGRESS — Stabilization reset before next E2E: startup signal reform + disconnect audit
Stopped the latest live backend after confirming the immediate startup blockers were fixed (`RuntimeBridge` task-message import regression and capability tool-registration timing). I also audited why the large pytest suite still gave a misleading green signal: most coverage still lives in unit/mock-integration space, while the real `main.py -> run_runtime() -> ApplicationRuntime.start()` path, WS-enabled publish chain, and background-task exception surfaces are not guarded by a proper startup smoke gate. The brief live interaction produced two useful runtime findings that should be folded into the next stabilization slices rather than ignored: first, `deploy_feedback_shortcircuit` still fired with `reason=rule_deploy_already_deployed`, so deploy-routing truth needs a focused re-check against world truth; second, once the game was closed, `world_model` entered a noisy `CONNECTION_ERROR` loop and `game_loop` ticks degraded to ~3s, which means degraded/disconnect mode is still too expensive and too loud. I updated `docs/yu/plan.md` to make testing reform, disconnect handling, outdated backend self-check replacement, and frontend task collapse an explicit queue ahead of the next full E2E round.

## [2026-04-12 04:35] DONE — Closed two live startup/runtime regressions and captured the next stabilization targets
Fixed the backend startup regression from a real `python main.py --enable-voice` run by restoring the missing `TaskMessageType` import path in `RuntimeBridge` and then fixing the next live blocker behind it: capability tasks were being marked `is_capability=True` only after task/agent/tool-executor creation, so the live `produce_units` handler never registered even though capability-only unit tests stayed green. I changed task creation so capability identity is present at construction time, added regressions that assert the real created capability agent exposes `produce_units`/`set_rally_point` and hides `request_units`, and re-verified the bridge-side task-message callback path. Verification: `pytest tests/test_kernel_task_creation.py tests/test_kernel.py -q` (`37 passed`) and `pytest tests/test_game_control.py tests/test_ws_and_review.py -q` (`50 passed`). I then relaunched the real backend, drove the live WS path (`sync_request`, `command_submit("展开基地车")`), and confirmed the original startup crash plus the `tool_handler_missing` loop were gone.

## [2026-04-12 04:55] IN PROGRESS — Reframed “next E2E” into a pre-E2E stabilization program
After the live run, I stopped the backend and audited the newest runtime session instead of forcing another E2E pass. The meaningful findings were: the deploy short-circuit still returns `rule_deploy_already_deployed` and needs a truth audit; once the game closes, `world_model` enters repeated `CONNECTION_ERROR` refreshes and `game_loop` ticks degrade into ~3s stalls; and the current large test suite still provides an over-broad green signal because startup/runtime contracts are underrepresented. I updated `docs/yu/plan.md` to make the next phase explicit: build a real `startup_smoke` layer for the current runtime path, add fail-fast handling for background asyncio task exceptions, modernize the outdated backend self-check path, fix disconnect/degraded-mode behavior, and queue the frontend task-panel collapse QoL behind the stability work. I also spun up distinct subagent audits for the frontend task collapse slice and the disconnect/degraded-mode path so the next implementation cuts can proceed without reopening the same broad architectural debate.

## [2026-04-11 17:34] DONE — Fixed Allied vehicle-counter drift and planner-side faction misclassification
Closed the follow-up that remained after Xi’s “3 commits clean + 1 residual blind spot” audit. There were actually two linked issues. First, `experts/knowledge.py::_COUNTER_TABLE` handled `vehicle`-heavy enemies with Soviet-only candidates (`v2rl`, `3tnk`), so explicit Allied requests still fell through to `None`; I added an Allied-only safe fallback (`e3` rocket infantry) while keeping the unknown-faction path conservative. Second, `ProductionAdvisor` could hide that blind spot by inferring the wrong side: `_planner_faction_hint()` only canonicalized through the demo-only resolver, so Allied-only ids like `tent` / `2tnk` were dropped and generic labels like `兵营` could leak into the Soviet path. I changed planner inference to use dataset-level canonical ids first and normalized `allies/soviets` to `allied/soviet` in the shared dataset faction helper. Regressions now cover explicit Allied vehicle-heavy, inferred-Allied vehicle-heavy, and normalized faction-hint behavior. Verification: `pytest tests/test_demo_capability_truth.py tests/test_planners.py -q` (`31 passed`).

## [2026-04-11 17:08] DONE — Re-bucketed `docs/yu/archive` by date and repaired active-path doc drift
Moved the remaining loose archive files under `docs/yu/archive/20260401/`, `20260402/`, `20260406/`, and `20260409/` so the archive root now only carries the index plus date buckets instead of a flat pile of old reports. In the same pass I repaired the active-path references that had drifted after the earlier archive move: `README.md` and `PROJECT_STRUCTURE.md` now point at the live execution board plus the archived long-form roadmap, and the surviving `docs/yu`/archive cross-links now follow the new dated paths. I also updated `docs/yu/plan.md` to add the three concrete missing follow-up slices that were already identified in audit notes but not reflected in the live plan: capability prompt truth alignment, reservation-contract surfacing, and task triage/debug bundle surfacing.

## [2026-04-11 14:50] DONE — Normalized battlefield snapshot reads across Adjutant/coordinator surfaces
Added `BattlefieldSnapshot.from_mapping()` in `runtime_views.py` and switched `Adjutant` battlefield query/fallback shaping to the shared typed read model instead of duplicating manual dict normalization. This closes another coordinator/runtime contract drift point and adds explicit regressions for runtime-query normalization. Verification: `pytest tests/test_runtime_views.py tests/test_adjutant_coordinator.py tests/test_adjutant.py -q` (`90 passed`).

## [2026-04-11 15:05] DONE — Aligned planner knowledge with normalized demo truth and fixed stale tool-surface test assumptions
Reviewed the restored `experts/knowledge.py` / `experts/planners.py` changes from stash, kept the direction that matches current runtime truth, and landed them as a clean slice: opening order now follows the normalized demo broad phase instead of pretending faction-specific barr/tent support is already landed, and counter recommendations now filter faction-locked demo units through explicit faction truth. In the same pass I fixed `tests/test_tool_handlers.py`, which was still asserting the old “ordinary tasks expose production tools” world and therefore over-reporting green coverage. Verification: `pytest tests/test_planners.py tests/test_tool_handlers.py tests/test_kernel.py tests/test_task_agent.py -q` (`139 passed`).

## [2026-04-11 15:20] DONE — Restored hidden stash state, preserved managed-job resource-loss signals, and reduced docs/yu working-set noise
First restored the previously hidden stash state into the visible worktree and created explicit branch refs for both stash entries so no mixed in-progress work remains hidden behind `git stash`. Then fixed a real runtime contract hole from Xi's test audit: `_ManagedJob` now implements `emit_signal()`, so placeholder/fallback jobs no longer silently drop `RESOURCE_LOST` signals before a real Expert exists. Added a Kernel integration regression for this path and re-ran a cross-surface suite (`232 passed`) covering runtime views, Adjutant, planner/tool surface, Kernel, and TaskAgent. Finally archived a batch of one-off 2026-04-09 slice/audit notes under `docs/yu/archive/20260409/` so the top-level Yu docs only keep active execution documents.

## [2026-03-29 16:00] DONE — Initialize yu workspace files
Created `docs/yu/agents.md`, `docs/yu/plan.md`, and `docs/yu/progress.md`.

## [2026-03-29 22:10] DONE — Adversarial review of `docs/wang/design.md`
Read `docs/wang/design.md`, identified implementation-blocking gaps, wrote the findings to `docs/wang/design_review.md`, and sent Wang a summary via `agent-chat`. The most critical blocker is an unresolved contradiction between "experts own their own loop" and the later single-thread `GameLoop` design.

## [2026-03-30 00:25] DONE — Round 2 re-audit of revised `docs/wang/design.md`
Re-read the revised design after Wang addressed the original blocker set, walked the recon scenario step by step, wrote findings to `docs/wang/design_review_r2.md`, and sent Wang a summary via `agent-chat`. The original blocker class is largely closed, but 6 concrete implementation gaps remain, led by `Outcome.result` vs `JobStatus`, incomplete scenario field population, missing mid-task resource-request plumbing, cancel API mismatch, incomplete dashboard command ingress, and an actor-centric action model that does not cleanly cover queue-based actions.

## [2026-03-30 00:40] DONE — Round 3 re-audit of revised `docs/wang/design.md`
Reviewed the next design revision after Wang addressed the 6 Round 2 gaps, wrote findings to `docs/wang/design_review_r3.md`, and sent Wang a summary via `agent-chat`. The mainline recon scenario is now implementable, but 4 local blockers remain: broken cancel matching (`job.intent` missing and regex-like selector mismatch), abort outcomes being dropped on cancel/preempt paths, unspecified wait-timeout runtime mechanics, and incomplete outbound dashboard event schemas.

## [2026-03-30 01:05] DONE — Round 4 final audit of revised `docs/wang/design.md`
Reviewed the next design revision after Wang addressed the 4 Round 3 gaps, walked the recon scenario and event schemas again, wrote findings to `docs/wang/design_review_r4.md`, and prepared the summary for Wang. The design is now close, but I did not clear it as zero blockers: 2 implementation blockers remain around the mid-task resource request / wait-queue contract and the preempt path's missing standard outcome semantics.

## [2026-03-30 01:35] DONE — Round 5 final audit of revised `docs/wang/design.md`
Reviewed the next design revision after Wang addressed the 2 Round 4 blockers and 2 minor inconsistencies, wrote findings to `docs/wang/design_review_r5.md`, and prepared the summary for Wang. I cleared the design as zero blockers: the mid-task resource request contract and preempt semantics are now concrete enough to implement. I also noted 2 residual editorial nits in the walkthrough that do not affect implementability.

## [2026-03-30 03:10] DONE — Brain-cerebellum architecture research for LLM+Expert coordination
Read `docs/wang/design.md` §0 and §1, then did web-backed research across robotics, agent runtimes, and game AI. Wrote findings to `docs/wang/brain_cerebellum_research.md` and prepared the summary for Wang. The strongest cross-domain conclusion is that OpenRA should use an event-driven supervisory LLM over autonomous Experts, with typed signals, runtime-injected context packets, and bounded autonomy instead of a flat tool-calling loop.

## [2026-03-30 03:45] DONE — Task Agent framework research with latency-first evaluation
Read the updated Task Agent sections of `docs/wang/design.md`, then researched raw provider SDKs, Claude Agent SDK, LangGraph, AutoGen Core, CrewAI, PydanticAI, and OpenAI Agents SDK using official docs. Wrote findings to `docs/wang/agent_framework_research.md` and prepared the summary for Wang. My recommendation is to build a custom minimal event-driven loop on the raw provider SDK, with PydanticAI as the best framework fallback if we want typed deps/tools without taking on a full orchestration runtime.

## [2026-03-30 04:15] DONE — Audit clean-rewritten `design.md` with four scenario walkthroughs
Re-read the clean rewrite of `docs/wang/design.md`, audited it for implementation gaps, then walked through four requested scenarios: tank production, global retreat, chase constraint, and multi-job surround. Wrote findings to `docs/wang/design_review_scenarios.md` and prepared the summary for Wang. The architecture direction is solid, but 7 concrete blockers remain, concentrated in missing Task Agent tool contracts, missing terminal/result semantics, an underspecified constraint lifecycle, actor-centric resource semantics that do not cover production well, missing global control semantics, missing multi-job coordination primitives, and no explicit Brain-side target-resolution path.

## [2026-03-30 04:40] DONE — Round 2 scenario audit of updated `design.md` with scenarios A-E
Re-ran scenarios A-D against the updated spec and added scenario E, then wrote findings to `docs/wang/design_review_scenarios_r2.md` and prepared the summary for Wang. The update closes most structural gaps, and scenario D is now mainline-implementable. But A-D are not yet zero blockers: A still lacks economy/production runtime semantics, B still lacks a retreat-capable executor/config, C still has a local `create_constraint` vs `enforcement` gap, and E is blocked by the absence of repair-capable executor/config plus no-repair-facility fallback semantics.

## [2026-03-30 05:05] DONE — Round 3 scenario audit of updated `design.md` with scenarios A-F
Re-ran scenarios A-E against the next design revision and added scenario F (`BASE_UNDER_ATTACK` passive trigger), then wrote findings to `docs/wang/design_review_scenarios_r3.md` and prepared the summary for Wang. The revision closes the previous A-D gaps in substance: A-D now have zero hard blockers on their mainline paths. Remaining blockers are concentrated in E’s no-repair-facility fallback policy and F’s missing authority/preemption policy for auto-created defense tasks competing with ongoing attack tasks.

## [2026-03-30 05:30] DONE — Round 4 scenario audit of updated `design.md` with scenarios A-G
Re-ran scenarios A-F against the next design revision and added scenario G (new-base expansion at the right-side ore field), then wrote findings to `docs/wang/design_review_scenarios_r4.md` and prepared the summary for Wang. The revision closes the previous E/F gaps in substance, so A-F now have zero hard blockers on their mainline paths. Remaining blockers are concentrated in G: `DeployExpert` is referenced without a config/runtime contract, the no-MCV prerequisite branch has no policy, and contested expansion-site handling is still underspecified.

## [2026-03-30 05:55] DONE — Round 5 scenario audit of updated `design.md` with scenarios A-H
Re-ran scenarios A-G against the next design revision and added scenario H (rapid sequential command submission), then wrote findings to `docs/wang/design_review_scenarios_r5.md` and prepared the summary for Wang. The revision closes the previous G gaps in substance, so A-G now have zero hard blockers on their mainline paths. The remaining blocker is in H: the spec still does not explicitly state the default scope rule for bare natural-language constraint commands like “别追太远” when multiple tasks already exist.

## [2026-03-30 06:20] DONE — Round 6 scenario audit of updated `design.md` with scenarios A-I
Re-ran scenarios A-H against the next design revision and added scenario I (no new player commands, but emerging strategic opportunity), then wrote findings to `docs/wang/design_review_scenarios_r6.md` and prepared the summary for Wang. The new `scope=global` rule closes H in substance, so A-H now have zero hard blockers on their mainline paths. One hard blocker remains in I: the spec still does not define whether no-command strategic opportunities are handled by bounded proactive autonomy, a standing background task, Kernel-created strategic rules, or recommendation-only escalation.

## [2026-03-30 06:35] DONE — Round 7 scenario audit of updated `design.md` with scenarios A-I
Re-ran scenarios A-I against the next design revision after Wang added explicit `execute / query / proactive notification` input paths and fixed the no-command authority model, then wrote findings to `docs/wang/design_review_scenarios_r7.md` and prepared the summary for Wang. I cleared all nine scenarios as zero hard blockers on their mainline paths. I also noted one residual editorial inconsistency in the recon walkthrough (`cancel_task` vs `cancel_tasks`) that does not affect implementation readiness.

## [2026-03-30 06:55] DONE — Full audit of `design.md` test checklist T1-T11
Read the full revised `docs/wang/design.md`, then verified checklist items T1-T11 against the formal data models, component responsibilities, and interface sections. Wrote findings to `docs/wang/design_review_checklist.md` and prepared the summary for Wang. I did not find a regression to the earlier scenario-level architecture blockers; the main issues are checklist-to-spec consistency gaps: undefined proactive-notification trigger types, missing dashboard/player output channels for notifications and query replies, an undefined `CommandProcessor` boundary, and checklist steps that use query/config contracts more concretely than the formal spec currently defines.

## [2026-03-30 07:10] DONE — Final consistency audit of `design.md` + `test_scenarios.md`
Read the cleaned `docs/wang/design.md` and the new standalone `docs/wang/test_scenarios.md`, then verified whether the 6 prior checklist-consistency gaps were actually closed. Wrote findings to `docs/wang/design_review_final.md` and prepared the summary for Wang. The structural gaps are now largely resolved: event types, `CommandProcessor`, `query_world` vocabulary, and outbound notification/query channels are all explicitly modeled. One prior gap remains only partially closed: several test rows still use shorthand or incomplete configs (`last_attack_target`, missing `max_chase_distance`, omitted `repeat`, and `...` placeholders) despite the design's strong-typed schema claim.

## [2026-03-30 07:25] DONE — Audit of Adjutant interaction layer in `design.md` §6
Read the revised `docs/wang/design.md` with the new Adjutant section and checked it against the rest of the runtime contracts plus the three requested routing scenarios. Wrote findings to `docs/wang/adjutant_review.md` and prepared the summary for Wang. The Adjutant direction is architecturally sound, but I did not clear it as implementation-ready: there is a direct role contradiction around query answering vs “Adjutant does not understand game details / make tactical decisions”, and the new dialogue layer still lacks a formal structured message contract, timeout ownership, and deterministic simultaneous-question routing rules.

## [2026-03-30 07:35] DONE — Re-audit of Adjutant interaction layer after four fixes
Re-read the revised `docs/wang/design.md` §6 after Wang addressed the four Adjutant gaps, wrote findings to `docs/wang/adjutant_review_r2.md`, and prepared the summary for Wang. I cleared the Adjutant layer as zero blockers for the previously reported issues: query handling is now framed as an independent query-LLM call, `TaskMessage` / `PlayerResponse` schemas are explicit, Kernel owns question timeouts with a late-reply policy, and simultaneous-question routing now has deterministic priority-based fallback rules. I noted only two non-blocking doc nits around `question_id` vs `message_id` wording and the sample `pending_questions` object lagging behind the formal schema.

## [2026-03-30 08:05] DONE — Audit of `design.md` updates + new `implementation_plan.md`
Read the updated `docs/wang/design.md` and new `docs/wang/implementation_plan.md`, then checked whether the implementation plan fully covers the revised design surface. Wrote findings to `docs/wang/impl_plan_review.md` and prepared the summary for Wang. The design updates themselves are directionally sound (`review_interval`, multi-turn Task-Agent loop, global timestamp requirement), but the implementation plan still misses four concrete work areas: backend WebSocket/message-gateway tasks, Adjutant-specific routing tests, explicit ownership for periodic `review_interval` wake scheduling, and timestamp propagation tasks for all outward-facing payloads rather than logs alone.

## [2026-03-30 09:00] DONE — Removal audit for the `the-seed` submodule
Inspected the `the-seed/` directory structure, cross-referenced its contents against the rewritten architecture, enumerated main-project imports that still reference it, and wrote findings to `docs/wang/theseed_removal_audit.md`. My conclusion is that the framework itself is not worth preserving for the rewrite: the only clear pre-removal extraction target is a small subset of OpenRA NLU rule assets still used by `nlu_pipeline` scripts, plus optional temporary compatibility for `ExecutionResult` if the old regression/smoke scripts must remain runnable during transition.

## [2026-03-30 10:30] DONE — Deep audit of updated `design.md` + `implementation_plan.md`
Read the latest `docs/wang/design.md` and `docs/wang/implementation_plan.md` after Wang integrated 5 new requirements, then wrote findings to `docs/wang/deep_audit_final.md` and prepared the summary for Wang. The design decisions themselves are mostly coherent, and the plan closed several earlier gaps (`1.6`, `1.7`, `1.8`, `4.4`, `5.2`, `0.5`). Remaining issues are now concentrated in integration quality: the new model-abstraction layer `0.4` is not yet threaded through all model-using tasks, timestamp propagation to LLM-facing context is not explicitly planned, benchmark instrumentation still sits too late relative to model evaluation/milestones, `5.7` voice dependencies are wrong, and the critical-path/milestone summary is stale relative to the detailed task table.

## [2026-03-30 10:40] DONE — Re-audit of `implementation_plan.md` after five fixes
Re-read the revised `docs/wang/implementation_plan.md` against the latest `docs/wang/design.md`, then wrote findings to `docs/wang/impl_plan_review_r2.md` and prepared the summary for Wang. I cleared all five previously reported planning gaps: `0.4` model abstraction is now threaded through the model-using tasks, benchmark requirements are integrated into milestone tests and model evaluation, timestamp propagation explicitly covers LLM context, `5.7` voice dependencies are corrected, and the key path/milestone summary now matches the detailed task table. I noted only two non-blocking nits: `Qwen3.5（暂定）` still appears as the default in the tech table despite the new abstraction, and the critical path remains intentionally simplified rather than showing every edge.

## [2026-03-30 11:20] DONE — Phase 0.5 benchmark framework
Implemented `benchmark/` as a standalone timing package with `@timed(...)`, `span(...)`, in-memory record storage, tag/time/top-N queries, and JSON export. Verified the acceptance path with inline Python: decorating a function with `@timed("llm_call")` records a queryable result, and additional checks confirmed top-N slowest ordering plus JSON export. `pytest` could not be used because the current environment does not have it installed (`python -m pytest` -> `No module named pytest`).

## [2026-03-30 11:40] DONE — Phase 0.1 cleanup of legacy standalone/demo surfaces
Deleted Wang-approved legacy files that still existed in the tree: `openra_api/game_midlayer.py`, `openra_api/jobs_main.py`, `main_legacy.py`, `test_simple.py`, `test_legacy.py`, `tactical_core/launcher.py`, `tactical_core/ui.py`, `openra_state/visualize_intel.py`, and `web-console/console.html`. Repaired the only direct code import break introduced by these deletions by switching `adapter/openra_env.py` to `openra_api.rts_middle_layer`, and updated `web-console/start-vnc.sh` to advertise `index.html` instead of the removed page. Static grep confirmed no remaining code imports of the deleted modules; a dynamic import check then exposed a pre-existing root cause for the next task: runtime imports still rely on `the_seed`, which is not importable from the repo root unless its submodule path is injected.

## [2026-03-30 12:20] DONE — Phase 0.1b removal of the `the-seed` submodule
Migrated the OpenRA NLU rule assets into `nlu_pipeline/rules/` plus `nlu_pipeline/templates/commands/`, updated all `nlu_pipeline/` imports off `the_seed`, and added a small local `the_seed/` compatibility package so remaining legacy runtime imports (`adapter.openra_env`, `agents.nlu_gateway`, `main.py`) stay importable after submodule removal. Then removed the git submodule with `git rm -r -f the-seed`, leaving `.gitmodules` with only `OpenCodeAlert`. Verification: `rg -n "the_seed" nlu_pipeline` returns no hits, and inline Python successfully imports `adapter.openra_env`, `agents.nlu_gateway`, and `main`, then routes a composite command through `nlu_pipeline.rules.CommandRouter` after the submodule directory is gone.

## [2026-03-30 12:40] DONE — Audit of xi's Phase 0 tasks 0.2/0.3/0.4
Audited `models/*`, `llm/provider.py`, and the directory skeleton against `docs/wang/design.md`, then wrote findings to `docs/wang/xi_phase0_audit.md`. Confirmed that the `AutonomyMode` drift is gone and `TaskKind` is correctly reduced to `instant` / `managed`, and verified locally that `MockProvider` is testable. I did not clear the work as zero-gap: two blockers remain, namely that `Job.config` is not mechanically bound to `expert_type`, and `AnthropicProvider` does not correctly convert a multi-turn tool-use transcript from the abstraction's OpenAI-style message format.

## [2026-03-30 12:50] DONE — Regression audit of xi's Phase 0 audit fixes
Re-audited xi's follow-up fix commit `1f5a7ce`, wrote the result to `docs/wang/xi_phase0_audit_r2.md`, and verified locally that `validate_job_config()` accepts matching config/expert pairs, rejects mismatches, and that the enum-typed config fields now preserve `EngagementMode` / `MoveMode` values at runtime. I cleared the requested Phase 0 follow-up fixes as zero blockers; the deferred Anthropic multi-turn gap is now explicitly documented in `llm/provider.py` per Wang's decision.

## [2026-03-30 15:55] DONE — Audit of xi's Task 1.4 Task Agent implementation
Audited xi commit `d321a3e` (`task_agent/` + `tests/test_task_agent.py`), wrote findings to `docs/wang/task_agent_audit.md`, and re-ran `python3 tests/test_task_agent.py` locally with all 11 tests passing. I did not clear the Task Agent loop as implementation-ready: the wake path currently drops routed `Event` objects before they reach the LLM context, and `default_if_timeout` is only logged on LLM failure instead of being applied. I also noted one contract-level mismatch where `create_constraint` does not require `enforcement` even though the design contract does.

## [2026-03-30 16:00] DONE — Regression audit of xi's Task 1.4 fixes
Re-audited xi commit `ccbf442`, wrote the result to `docs/wang/task_agent_audit_r2.md`, and re-ran `python3 tests/test_task_agent.py` locally with all 13 tests passing. I cleared the previously reported `2 blockers + 1 should-fix`: routed `Event` objects now enter the LLM context, `default_if_timeout` now executes a real tool path on LLM failure, and `create_constraint.enforcement` is now required by schema. I left only one integration note: the exact downstream semantics of `patch_job(..., {"decision_response": default})` remain a later Job-side concern rather than a Task Agent blocker.

## [2026-03-31 00:16] DONE — Phase 1.1 `WorldModel v1`
Implemented `world_model/core.py` with a mock-first `WorldModel`, `GameAPIWorldSource`, layered refresh scheduling (`0.1s / 0.5s / 1.0s`), a normalized actor registry, unified query API, runtime state tracking, snapshot diff to `Event[]`, and benchmark instrumentation via `@timed("world_refresh")`. Added `tests/test_world_model.py` and verified it with `python3 tests/test_world_model.py` (4 tests passed), then re-ran `python3 tests/test_task_agent.py` (13 tests passed) to confirm no regression. The main design choice was to suppress event emission on the initial baseline refresh so `Event[]` only represents diffs from an already-known snapshot rather than first-load state.

## [2026-03-31 00:32] DONE — Phase 1.3a `Kernel Task lifecycle`
Implemented `kernel/core.py` with deterministic Task/Job lifecycle management: `create_task`, `cancel_task`, `cancel_tasks`, `start_job`, `abort_job`, `patch_job`, `pause_job`, `resume_job`, `complete_task`, TaskAgent instance management, TaskAgent tool handlers, and `route_event` / `route_signal` skeletons. Added `tests/test_kernel.py` and verified `python3 tests/test_kernel.py` (4 tests passed), `python3 -m py_compile kernel/core.py tests/test_kernel.py`, plus regression runs of `python3 tests/test_world_model.py` and `python3 tests/test_task_agent.py`. Root causes fixed during verification: Kernel-level pause/resume was not normalizing `JobStatus`, abort path resource revocation was overwriting `ABORTED` back to `WAITING`, and late signals from finalized tasks needed to be dropped at `route_signal()`.

## [2026-03-31 00:32] DONE — Audit of xi `2.1 Expert Base` + `1.2 GameLoop`
Audited xi commit `e2a62cb` (`experts/base.py`, `tests/test_expert_base.py`) and commit `bd0f4c6` (`game_loop/loop.py`, `tests/test_game_loop.py`), then verified both test files locally (`11` + `7` tests passed). I did not clear the work as integration-ready: `BaseJob.abort()` can be overwritten back to `WAITING` by later resource revocation, `GameLoop` currently expects `kernel.route_events(...)` while the live Kernel surface is `route_event(...)`, and `GameLoop` double-routes events with the current `WorldModel.refresh()` + `detect_events()` contract. I also noted one secondary design-quality issue: `BaseJob.pause()/resume()` do not mechanically update `status`, so callers must remember to keep model state in sync themselves.

## [2026-03-31 00:38] DONE — Kernel follow-up fixes from concentrated audit
Applied Wang-requested follow-up fixes in `kernel/core.py`: added `route_events(self, events)` as the batch adapter over `route_event`, and replaced `_config_from_payload()`'s fragile `validate_job_config.__globals__[...]` lookup with a direct import of `EXPERT_CONFIG_REGISTRY` from `models.configs`. Added a regression case to `tests/test_kernel.py` for the batched event entrypoint and re-verified `python3 tests/test_kernel.py` (5 tests passed), `python3 tests/test_world_model.py` (4 tests passed), `python3 tests/test_task_agent.py` (13 tests passed), plus `python3 -m py_compile kernel/core.py tests/test_kernel.py`.

## [2026-03-31 00:38] DONE — Regression audit of xi fix `001feec`
Re-audited xi's follow-up fix for the Batch 2 findings and wrote the result to `docs/wang/xi_batch2_audit_r2.md`. The original `on_resource_revoked()` terminal-state overwrite bug is fixed, and GameLoop no longer double-routes `WorldModel` events. I did not clear the whole fix set as zero blockers because the new `BaseJob.resume()` implementation now unconditionally restores `RUNNING`, which means an already `ABORTED` job can be resurrected; this is not covered by the current expert-base tests.

## [2026-03-31 01:00] DONE — Final regression audit of xi fix `af8d700`
Re-audited xi's final expert-base fix and wrote the result to `docs/wang/xi_batch2_audit_r3.md`. I verified `python3 tests/test_expert_base.py` with all 12 tests passing, confirmed the new `test_abort_then_resume_no_revive` covers the previous blocker, and ran an extra runtime check showing `resume()` now preserves all three terminal states (`aborted / succeeded / failed`). This clears the last remaining blocker in the Batch 2 concentrated audit chain on my side.

## [2026-03-31 01:08] DONE — Batch 3 `1.3b + 1.3c` Kernel resource allocation and event routing
Extended `kernel/core.py` from lifecycle-only orchestration into actual resource and routing logic: job `ResourceNeed` registration/inference, actor and production-queue matching via `WorldModel`, priority-based allocation, lower-priority preemption, auto-replacement after `UNIT_DIED`, player-notification buffering, `BASE_UNDER_ATTACK` auto-task creation, and concrete routing rules for all nine `EventType` values. Expanded `tests/test_kernel.py` to cover actor matching, production-queue matching, single-resource preemption, multi-resource degradation, death-triggered replacement, broadcast-vs-notification routing, and batched event entry. Verified `python3 tests/test_kernel.py` with 9 tests passing, alongside prior regression runs of the world/task/game/expert suites and `python3 -m py_compile kernel/core.py tests/test_kernel.py`.

## [2026-03-31 11:43] DONE — Audit of xi `1.5 Task tools + 1.7 timestamp`
Audited xi commit `ddd5004` (`task_agent/handlers.py`, `tests/test_tool_handlers.py`), wrote findings to `docs/wang/xi_task_tools_audit.md`, and re-ran `python3 tests/test_tool_handlers.py` locally with all 8 tests passing. I did not clear the work as zero-gap: `create_constraint` / `remove_constraint` in `TaskToolHandlers` are still TODO stubs that return timestamped success without mutating Kernel or WorldModel state, even though the live Kernel already exposes real constraint handlers. Root cause: the current tests only assert timestamp presence for those two tools, and `KernelLike` omits the constraint methods entirely, so the no-op implementation passes both typing and test coverage.

## [2026-03-31 11:44] DONE — Regression audit of xi constraint fix `99a9291`
Re-audited xi's follow-up fix for the `1.5 Task tools + 1.7 timestamp` findings and wrote the result to `docs/wang/xi_task_tools_audit_r2.md`. I verified `python3 tests/test_tool_handlers.py` with all 9 tests passing, confirmed `WorldModelLike` now declares the constraint methods the handlers actually use, and re-ran a live `Kernel + WorldModel + TaskToolHandlers` repro showing `create_constraint` now populates `world.query("constraints")` and `remove_constraint` clears it. This closes the previous `1 blocker + 1 should-fix` with zero remaining blockers on my side.

## [2026-03-31 11:47] DONE — Batch 4 `1.3d + 1.3e` Kernel timeout/cancel and auto-response rules
Extended `kernel/core.py` with deterministic pending-question ownership in Kernel: `register_task_message()` now records `task_question` messages, `tick()` expires them by deadline, timeout injects a default `PlayerResponse`, late replies return the explicit `"已按默认处理，如需更改请重新下令"` path, and task cancel/complete now clear pending questions. I also added an extensible auto-response rule registry and registered the default `BASE_UNDER_ATTACK -> defend_base(priority=80)` rule there instead of hardcoding the behavior in the event switch. Verification: `python3 tests/test_kernel.py` (12 passed), `python3 tests/test_world_model.py` (4 passed), `python3 tests/test_task_agent.py` (13 passed), and `python3 -m py_compile kernel/core.py tests/test_kernel.py`. One integration boundary remains explicit by design: Kernel now delivers `PlayerResponse` via an agent hook when available, while xi's Adjutant / WS side will consume the pending-question surfaces next.

## [2026-03-31 11:53] DONE — Audit of xi `1.6 WS backend + 1.8 review_interval`
Audited xi commit `2728463` (`ws_server/server.py`, `game_loop/loop.py`, `tests/test_ws_and_review.py`), wrote findings to `docs/wang/xi_batch4_audit.md`, and verified `python3 tests/test_ws_and_review.py` (7 passed), `python3 tests/test_game_loop.py` (7 passed), and `python3 -m py_compile ws_server/server.py game_loop/loop.py tests/test_ws_and_review.py`. I did not clear the work as zero-gap: `GameLoop` never calls the new `Kernel.tick()` added in Batch 4, so pending-question timeouts do not run in the live main loop, and the new review wake path is racy because `GameLoop` sets `AgentQueue._wake_event` directly while `wait_for_wake()` clears any pre-existing state before waiting. Root cause: the `1.8` review scheduling was added as a local queue/event tweak without re-integrating against the now-expanded Kernel tick contract or the real `TaskAgent` sleep semantics.

## [2026-03-31 11:56] DONE — Regression audit of xi Batch 4 fix `8e594c4`
Re-audited xi's follow-up fix for the Batch 4 findings and wrote the result to `docs/wang/xi_batch4_audit_r2.md`. I verified `python3 tests/test_ws_and_review.py` and `python3 tests/test_game_loop.py` both still pass, and confirmed with a live `Kernel + GameLoop` repro that `GameLoop._tick()` now drives `Kernel.tick(now)` so pending-question timeouts actually expire and inject default `PlayerResponse` values. I did not clear the whole fix set as zero blockers because the new `trigger_review()` sentinel path still loses a pre-wait wake: if review is triggered just before `wait_for_wake()`, the method clears the event, returns `False`, and only processes the sentinel on the later timeout path. Root cause: the queue still treats wake as an edge-triggered event plus a filtered sentinel, instead of a level/persistent condition visible to `wait_for_wake()` itself.

## [2026-03-31 12:00] DONE — Final regression audit of xi Batch 4 review wake fix `0ba9207`
Re-audited xi's final queue-side fix and wrote the result to `docs/wang/xi_batch4_audit_r3.md`. I verified `python3 tests/test_ws_and_review.py` (7 passed), `python3 tests/test_game_loop.py` (7 passed), `python3 tests/test_task_agent.py` (13 passed), and `python3 -m py_compile task_agent/queue.py tests/test_ws_and_review.py tests/test_game_loop.py tests/test_task_agent.py`. I also re-ran the exact narrow-window repro from the previous round: `trigger_review()` followed immediately by `wait_for_wake()` now returns `True`, and a real `GameLoop`-driven review wake also returns `True`. This clears the last remaining Batch 4 blocker on my side.

## [2026-03-31 12:07] DONE — Batch 5 `1.3f` Kernel + WorldModel error recovery
Implemented error recovery across `world_model/core.py`, `game_loop/loop.py`, and `kernel/core.py`. `WorldModel` now tracks refresh health (`stale`, consecutive failures, last error, threshold) while preserving the last good snapshot on refresh failures; `GameLoop` now reacts to stale refresh health by pausing registered jobs, notifying the player once the consecutive-failure threshold is crossed, and resuming only the jobs it paused once refreshes recover; and job tick exceptions are now converted into `Signal(task_complete, result=failed)` with error payload instead of only being logged. I also added `Kernel.push_player_notification(...)` as the deterministic player-notification outlet for this path. Verification: `python3 tests/test_world_model.py` (5 passed), `python3 tests/test_game_loop.py` (9 passed), `python3 tests/test_kernel.py` (12 passed), `python3 tests/test_ws_and_review.py` (7 passed), `python3 tests/test_task_agent.py` (16 passed), and `python3 -m py_compile world_model/core.py game_loop/loop.py kernel/core.py tests/test_world_model.py tests/test_game_loop.py tests/test_ws_and_review.py`.

## [2026-03-31 02:09] DONE — Audit of xi `1.3f` Task Agent error recovery `2dc268f`
Audited `task_agent/agent.py` and `tests/test_task_agent.py`, wrote findings to `docs/wang/xi_batch5_task_agent_audit.md`, and prepared the summary for Wang. I did not clear this as `zero blockers`: repeated LLM failures are still not wired to any real player-notification channel. Root cause: `_notify_player_llm_failure()` calls `complete_task` with empty args and then only logs, even though `design.md` requires a structured player warning path (`TaskMessage` / `task_warning`) for repeated failures. I verified `python3 tests/test_task_agent.py` (16 passed), `python3 -m py_compile task_agent/agent.py tests/test_task_agent.py`, a live repro showing the warning path degenerates into bogus `complete_task({})` attempts, and a separate live repro confirming `_safe_wake_cycle()` does isolate a non-LLM exception and recover on the next wake. I also noted one non-blocking test gap: the new `error_isolation` test exercises `_call_llm()` failure handling, not the `_safe_wake_cycle()` generic-exception path it claims to cover.

## [2026-03-31 02:12] DONE — Regression audit of xi `1.3f` final fix `72c3c21`
Re-audited xi's follow-up fix for the Task-Agent-side `1.3f` findings and wrote the result to `docs/wang/xi_batch5_task_agent_audit_r2.md`. The non-LLM exception isolation test is now aligned with the actual `_safe_wake_cycle()` contract: it raises in `world_summary_provider`, and I verified both the test and a separate live repro recover correctly on the next wake. I did not clear the work as `zero blockers`, because the new repeated-failure warning path still has no real outward routing. Root cause: `_notify_player_llm_failure()` now creates `ExpertSignal(kind=RISK_ALERT)`, but pushes it only into the Task Agent's own inbound queue; Kernel signal routing is one-way `Kernel -> TaskAgent`, so no `TaskMessage` or player notification is ever emitted. I verified `python3 tests/test_task_agent.py` (16 passed), `python3 -m py_compile task_agent/agent.py tests/test_task_agent.py`, and a live `Kernel + TaskAgent + WorldModel` repro where repeated LLM failures end the task as `failed` while `kernel.list_task_messages(task_id)` and `kernel.player_notifications` both remain empty.

## [2026-03-31 02:17] DONE — Regression audit of xi player-warning callback fix `9882e5c`
Re-audited xi's callback-based follow-up for the final `1.3f` player-warning issue and wrote the result to `docs/wang/xi_batch5_task_agent_audit_r3.md`. The Task Agent now has the correct local API shape: it accepts `message_callback`, builds `TaskMessage(task_warning)`, and the updated unit test proves that path works when the callback is manually injected. I did not clear the work as `zero blockers`, because the live runtime integration is still missing. Root cause: `Kernel._default_task_agent_factory()` still constructs `TaskAgent(...)` without passing `message_callback=self.register_task_message`, so the new outbound warning hook is never wired in production. I verified `python3 tests/test_task_agent.py` (16 passed), `python3 -m py_compile task_agent/agent.py tests/test_task_agent.py task_agent/__init__.py`, and Wang's requested live repro (`Kernel + WorldModel + always-failing LLM`), which still ends with task status `failed` while `kernel.list_task_messages(task_id)` remains empty.

## [2026-03-31 02:20] DONE — Fixed Kernel wiring for TaskAgent player-warning callback
Patched `kernel/core.py` so the default TaskAgent factory now passes `message_callback=self.register_task_message` into `TaskAgent`. Root cause was purely assembly-level: xi's TaskAgent-side callback implementation was already correct, but Kernel never connected the callback in the live runtime. Verification: `python3 tests/test_kernel.py` (12 passed), `python3 tests/test_task_agent.py` (16 passed), `python3 -m py_compile kernel/core.py`, and Wang's requested live repro (`Kernel + WorldModel + always-failing LLM`) now shows task status `failed` with `kernel.list_task_messages(task_id)` containing two `task_warning` messages for the repeated LLM failures.

## [2026-03-31 02:28] DONE — Implemented `2.2 ReconExpert + ReconJob`
Added `experts/recon.py` with `ReconExpert` and `ReconJob`, and exported them from `experts/__init__.py`. The job now runs a small RTS-style scouting FSM: `searching` scores diagonal-biased waypoints from current `WorldModel` map extents, `tracking` follows enemy harvester clues or moves aggressively near likely enemy-base positions, and `retreating` triggers when HP falls below `retreat_hp_pct`. It emits `progress`, `risk_alert`, `target_found`, and final `task_complete` signals as appropriate, and declares its own `ResourceNeed` for one fast self-owned vehicle. Root cause/constraint noted: `WorldModel v1` does not yet expose explicit unexplored-cell coordinates, so the search phase uses deterministic waypoint heuristics over the available `map` metadata instead of per-cell frontier scoring. Verification: `python3 tests/test_recon_expert.py` (4 passed), `python3 tests/test_expert_base.py` (12 passed), `python3 tests/test_kernel.py` (12 passed), and `python3 -m py_compile experts/recon.py experts/__init__.py tests/test_recon_expert.py`.

## [2026-03-31 02:34] DONE — Implemented `2.3` T1 end-to-end milestone test + benchmark validation
Added `tests/test_e2e_t1.py` to exercise the full T1 chain on mocks: direct `Kernel.create_task("探索地图，找到敌人基地")`, real `TaskAgent` tool-use creating a `ReconExpert` job, Kernel resource allocation, `GameLoop`-driven `WorldModel` refresh + `ReconJob` ticks, `ENEMY_DISCOVERED` event history, `task_complete` signal routing back into the Task Agent, and final `complete_task(succeeded)`. Root cause addressed during test design: relying only on context-packet `jobs` inside the same wake caused the mock LLM to re-issue `start_job`, because TaskAgent does not rebuild context mid-wake; I fixed the scenario provider to track whether it had already started the recon job and only emit `complete_task` once it actually sees a `task_complete` signal in a later wake. The test also verifies benchmark visibility and JSON export for `world_refresh`, `llm_call`, `tool_exec`, and `job_tick`. Verification: `python3 tests/test_e2e_t1.py` (passed), `python3 -m py_compile tests/test_e2e_t1.py`, `python3 tests/test_kernel.py` (12 passed), `python3 tests/test_task_agent.py` (16 passed), and `PYTHONPATH=. python3 tests/test_benchmark.py` (path workaround for that standalone script).

## [2026-03-31 02:41] DONE — Implemented `3.1 EconomyExpert + EconomyJob`
Added `experts/economy.py` with `EconomyExpert` and `EconomyJob`, exported them from `experts/__init__.py`, and added `tests/test_economy_expert.py`. The job binds a `production_queue` resource, issues one production order at a time through `GameAPI.produce(...)`, tracks per-unit completion by scanning `WorldModel` `PRODUCTION_COMPLETE` events, emits `progress` for each finished unit, and completes with `task_complete(result=succeeded)` once the requested count is reached. It enters `waiting` instead of failing on low power, no funds, queue pause, or queue/facility loss. Root cause discovered and fixed during implementation: the base `BaseJob.do_tick()` skips all non-`RUNNING` states, which would make a waiting economy job unrecoverable, so `EconomyJob` overrides `do_tick()` to keep performing light recovery checks while in `WAITING`. A second bug surfaced in tests: an empty production queue was mistakenly treated as `queue_paused` because `all([])` returned true; I corrected the pause check to require a non-empty queue item list. Verification: `python3 tests/test_economy_expert.py` (4 passed), `python3 -m py_compile experts/economy.py experts/__init__.py tests/test_economy_expert.py`, `python3 tests/test_kernel.py` (12 passed), `python3 tests/test_expert_base.py` (12 passed), `python3 tests/test_recon_expert.py` (4 passed), and `python3 tests/test_e2e_t1.py` (passed).

## [2026-03-31 03:27] DONE — Implemented `4.5` Adjutant E2E coverage for T9-T11
Added `tests/test_e2e_adjutant.py` with three async end-to-end scenarios over `Adjutant + Kernel + WorldModel + GameLoop`: T9 query routing that answers directly and does not create a Task, T10 pending-question reply routing that reaches the owning Task Agent and patches its Job, and T11 mixed routing where a pending question coexists with a new command and later times out to its default response. The test also verifies benchmark visibility and JSON export for `llm_call`, `tool_exec`, `world_refresh`, and `job_tick`. Because the current base `TaskAgent` still lacks a native `push_player_response()` intake surface, the E2E harness uses a small test-only `ReplyAwareTaskAgent` adapter that re-injects `PlayerResponse` into the normal wake loop without modifying production code. Root cause found during verification: my first test draft hard-coded `TaskMessage.timestamp=100.0`, which made `Kernel.submit_player_response()` immediately treat the question as timed out against wall-clock time; switching to live timestamps fixed the routing failure. Verification: `python3 tests/test_e2e_adjutant.py` (3 passed), `python3 -m py_compile tests/test_e2e_adjutant.py`, `python3 tests/test_adjutant.py` (10 passed), and `python3 tests/test_adjutant_routing.py` (10 passed).

## [2026-03-31 03:27] DONE — Audited xi Phase 2-4 batch (`4.1+4.3+4.4+3.2+3.4+3.3+3.5`)
Audited Adjutant, notifications, Movement/Deploy/Combat experts, and the T2-T8 milestone tests, then wrote the report to `docs/wang/xi_phase24_audit.md`. I did not clear the batch as zero-gap. The main root cause is contract drift hidden by mocks: `MovementExpert`, `DeployExpert`, and `CombatExpert` are written against ad-hoc `move_actors` / `deploy_actor` / list-based `attack_target` methods that do not exist on the shared `openra_api.GameAPI`, and `tests/test_e2e_experts.py` uses a fallback `MockKernel.start_job()` path that can pass without instantiating any real expert runtime. I also reproduced a separate runtime bug in `MovementJob`: if all assigned actors disappear from `WorldModel`, `_all_arrived()` returns `True` and the job emits `task_complete(result="succeeded")` instead of failing or waiting. Verification: `python3 tests/test_adjutant.py`, `python3 tests/test_adjutant_routing.py`, `python3 tests/test_movement_deploy.py`, `python3 tests/test_combat.py`, `python3 tests/test_e2e_experts.py`, plus live Python repros for the API-contract mismatch and the movement false-success path.

## [2026-03-31 03:41] DONE — Regression audit of xi Phase 2-4 fixes `505a044`
Re-audited xi's follow-up fix commit `505a044` and wrote the result to `docs/wang/xi_phase24_audit_r2.md`. I confirmed two prior blockers are closed: the Movement/Deploy/Combat experts now call the real `GameAPI`-shape methods (`move_units_by_location`, `deploy_units`, `attack_target(Actor, Actor)`), and `MovementJob._all_arrived()` no longer treats “all actors missing” as success. I did not clear the batch as `zero blockers`, because the claimed `3.5` E2E fix was not actually implemented: `tests/test_e2e_experts.py` is unchanged, still uses `MockKernel`, and still allows fallback fake jobs instead of the shared runtime chain. I also verified the notification retry should-fix is still incomplete: `NotificationManager` now rewinds by `failed_count`, but if a sink failure happens in the middle of a batch it skips the failed item and duplicates later successful items instead of retrying the exact failed notification.

## [2026-03-31 03:47] DONE — Final regression audit of xi Phase 2-4 fixes `47a783d`
Re-audited xi's claimed final Phase 2-4 fix commit `47a783d` and wrote the result to `docs/wang/xi_phase24_audit_r3.md`. I confirmed the notification retry root cause is now closed: `NotificationManager` switched from count-based progress tracking to per-index success tracking, and a mid-batch sink failure now retries the failed notification without duplicating later successful ones. I did not clear the whole batch as `zero blockers`, because the rewritten `tests/test_e2e_experts.py` still leaves one false-green path in place: `make_kernel()` registers only `MovementExpert`, `DeployExpert`, and `CombatExpert`, so T2 starts an `EconomyExpert` job that falls through live Kernel's `_make_job_controller()` fallback to `_ManagedJob`. Root cause: the test suite is more real than before, but it still over-claims "real Experts + real TaskAgent" while bypassing the actual TaskAgent loop and leaving one scenario off the real expert runtime.

## [2026-03-31 03:52] DONE — Final regression audit of xi EconomyExpert registration fix `749e156`
Re-audited xi's follow-up fix commit `749e156` and wrote the result to `docs/wang/xi_phase24_audit_r4.md`. I confirmed the last remaining Phase 2-4 blocker is closed: `tests/test_e2e_experts.py` now imports and registers `EconomyExpert`, the mock GameAPI exposes the production methods that `EconomyJob` needs, and the T2 live repro now produces a real `EconomyJob` controller instead of falling back to `_ManagedJob`. Verification: `python3 tests/test_e2e_experts.py`, `python3 tests/test_economy_expert.py`, and a direct `start_job(\"EconomyExpert\", ...)` runtime repro checking the concrete controller class. I left one non-blocking note in the report that the file still uses a test-side `RecordingAgent` and direct tool execution rather than the full TaskAgent/GameLoop runtime, but Wang's requested blocker is now definitively closed.

## [2026-03-31 04:00] DONE — Implemented Phase 6 structured logging (`6.1+6.2+6.3`)
Added a new `logging_system/` package with an in-memory structured JSON log store, per-component structured loggers, query/replay/export helpers, benchmark summary/export helpers, and a benchmark subscriber bridge that records each benchmark span as a structured `benchmark` log event. Root cause addressed: runtime observability was fragmented across ad-hoc stdlib logging and benchmark timings, with no unified replay/export surface. I then added minimal instrumentation at stable chokepoints instead of refactoring business logic: `Kernel` task/job/event/resource lifecycle, `TaskAgent` wake/LLM path, `ToolExecutor`, `BaseJob.emit_signal` and resource callbacks, `WorldModel.refresh`, `GameLoop`, and `Adjutant` input classification. Verification: `python3 tests/test_logging_system.py`, `python3 tests/test_kernel.py`, `python3 tests/test_task_agent.py`, `python3 tests/test_world_model.py`, `python3 tests/test_game_loop.py`, `python3 tests/test_adjutant.py`, `python3 tests/test_adjutant_routing.py`, `python3 tests/test_e2e_t1.py`, `python3 tests/test_e2e_adjutant.py`, `python3 tests/test_benchmark.py`, and `python3 -m py_compile` over the new/modified logging files. I also fixed `tests/test_benchmark.py`'s long-standing standalone import-path issue while verifying the benchmark side of the framework.

## [2026-03-31 04:04] DONE — Audit of xi Phase 5 Vue 3 dashboard `f9505a1`
Audited xi's Phase 5 Vue 3 dashboard commit `f9505a1` and wrote the result to `docs/wang/xi_phase56_audit.md`. I did not clear it as `zero-gap`: three implementation blockers remain. Root causes: the pending-question UI is only a visual stub with no real WS/back-end reply protocol; `OpsPanel` does not actually wire VNC, service control, or backend `mode_switch`; and the benchmark pane depends on an undeclared `world_snapshot.data.benchmark` payload that `ws_server` does not define as a stable contract. I also noted three should-fix issues: frozen time-ago labels, `useWebSocket.disconnect()` still triggering reconnect, and `DiagPanel` assuming log fields that do not match the real structured logging schema. Verification: `npm run build` in `web-console-v2/`, plus source-level protocol comparison against `ws_server/server.py`.

## [2026-03-31 04:14] DONE — Regression audit of xi Phase 5 fixes `074a5e0`
Re-audited xi's follow-up dashboard fix commit `074a5e0` and wrote the result to `docs/wang/xi_phase56_audit_r2.md`. I confirmed several claimed fixes are real: `TaskPanel` now emits structured `question_reply` messages, `ws_server` routes that inbound message type, `OpsPanel` sends `mode_switch`, the VNC URL is configurable, `send_benchmark()` exists on the WS server, `DiagPanel` maps incoming `log_entry` payloads closer to the structured logging schema, and intentional disconnects no longer auto-reconnect. I did not clear the work as `zero blockers`, because two contract gaps remain live. First, `App.vue`'s top-level mode toggle still only mutates local frontend state and never sends `mode_switch`, so UI mode and backend mode can still diverge. Second, `TaskPanel` now consumes `msg.data.pending_questions` from `task_list`, but `ws_server.send_task_list()` still broadcasts only `{\"tasks\": tasks}`, so one of xi's claimed pending-question fill paths is not actually wired. I also left two should-fix notes: time-ago labels are still frozen because the reactive `useTimeAgo()` ticker is unused, and `DiagPanel`'s CSS still keys off lowercase log levels while the structured logger emits uppercase `ERROR/WARN`. Verification: `npm run build` in `web-console-v2/`, `python3 tests/test_ws_and_review.py` (7 passed), and direct source/protocol comparison across `App.vue`, `TaskPanel.vue`, `DiagPanel.vue`, and `ws_server/server.py`.

## [2026-03-31 04:20] DONE — Final regression audit of xi Phase 5 fixes `9a0158e`
Re-audited xi's follow-up dashboard fix commit `9a0158e` and wrote the result to `docs/wang/xi_phase56_audit_r3.md`. I cleared this round as `zero blockers`: `App.vue`'s header toggle now sends `mode_switch`, and `ws_server.send_task_list()` now accepts `pending_questions`, which closes the two live contract blockers from the previous audit. I also confirmed the diagnostics color mapping fix is real: `DiagPanel` now normalizes `entry.level` via `toLowerCase()` and styles both `.warn` and `.warning`, so uppercase structured log levels now map correctly. I left one non-blocking UI nit instead of a blocker: xi's claimed “time-ago 1s reactive interval” only fully landed in `ChatView`; `TaskPanel` still renders `formatTimeAgo(task.timestamp)` without any reactive tick source, so task-age labels can still freeze until the component re-renders for some other reason. Verification: `npm run build` in `web-console-v2/`, `python3 tests/test_ws_and_review.py` (7 passed), and direct source/protocol comparison across `App.vue`, `TaskPanel.vue`, `ChatView.vue`, `DiagPanel.vue`, and `ws_server/server.py`.

## [2026-03-31 04:35] DONE — Implemented Phase 7 `7.1 main.py` rewrite + `7.3` unified E2E T1-T11
Replaced the legacy `main.py` entrypoint with a real Phase 7 runtime assembly: `ApplicationRuntime` now constructs the stack in design order (`GameAPI -> WorldModel -> Kernel -> Dashboard/WS -> GameLoop`), wires all five Experts into Kernel, supports `.env` + CLI config, publishes benchmark/log exports on shutdown, and handles graceful termination. Root cause addressed during integration: the runtime modules were individually implemented but not self-assembling. In live use, new Tasks/Jobs were not automatically visible to `GameLoop`, dashboard fanout had no single bridge for task/task-message/log/benchmark/state publishing, and base `TaskAgent` still lacked a native `push_player_response()` intake, which would have broken real WS -> Kernel -> TaskAgent reply flow. I fixed that by adding a thin `RuntimeBridge` in `main.py` for GameLoop registration and WS fanout, and by adding `TaskAgent.push_player_response()` so `Kernel.submit_player_response()` now reaches the agent without a test-only adapter. I also added `tests/test_e2e_full.py`, which runs T1-T11 under one shared runtime shape and exports Phase 7 benchmark outputs to `docs/wang/phase7_e2e_benchmark_records.json`, `docs/wang/phase7_e2e_benchmark_summary.json`, and `docs/wang/phase7_e2e_benchmark_summary.md`. Verification: `python3 -m py_compile main.py task_agent/agent.py tests/test_e2e_full.py`, `python3 tests/test_e2e_full.py`, `python3 tests/test_task_agent.py`, `python3 tests/test_e2e_adjutant.py`, and `python3 tests/test_ws_and_review.py`.

## [2026-03-31 04:42] DONE — Final audit of xi Phase 7.2 LLM benchmark report `14c0a5c`
Audited xi's `14c0a5c` LLM benchmark deliverable and wrote the result to `docs/wang/xi_llm_benchmark_audit.md`. I did not clear it as `zero-gap`. Root cause is not that the script fails outright — in fact `QWEN_API_KEY='' python3 tests/test_llm_benchmark.py` runs successfully and produces the Mock baseline — but that the evidence chain is too weak for the report's recommendation strength. First, the script overwrites a single fixed `docs/llm_benchmark_report.md` file on every run and exports no raw JSON results, so the published Qwen latency/token numbers are not stably reproducible or auditable after the fact. Second, T6 is only a single-turn measurement, yet the report upgrades a first-turn `query_world` response into a claim that complex multi-Job coordination behavior is validated. Third, the report's cost, prompt-caching, context-compression, and streaming recommendations are not derived from measured outputs in the script at all. I also verified that `python3 tests/test_llm_benchmark.py --help` times out instead of returning CLI help, so the script is runnable but not a real standalone benchmark tool yet. Verification: `python3 -m py_compile tests/test_llm_benchmark.py`, `QWEN_API_KEY='' python3 tests/test_llm_benchmark.py`, a 5-second timeout-wrapped `python3 tests/test_llm_benchmark.py --help`, and direct report/script source comparison.

## [2026-03-31 04:46] DONE — Final regression audit of xi Phase 7.2 fixes `415849f`
Re-audited xi's follow-up benchmark fix and wrote the result to `docs/wang/xi_llm_benchmark_audit_r2.md`. Most of the previous audit is now closed: the script exports timestamped raw JSON, no-key runs no longer overwrite the main report, `--help` now works, `python-dotenv` is a graceful fallback, and the report body now downgrades T6 to first-turn-only evidence while labeling cost and optimization guidance as estimate/untested. I did not clear it as `zero-gap`, because the CLI surface is still not actually wired through runtime execution. Root cause: `argparse` parses `--skip-live` and `--model`, but `asyncio.run(main())` still calls a zero-argument `main()` that only keys off `QWEN_API_KEY` and still hardcodes `model=\"qwen-plus\"`. I verified this with a live repro: `python3 tests/test_llm_benchmark.py --skip-live` still executed the real Qwen path and updated `docs/llm_benchmark_report.md`. Verification: `python3 -m py_compile tests/test_llm_benchmark.py`, `python3 tests/test_llm_benchmark.py --help`, `QWEN_API_KEY='' python3 tests/test_llm_benchmark.py`, and `python3 tests/test_llm_benchmark.py --skip-live`.

## [2026-03-31 04:50] DONE — Absolute final regression audit of xi Phase 7.2 fix `6311e2a`
Re-audited xi's final benchmark CLI fix and wrote the result to `docs/wang/xi_llm_benchmark_audit_r3.md`. This closes the last remaining Phase 7.2 blockers: `--skip-live` now really bypasses the live Qwen path, and `--model` now really flows into `QwenProvider(model=...)`. I re-used Wang's required repro (`python3 tests/test_llm_benchmark.py --skip-live`) and verified that it returns `0`, prints the explicit skip banner, does not print a live Qwen header, does not overwrite `docs/llm_benchmark_report.md`, and only emits timestamped raw JSON. I also added a no-network provider-capture check showing `main(skip_live=False, model="custom-model")` instantiates the provider with `model="custom-model"`. I cleaned the temporary benchmark artifacts after verification. By my audit standard, `7.2` is now `zero blockers`, which also clears the final project-close gate Wang had set.

## [2026-04-01 03:28] DONE — OpenRA game-control research for restart/reset/shortcuts
Investigated the current game lifecycle/control surface and wrote the report to `docs/wang/openra_control_research.md`. Root cause of the reset gap is architectural, not incidental: the Copilot TCP/GameAPI layer only exposes in-match commands and queries, while game lifecycle control lives either in OpenRA UI (`Game.RestartGame()` via ingame menu) or in outer process scripts (`web-console/api/service.py` start/stop/restart). I confirmed three distinct reset paths: process-level restart via the old web-console service; in-engine restart via `Game.RestartGame()`; and startup-time deterministic restore via `Game.LoadSave=<save>`. The key finding is that `Game.RestartGame()` reseeds RNG, so it is not a precise test reset, whereas `Game.LoadSave=<baseline>` is already wired through `OpenCodeAlert/start.sh` and is the most suitable current path for automated state reset. I also cataloged the exposed `GameAPI` control/query surface (`match_info_query`, `control_point_query`, production queue control, stop/repair/deploy/attack, etc.) and the built-in developer/debug cheats (`give-cash`, `instant-build`, `build-anywhere`, `unlimited-power`, shroud clear/reset, etc.), noting that the latter are available through in-game developer commands and debug UI but are not currently part of the remote Copilot API.

## [2026-04-01 03:33] DONE — Fixed false-positive `BASE_UNDER_ATTACK` events
Patched `world_model/core.py` to stop treating any self-building HP drop as a base attack. Root cause was exactly the live issue Wang relayed: the diff layer promoted every building HP decrease into `BASE_UNDER_ATTACK`, so power-related state changes or snapshot jitter could fan out into spurious `defend_base` task creation. I changed the inference to require a meaningful damage step (`> 5%` of max HP) and at least one nearby enemy combat-capable actor before emitting `BASE_UNDER_ATTACK`, while keeping `UNIT_DAMAGED` intact for ordinary HP deltas. I also added a regression test that covers three cases in order: small damage with no enemies, large damage with no enemies, and large damage with a nearby enemy combat unit. Verification: `python3 tests/test_world_model.py` (6 passed), `python3 tests/test_kernel.py` (12 passed), and `python3 -m py_compile world_model/core.py tests/test_world_model.py`.

## [2026-04-01 04:14] DONE — Implemented OpenRA restart tooling with baseline-save support
Built a new `game_control.py` process-control layer with `is_game_running()`, `stop_game()`, `start_game(save_path=None)`, `restart_game(save_path=None)`, `wait_for_api()`, and CLI entrypoints for `start/stop/restart/status`. Root cause was architectural: the runtime only knew how to talk to the in-match Copilot API, but restart/reset lives outside that surface at the OpenRA process layer, so there was no safe way to stop/start the game and then reattach the live runtime. I integrated `game_restart` WS inbound handling, added `ApplicationRuntime.restart_game()` with loop stop/start, task cancellation, API health wait, and `WorldModel.reset_snapshot()` so a fresh match does not diff against the previous one, then verified the path with focused tests. Verification: `python3 -m py_compile game_control.py main.py ws_server/server.py world_model/core.py tests/test_game_control.py tests/test_ws_and_review.py`, `python3 tests/test_game_control.py` (4 passed), `python3 tests/test_ws_and_review.py` (7 passed), and `python3 tests/test_world_model.py` / `python3 tests/test_kernel.py` (6/12 passed).

## [2026-04-01 04:41] DONE — Root-cause investigation of Adjutant LLM hang in `main.py`
Traced the live path `RuntimeBridge.on_command_submit -> Adjutant.handle_player_input -> _classify_input/_handle_query -> llm.chat` and wrote the report to `docs/wang/adjutant_hang_investigation.md`. The root cause is architectural, not provider-specific: `ApplicationRuntime.start()` launches `GameLoop` on the same asyncio event loop that serves WS and awaits Adjutant/Qwen coroutines, but `GameLoop._tick()` directly calls synchronous `WorldModel.refresh()` and synchronous job ticks. `WorldModel.refresh()` in turn fans out to blocking `GameAPI` socket calls (actor queries, economy, map, and five production queues), and `GameAPI._send_request()` itself uses blocking sockets with 10s timeouts and retries. That means a slow game poll can starve the event loop long enough that even Adjutant's existing `asyncio.wait_for()` timeouts do not fire, making the LLM call look hung. I verified the diagnosis three ways: line-by-line code tracing; a minimal repro where a fake async LLM returns in ~0.10s without `GameLoop` but stalls when `GameLoop` uses `time.sleep(0.3)` in `world_model.refresh()`; and an `ApplicationRuntime`-shaped repro where `FastWorldSource` returns in ~0.10s but `BlockingWorldSource` gets stuck in repeated over-budget ticks until an external process alarm interrupts it. The recommended fix is to move GameLoop/game I/O onto a dedicated worker thread (or otherwise isolate blocking game polling from the main asyncio loop), with explicit provider HTTP timeouts as secondary hardening rather than the primary fix.

## [2026-04-01 04:44] DONE — Git maintenance sweep for my own leftover changes
Re-read `docs/wang/workflow.md` after Wang's reminder and checked the worktree for my own uncommitted changes. I found one leftover local edit in `world_model/core.py` from my earlier patch flow: the `world_refresh_completed` log level had been changed from `info` to `debug` but never committed. I treated that as my responsibility, not as a shared-worktree cleanup task, and prepared a small local commit to avoid leaving my own tail in the index/worktree. The rest of the dirty files currently shown by `git status` appear unrelated to my recent tasks, so I did not sweep or alter them.

## [2026-04-01 04:48] DONE — Split git maintenance into ignore commit plus `space` docs commit
Per the user's instruction, I handled two repo-maintenance items without touching unrelated local state. First, I added `.playwright-mcp/` to `.gitignore` and committed that as its own small repo-hygiene change. Then I staged the existing untracked `AGENTS.md` and `docs/space/` tree as a separate commit, keeping the cleanup split by concern rather than bundling browser-local noise with shared docs. I did not sweep other untracked paths like `.claude/`, `.playwright-mcp/` contents, screenshots, or shell scripts because the user only asked for the Playwright ignore and the `space`/`AGENTS` material.

## [2026-04-01 04:53] DONE — Moved GameLoop blocking tick work onto worker threads
Implemented Wang's fast mitigation for the Adjutant hang root cause by offloading the blocking GameLoop paths to worker threads. In `game_loop/loop.py`, `WorldModel.refresh()` now runs via `await asyncio.to_thread(...)` and per-job `job.do_tick()` now runs the same way, so the main asyncio loop is no longer monopolized by synchronous game polling and expert execution. I explicitly did not offload `kernel.tick()` because the current implementation is an in-memory pending-question timeout scan, not a socket or sleep path. I added a regression test that reproduces the original starvation shape: a blocking `world_model.refresh()` (`time.sleep(0.3)`) runs alongside an Adjutant with a fake async LLM, and `handle_player_input("战况如何？")` now still returns in about `0.10s` instead of stalling behind the tick loop. Verification: `python3 -m py_compile game_loop/loop.py tests/test_game_loop.py`, `python3 tests/test_game_loop.py` (10 passed), `python3 tests/test_ws_and_review.py` (7 passed), and a live `ApplicationRuntime`-shaped repro with a blocking world source showing `runtime.adjutant.handle_player_input(...)` returning `{'elapsed': 0.103, 'result': 'query'}` while GameLoop logs a 300ms over-budget tick.

## [2026-04-01 05:16] DONE — Fixed OpenCodeAlert GameAPI request log spam at the server
Investigated the OpenCodeAlert side of the console spam and found that the noise did not come from the `CopilotDebug`-gated JSON request/response dumps. Root cause: `openra_api.GameAPI` opens a short-lived TCP connection per request, while `OpenRA.Game/CopilotCommandServer.cs` logged every `AcceptAsync()` success as `INFO: 接受新的客户端连接`, plus the stop-loop exits, so normal `WorldModel` polling amplified into console spam even with `CopilotDebug = false`. I fixed this by adding `LogDebug(...)` and moving the connection-accept / stop-loop logs behind `DebugMode`, while keeping startup/shutdown/error logs at info/error. I verified `dotnet build OpenCodeAlert/OpenRA.Game/OpenRA.Game.csproj` (0 errors; only existing analyzer/style warnings), wrote the report to `docs/wang/opencodealert_gameapi_log_spam.md`, and documented the backend recommendation separately: lowering live actor refresh from `0.1s` to `0.2-0.25s` is worth evaluating as a follow-up, but true refresh-on-change is not possible without a game-side push/event channel.

## [2026-04-01 05:47] DONE — Reduced OpenCodeAlert warning noise in `OpenRA.Game`
Cleaned the highest-noise warning cluster in `OpenCodeAlert/OpenRA.Game` after confirming `cd OpenCodeAlert && dotnet build` and `dotnet build OpenRA.Game/OpenRA.Game.csproj` report different analyzer surfaces. I fixed the real concentration rather than chasing the entire solution blindly: `CopilotCommandServer.cs` and `CopilotModels.cs` were carrying most of the local style/analyzer noise, so I normalized using order, tuple naming, invariant-culture formatting, extracted repeated exception-detail building, moved the `JObjectExtensions` namespace out of `Game.cs`, trimmed comment/whitespace issues, and locally suppressed the one legacy `worldRenderer` naming warning instead of attempting a repository-wide rename. Verification: `cd OpenCodeAlert && dotnet build OpenRA.Game/OpenRA.Game.csproj` now reports only the project-configuration `SA0001` warning, and `cd OpenCodeAlert && dotnet build` dropped from 273 warnings to 126 warnings. Remaining solution warnings are now concentrated in `OpenRA.Mods.Common`, not `OpenRA.Game`.

## [2026-04-01 05:54] DONE — Small follow-up cleanup in `OpenRA.Mods.Common`
Accepted one extra formatting cleanup that `dotnet format` surfaced in `OpenRA.Mods.Common/Copilot/CopilotControlPoint.cs` instead of leaving the submodule dirty. The change is mechanical only: simplify a nullable return, normalize using order/spacing, and remove a stray blank line. Verification: `cd OpenCodeAlert && dotnet build` now reports 120 warnings instead of 126, with the remaining warnings still concentrated in other `OpenRA.Mods.Common` files.

## [2026-04-01 06:11] DONE — Implemented persistent `GameAPI` connection reuse with protocol framing
Changed `openra_api/game_api.py` from per-request socket creation to a persistent, lock-protected connection with lazy connect, automatic reconnect on disconnect, and a public `close()` hook. Root cause was not only the repeated TCP handshake: the old client/server protocol relied on EOF or socket timeout to delimit a message, which fundamentally conflicts with long-lived connections. I fixed that at both ends by switching to newline-delimited request/response framing while keeping compatibility with legacy one-shot JSON packets via opportunistic full-JSON parsing. On the Python side, `GameAPI.is_server_running()` now also uses the framed reader so API health checks do not stall behind a kept-open socket, and `ApplicationRuntime.stop()` / `restart_game()` now proactively call `api.close()` so restart paths do not carry a dead connection forward. On the C# side, `OpenRA.Game/CopilotCommandServer.cs` now loops over multiple requests per accepted client socket instead of closing after one response. Verification: `python3 tests/test_game_api.py` (3 passed: single-connection reuse, reconnect-after-server-close, and concurrent-call serialization), `python3 tests/test_game_control.py` (4 passed, including runtime restart/stop closing the API), `python3 -m py_compile openra_api/game_api.py main.py tests/test_game_api.py tests/test_game_control.py`, and `cd OpenCodeAlert && dotnet build OpenRA.Game/OpenRA.Game.csproj` (0 errors; only the existing project-config `SA0001` warning remains).

## [2026-04-01 06:18] DONE — Fixed `WorldModel` category inference for Chinese building names
Repaired `WorldModel._actor_category()` so normalized actors no longer fall back to `vehicle` just because a Chinese building name is missing from the small explicit rule table. Root cause: `建造厂` and `电厂` were absent from `DEFAULT_UNIT_CATEGORY_RULES`, and the fallback path only knew a few English/code-form building tokens before defaulting to `ActorCategory.VEHICLE`. I fixed this by adding a better middle layer in `world_model/core.py`: after the explicit intel rules, category inference now also consults normalized building/unit vocab derived from `GameAPI.BUILDING_DEPENDENCIES` / `UNIT_DEPENDENCIES` plus a small extra alias set for `建造厂/指挥中心`. That keeps known Chinese structures on the building path without changing the public query interface. Verification: `python3 tests/test_world_model.py` (7 passed, including a new regression covering `建造厂`, `发电厂`, `兵营`, `重坦`, `基地车`, and `矿车`), `python3 tests/test_kernel.py` (12 passed), and `python3 -m py_compile world_model/core.py tests/test_world_model.py`.

## [2026-04-01 13:48] DONE — Fixed Kernel soft actor matching so Recon no longer claims buildings
Repaired `kernel/core.py::_actor_matches_need()` so soft actor needs such as Recon's `{owner: "self"}` no longer match `category=building` or `mobility=static` actors by default. Root cause: the matcher previously applied only the explicit predicates and otherwise returned `True`, so if all mobile self units were unavailable the allocator would happily hand a structure to any actor-based job that asked only for ownership. I kept the fix narrow: building/static actors are now allocatable only when the `ResourceNeed` explicitly asks for `category="building"` or `mobility="static"`, and I added kernel regressions proving both sides of that contract. Verification: `python3 tests/test_kernel.py` (14 passed), `python3 tests/test_world_model.py` (7 passed), and `python3 -m py_compile kernel/core.py tests/test_kernel.py`. I also documented the remaining `BASE_UNDER_ATTACK -> defend_base` latency issue separately for Wang: today the Kernel only auto-creates a managed task and then waits on TaskAgent/LLM, so the fast fix should be a direct Kernel-side `CombatJob` auto-response rather than another task-level roundtrip.

## [2026-04-01 13:52] DONE — Made `BASE_UNDER_ATTACK` trigger an immediate Kernel-side defend job
Reworked the `BASE_UNDER_ATTACK` auto-response so the Kernel no longer stops at `create_task("defend_base")`. Root cause: the old path created a managed task and then waited for TaskAgent/LLM to start an actual defense job, which turned an emergency reaction into a multi-second roundtrip. I kept the task for visibility, but changed the Kernel to resolve a base-defense target position (event position first, then attacked actor position, then self-building centroid) and directly `start_job("CombatExpert", CombatJobConfig(... HOLD ...))` on the defend task. Repeated attack events now retarget the existing defend combat job instead of spawning duplicates. I also re-verified the already-implemented game restart surface (`stop/start/restart + baseline save`) to cover Wang's third item in the same pass. Verification: `python3 tests/test_kernel.py` (14 passed, including immediate defend-job assertions and retarget dedup), `python3 tests/test_world_model.py` (7 passed), `python3 tests/test_game_control.py` (4 passed), and `python3 -m py_compile kernel/core.py tests/test_kernel.py main.py tests/test_game_control.py`.

## [2026-04-01 14:10] DONE — Restored user-visible command/reply feedback to the chat channel
Traced the “命令执行后用户看不到任何提示” complaint to a response-channel mismatch in `main.py`. Root cause: queries were sent via `ws_server.send_query_response(...)` and therefore appeared as normal Adjutant chat replies, but command acks and question-reply confirmations were routed through `player_notification`, which is the async system-event channel. The command still executed, but the chat UX looked silent because the direct reply was not emitted on the primary dialogue path. I fixed this by introducing `_emit_adjutant_response()` in the runtime bridge and routing all synchronous user-initiated results (`command`, `reply`, `query`, and direct failures) through `query_response`, while leaving `player_notification` for async world/task events. I also made `Adjutant._handle_command()` include the created `task_id` in the visible response text. Verification: `python3 tests/test_game_control.py` (6 passed, including new bridge regressions for command ack and reply success visibility), `python3 tests/test_adjutant.py` (10 passed), `python3 tests/test_ws_and_review.py` (7 passed before/after bridge change, no protocol regression), and `python3 -m py_compile adjutant/adjutant.py main.py tests/test_game_control.py`.

## [2026-04-01 14:18] DONE — Fixed frontend chat cache persistence and player-message echo edge cases
Repaired the remaining frontend-side chat issues in `web-console-v2`. Root cause had two parts: `ChatView.vue` persisted the full chat log in `localStorage`, so stale dialogue survived reloads and even code updates, and its WebSocket listeners were registered at module-evaluation time with no unsubscribe path, so a remount could duplicate inbound replay. On top of that, the player bubble was echoed before confirming the WS send actually went out. I changed chat history to `sessionStorage` so it is scoped to the current browser session instead of becoming a long-lived cache, updated `useWebSocket.on()` to return an unsubscribe function, moved `query_response` / `player_notification` subscription wiring into `onMounted`/`onUnmounted`, and made `ChatView.sendMessage()` only append the local player bubble after `send()` succeeds. Verification: `cd web-console-v2 && npm run build` (passed) and `python3 tests/test_ws_and_review.py` (7 passed).

## [2026-04-01 14:55] DONE — Fixed live production-name aliasing and building-queue auto-placement
Continued Wang's Live E2E Round 5 investigation and confirmed two separate production-path root causes. First, the live runtime could still receive LLM-produced CamelCase names such as `PowerPlant`, while the game-side alias table only reliably accepted lowercase English aliases / Chinese names / internal codes. I fixed that by adding a shared `openra_api/production_names.py` helper that normalizes surface forms and expands aliases from `OpenCodeAlert/mods/common/Copilot.yaml`, then reused it in both `GameAPI.can_produce()/produce()` and `EconomyJob` queue/event matching. Second, the real user-visible building jam was not just naming: `EconomyJob` was issuing building production with `auto_place_building=False`, and it ignored `has_ready_item` / ready queue items as meaningful build-queue state. I fixed that by enabling `auto_place_building=True` for `queue_type="Building"`, auto-calling `place_building("Building")` when the matching building is ready, and surfacing a new blocked reason when some other ready building is clogging the queue. Verification: `python3 tests/test_game_api.py` (5 passed), `python3 tests/test_economy_expert.py` (7 passed), `python3 -m py_compile openra_api/production_names.py openra_api/game_api.py experts/economy.py task_agent/agent.py tests/test_game_api.py tests/test_economy_expert.py`, live `GameAPI.can_produce("PowerPlant") == True`, live `GameAPI.produce("PowerPlant", 1, True)` returning a wait id, live `GameAPI.query_production_queue("Building")` returning valid data, and a direct live `EconomyJob(unit_type="PowerPlant", queue_type="Building")` repro that changed the build queue from `has_ready_item=True` with a stuck ready `powr` to `has_ready_item=False` after one `tick()`.

## [2026-04-01 15:42] PARTIAL — Live Round 6 opened multiple stacked barracks-task root causes
Started Wang's requested Live Round 6 from a restarted backend, but the live game itself was not a clean MCV-only baseline: it already contained multiple barracks, infantry, and a jammed `Building` queue. I still used it to trace the `建造兵营` complaint. Root causes found in order: terminal task completion leaked queue resources (`dff8cb4`); auto-place could report success when nothing changed on the game side (`12b63c814a` in `OpenCodeAlert`, top-level pointer `21b15e5`, plus Python queue-effect verification in `65dde1c`); prompt-only structure-build guidance was insufficient so simple build commands needed deterministic bootstrap (`7af8a42`); bootstrapped build tasks needed a deterministic close path or the LLM would hijack them into unrelated work like recon (`a4f08fc`); building tasks could complete before the final ready building was actually placed (`777e8f0`); and pre-existing ready buildings needed to count toward the current task or the task would clear the queue and still hang (`e7daa12`). After those fixes, the remaining live blocker for `建造兵营` on the current save is no longer a hidden queue jam but legitimate low power (`Power=10`, `Drained=190`, `Provided=200`). I then fixed the user-feedback gap by routing `SignalKind.BLOCKED` through Kernel `TaskMessage(task_warning)` registration (`8b84985`), and verified live that the command now produces a visible warning (`电力不足，生产暂停等待恢复`) instead of silently appearing stuck. Detailed step log is in `docs/yu/archive/20260401/live_test_round6.md`. The full clean opening / recon / combat chain is still pending because this save is not a clean opening baseline.

## [2026-04-01 16:46] DONE — Demoted `queue_unassigned` from player warning to internal progress
User review exposed a second feedback-quality bug: `生产队列资源未分配，等待中` was being shown as if it were a real blocker. Root cause was that `EconomyJob._enter_waiting()` emitted `SignalKind.BLOCKED` even for `reason="queue_unassigned"`, and Kernel converted every BLOCKED signal into a player-visible `TaskMessage(task_warning)`. That reason is only an internal scheduling state while the production queue resource has not yet been assigned, so I changed it to emit `SignalKind.PROGRESS` instead. I kept true blockers (`low_power`, `no_funds`, `cannot_produce`, ready-item queue jams, failed building placement) on the warning path. Verification: `python3 tests/test_economy_expert.py` (11 passed), `python3 tests/test_kernel.py` (14 passed), and `python3 -m py_compile experts/economy.py tests/test_economy_expert.py`. If the frontend still shows the old string in the current browser tab, it is probably a session-cached notification from before this fix rather than a newly emitted warning.

## [2026-04-01 16:52] DONE — Wrote final Round 6 system report for Wang/user sync
Paused further live poking and consolidated the current state into a system-level final report instead of another bug-by-bug update. The report (`docs/wang/live_round6_final_report.md`) organizes the live findings into three buckets: issues already fixed in the production/build/user-feedback/runtime layers, still-open workflow problems (especially `攻击敌人` with no visible target), and the main drifts between the frozen design and the runtime that actually stabilized in live use. The key conclusion is that the system is no longer failing at the foundation layer; the remaining risk is high-level workflow looseness, so the next recommended step is a short live-hardening cycle rather than more feature expansion.

## [2026-04-01 17:00] DONE — Researched Planner Expert direction and recommended `ProductionAdvisor` first
Wang assigned a Planner Expert investigation before any code changes. I traced the current gap from the frozen design into the runtime: `design.md` defines Planner Experts as non-LLM proposal generators, `experts/base.py` already has a sufficient `PlannerExpert` abstraction, and both `task_agent/handlers.py` and `kernel/core.py` still expose `query_planner` as a stub. I then compared the three candidate planners against the current repo's actual data surface. Conclusion: `ProductionAdvisor` is the only good first implementation because it directly addresses the live `攻击敌人` drift, works with today's `WorldModel` queries (`economy`, `production_queues`, `my_actors`, `enemy_actors`, `world_summary`), and can be implemented cleanly with deterministic rules. I explicitly recommended not starting with `AttackRoutePlanner` (needs richer map/threat semantics) or `ReconRoutePlanner` (too much overlap with the current `ReconExpert` heuristic). I wrote the intermediate report to `docs/wang/planner_expert_research.md` and prepared to wait for Wang's direction before touching code.

## [2026-04-01 17:23] DONE — Implemented first Planner Expert: `ProductionAdvisor`
Wang confirmed the direction, so I implemented the first real Planner Expert instead of leaving `query_planner` as a live stub. I added `experts/planners.py` with a narrow, rule-based `ProductionAdvisor(PlannerExpert)` plus a small planner registry/router. The planner returns a structured proposal only, never text and never side effects. First-pass rules cover exactly the approved scope: `no_visible_enemy -> scout_first`, `low_power -> tech_up(powr)`, `queue_blocked -> hold`, and `need_mobile_scout -> produce(jeep)`. I then replaced the stub in `task_agent/handlers.py` and aligned `kernel/core.py::_tool_query_planner()` to the same runtime surface so TaskAgent no longer sees a fake planner capability. I also exported the planner in `experts/__init__.py`, added `tests/test_planners.py`, extended `tests/test_tool_handlers.py` with a real `ProductionAdvisor` routing test, and re-ran the relevant regressions. Verification: `python3 tests/test_planners.py` (5 passed), `python3 tests/test_tool_handlers.py` (10 passed), `python3 tests/test_kernel.py` (14 passed), and `python3 -m py_compile experts/planners.py experts/__init__.py task_agent/handlers.py kernel/core.py tests/test_planners.py tests/test_tool_handlers.py`.

## [2026-04-01 17:42] DONE — Added Adjutant rule routing for simple commands
Implemented Wang's requested Adjutant-side rule router so high-frequency simple commands no longer pay the full `Adjutant LLM classify + TaskAgent LLM choose Expert` latency path. I added `_try_rule_match()` and `_handle_rule_command()` in `adjutant/adjutant.py`, and inserted them at the top of `handle_player_input()`: if a simple command matches, Adjutant now creates the Task through Kernel and immediately starts the corresponding Job through Kernel before returning the chat ack. Covered rule families are exactly the approved narrow set: deploy MCV, build simple structures, simple production commands, and simple recon commands. Query-like or multi-stage texts still fall through to the old LLM path unchanged. I also extended the Adjutant tests so rule-routed commands explicitly prove `llm.call_log == 0`, while unmatched commands still hit the LLM classifier. Verification: `python3 tests/test_adjutant.py` (15 passed), `python3 tests/test_adjutant_routing.py` (10 passed), `python3 tests/test_ws_and_review.py` (7 passed), and `python3 -m py_compile adjutant/adjutant.py tests/test_adjutant.py tests/test_adjutant_routing.py`.

## [2026-04-01 18:05] DONE — Hardened frontend/backend WebSocket reconnect behavior
Wang assigned a targeted WS robustness pass across the frontend and backend. I kept the changes narrow and closed the concrete issues he listed. On the backend, `ws_server/server.py` now creates `aiohttp.web.WebSocketResponse(max_msg_size=10 * 1024 * 1024)` so larger snapshot/log payloads do not fail at the default size cap. On the frontend, `web-console-v2/src/composables/useWebSocket.js` now exposes a real `reconnecting` state, clears its raw inbound `messages` buffer on reconnect success to avoid stale replay/duplication after a fresh `sync_request`, and keeps that reconnect state visible to the app shell. `web-console-v2/src/App.vue` now surfaces `◌ 重连中` in the header so the user can distinguish temporary reconnect from a hard disconnect. `web-console-v2/src/components/TaskPanel.vue` now replaces `pendingQuestions` outright on every `task_list` instead of keeping stale state via fallback. I confirmed that `ChatView.vue` already satisfied Wang's third requirement before this patch: player text is only locally echoed when `send()` returns `true`, so no code change was needed there. Verification: `cd web-console-v2 && npm run build`, `python3 tests/test_ws_and_review.py` (7 passed), and `python3 -m py_compile ws_server/server.py`.

## [2026-04-01 18:51] DONE — Improved observability for TaskAgent reasoning and diagnostics filtering
Wang's next request was observability, specifically to make it clear what the system is thinking and to reduce diagnostic noise. I implemented this in two layers. First, `task_agent/agent.py` now persists the non-tool text returned by the LLM as structured `task_agent` logs with `event="llm_reasoning"`, tagged with `task_id`, `wake`, and `turn`, so diagnostics can show the reasoning text instead of only the downstream tool calls. Second, `web-console-v2/src/components/DiagPanel.vue` now supports both level filtering and explicit component filtering (`ALL / adjutant / task_agent / kernel / expert / world_model / game_loop`), shows the component separately from the event/tag in the log stream, and limits the benchmark panel to the top 20 tags sorted by frequency (with an overflow note when items are hidden). I deliberately did not add recent reasoning text directly onto `TaskPanel` in this pass: Wang marked it optional, and keeping verbose reasoning in Diagnostics avoids turning the task view into a mixed debug/user surface. I also confirmed that the prior `ChatView` local-echo guard still holds and did not need changes in this task. Verification: `cd web-console-v2 && npm run build`, `python3 tests/test_task_agent.py` (20 passed, including a new `llm_reasoning` structured-log assertion), `python3 tests/test_logging_system.py` (4 passed), and `python3 -m py_compile task_agent/agent.py`.

## [2026-04-01 19:02] DONE — Implemented `UnitRegistry` and centralized YAML-backed unit metadata
Wang asked for the missing `UnitRegistry` layer from the design startup chain, mainly to stop name/category/build-queue knowledge from being hardcoded in four different places. I added `unit_registry.py` as the new source of truth. Instead of trying to fully deserialize OpenRA's custom rules syntax, the loader does a narrow parse over `OpenCodeAlert/mods/ra/rules/{structures,vehicles,infantry,aircraft,ships}.yaml`, extracting only the metadata we actually need: build queue, cost, prerequisites, and the buildable actor id. I then merge that with alias/display-name data from `OpenCodeAlert/mods/common/Copilot.yaml`, producing `UnitEntry(unit_id, display_name, category, queue_type, cost, faction, prerequisites, aliases)` plus the requested lookup API: `get`, `resolve_name`, `list_buildable`, and an internal `match_in_text` used by routing. I integrated the registry into `main.py` (`GameAPI -> UnitRegistry -> WorldModel -> Kernel ...`), switched `openra_api/production_names.py` to registry-backed alias expansion, changed `Adjutant` rule routing to resolve build/production targets through the registry instead of local alias tables, and changed `WorldModel` category inference to consult the registry before its old fallback heuristics. I kept `MCV` and `HARV` as explicit gameplay-special categories ahead of the generic registry mapping, and preserved the existing simple-command behavior by normalizing registry ids back to the legacy lowercase production token at the Adjutant boundary. Verification: `python3 tests/test_unit_registry.py` (4 passed), `python3 tests/test_adjutant.py` (15 passed), `python3 tests/test_adjutant_routing.py` (10 passed), `python3 tests/test_world_model.py` (8 passed), `python3 tests/test_game_api.py` (7 passed), and `python3 -m py_compile unit_registry.py openra_api/production_names.py adjutant/adjutant.py world_model/core.py main.py`.

## [2026-04-01 19:15] DONE — Added executable live E2E runner for real game/backend checks
Wang's next task was to turn the ad hoc live scripts into a formal engineering asset. I added `tests/test_live_e2e.py` as a single-file executable runner rather than another mock-style test. The runner holds a persistent WebSocket connection to the real backend (`max_size = 10 * 1024 * 1024`), keeps the latest `task_list` / `world_snapshot` in memory, exposes the required `send_command`, `wait_for_game_state`, `query_actors`, and `check_backend_running` helpers, and groups the live scenarios into phase-based entrypoints (`phase_a` through `phase_e`). Each case prints command/reply/result/timing and the script ends with a `N passed / M failed` summary plus non-zero exit on failure. The implemented scenarios are the exact Wang set: connectivity, MCV deploy, power/barracks/refinery building, infantry production, recon start, and a query-quality check. I deliberately kept it as a plain `python3` runner, not a pytest fixture tree, because the target usage is operator-style live verification against an already running game/backend. Verification: `python3 -m py_compile tests/test_live_e2e.py`, and I also actually ran `python3 tests/test_live_e2e.py phase_a`; the runner behaved correctly and reported `GameAPI server is not reachable`, which means the script path itself is fine but the live game endpoint was not up in the current environment.

## [2026-04-06 22:11] DONE — Added stale-world guards for Adjutant stateful commands and queries
Unified stale-world protection at the Adjutant boundary so demo-facing commands and queries fail closed instead of answering from stale snapshots. Added a shared stale guard response path, short-circuited obvious query-like inputs immediately, and kept stale protection inside direct rule/NLU/capability handlers and generic command/query handlers. Also fed world refresh health into classification/query context so the LLM sees degraded runtime state explicitly. Added regressions for stale rule-routed builds, stale queries, and classified stale commands; verified the existing world model / game loop stale-health tests still pass. Root cause: stale world snapshots were still allowed to reach the query/command path and could produce confident but wrong answers unless the Adjutant entrypoint rejected them explicitly.
## [2026-04-06 22:27] DONE — Refined stale-world guardrails with pending-reply protection and richer health facts
Tightened the stale-world hotfix after the first pass: query-like inputs now only short-circuit before classification when there is no pending question, so stale-world protection does not swallow likely replies. Also enriched `WorldModel.world_summary()` and `compute_runtime_facts()` with stale/last-error/failure-count fields so prompt consumers and debug views can inspect degraded runtime health directly. Verification stayed green after the refinement. Root cause remains the same: stale snapshots were still capable of flowing into high-level command/query handling unless Adjutant failed closed at the entrypoint.
## [2026-04-06 22:48] DONE — Exposed task waiting hints in the task panel
Added a small frontend-only hint in `TaskPanel` so running tasks that are waiting on capability / resource conditions no longer look dead. The panel now inspects existing `task.jobs` metadata (`status`, `summary`, `expert_type`) and shows a task-level "waiting on capability/resource" message when the job trace indicates capability/request_units-style waiting. No backend payload changes were needed because the current `task_list` already contains enough job metadata. Verified with `npm run build`. Root cause: the UI had job badges but no task-level explanation for waiting states, so managed tasks looked inert even when they were still alive.
## [2026-04-09 00:00] DONE — Audited logging/diagnostics/session replay UX for E2E iteration pain
Wrote a code-grounded audit of the logging + diagnostics + session replay pipeline into `docs/yu/archive/20260409/logging_diagnostics_iteration_audit_20260409.md`. Main findings: the system now has durable structured logs and task traces, but iteration still feels painful because replay is current-session-only, traces are raw rather than summarized, and there is no disk-backed session browser or one-click triage bundle. The best next step order is: low-risk task summary UX, session catalog/replay, task-centric summarization, then root-cause tooling and cross-session comparison. Root cause: observability exists, but it is not yet organized into a developer workflow.
## [2026-04-06 04:38] DONE — Split capability-only runtime facts from ordinary task context
Kernel now wires a capability-aware runtime-facts provider per task, so ordinary managed tasks no longer see the `buildable` / `feasibility` fields that can make them believe they are allowed to build prerequisites themselves. `WorldModel.compute_runtime_facts()` now accepts an `include_buildable` toggle; capability tasks keep the richer buildability view, while ordinary tasks get only neutral world facts. Also hardened `request_units` semantics so `building` requests do not idle-bind an existing construction-yard actor; they now go through production/bootstrap instead. Root cause: the runtime-facts surface was shared too broadly, and building requests could be misinterpreted as claims on an existing yard instead of a production action.
## [2026-04-06 04:39] DONE — Re-verified request-unit semantics regressions
Re-ran the affected kernel/world-model regressions after the final provider tweak: ordinary tasks now receive runtime facts without capability buildability/feasibility hints, capability tasks keep the richer view, and `building` unit requests no longer idle-bind a construction-yard actor. Verified with full `test_kernel.py`, full `test_world_model.py`, and targeted direct invocation of the new unit-request regressions. Root cause stays the same: ordinary-task facts were too capability-shaped, and building requests were being treated like actor claims instead of production/bootstrap requests.
## [2026-04-09 00:00] DONE — Consolidated roadmap / deferred-work audit
Reviewed the main roadmap/history/deferred-work docs and consolidated them into a single phased roadmap report. Main conclusion: the docs are directionally consistent, but they mix runtime hotfixes, near-term capability work, and future commander/RTS substrate design in the same backlog language. The clean ordering is demo-safety → capability consolidation → execution semantics → information plane → composite-task policy → tactical substrate. Root causes identified: stale docs still blur current vs target state, and several already-fixed runtime issues remain listed as deferred work.
## [2026-04-09 00:10] DONE — Roadmap consolidation report finalized
## [2026-04-12 07:34] DONE — Surfaced stale-world detail into unit-pipeline payloads and capability context
Closed the remaining live unit-pipeline blind spot for stale-world handling. `WorldModel.production_readiness_for()` now carries refresh-health detail (`world_sync_last_error`, `world_sync_consecutive_failures`, `world_sync_failure_threshold`) alongside the existing `reason="world_sync_stale"` decision, and `kernel/unit_request_runtime.py` threads those fields into both `unfulfilled_requests` and `unit_reservations` payloads. On the consumer side, `task_agent/context.py` now renders those details directly inside `[待处理请求]` and `[预留]` lines when a request/reservation is blocked by stale world sync, so capability no longer sees only a blocker code while the concrete disconnect reason is hidden elsewhere. Verification: `pytest tests/test_unit_request.py tests/test_capability_task.py -q` (94 passed), `pytest tests/test_world_model.py -q -k 'refresh_failure_marks_stale_and_recovers or compute_runtime_facts_exposes_unit_reservations'` (2 passed), `pytest tests/test_kernel_runtime_projection.py -q` (7 passed), and `python3 -m py_compile world_model/core.py kernel/unit_request_runtime.py task_agent/context.py tests/test_unit_request.py tests/test_capability_task.py`.

## [2026-04-12 07:18] DONE — Surfaced stale-world failure detail in Adjutant/coordinator responses
Closed the next stale-world visibility gap above task triage: Adjutant and the coordinator snapshot no longer flatten world-sync failure to a generic warning string. `adjutant/adjutant.py` now derives a concise detail block from `refresh_health()` (`连续失败 x/y` plus the concrete `last_error` when present), appends it to stale guard replies, and includes the same structured fields (`world_sync_error`, `world_sync_failures`, `world_sync_failure_threshold`) in the returned stale-guard payload. The coordinator alert path now reuses the same formatter, so `coordinator_snapshot["alerts"]` and the derived status line expose the actual refresh failure instead of only “世界状态同步异常”. While validating this slice, I also tightened three stale/full-file adjutant tests that had drifted independently of this change: one overfit `status_line` assertion now checks the alert instead of assuming alert ordering, and the `MockWorldModel.runtime_state` fixture was brought back in sync with `RuntimeStateSnapshot` by restoring the `unfulfilled_requests` key. Verification: `pytest tests/test_adjutant.py tests/test_adjutant_coordinator.py -q` (102 passed) and `python3 -m py_compile adjutant/adjutant.py tests/test_adjutant.py tests/test_adjutant_coordinator.py`.

## [2026-04-12 06:39] DONE — Closed the immediate runtime-reset residual test gaps
Followed Xi’s audit on `56ce53a` with two narrow signal-only tests instead of reopening the production path. First, `tests/test_game_loop.py` now directly asserts `GameLoop.reset_runtime_state()` clears the exact stale/disconnect bookkeeping fields it owns, so future additions to that state cannot silently skip reset. Second, `tests/test_ws_and_review.py` now verifies `session_clear` does trigger the `sync_runtime()` unregister path for previously registered jobs/agents, which closes the concern that game-loop registry entries might linger across session reset even if kernel state is wiped first. Verification: `pytest tests/test_game_loop.py -q -k 'reset_runtime_state_clears_stale_bookkeeping or worldmodel_stale_pauses_jobs_notifies_and_recovers'` and `pytest tests/test_ws_and_review.py -q -k 'session_clear_unregisters_runtime_bindings or session_clear_rotates_persisted_log_session or runtime_bridge_sync_runtime_uses_public_kernel_accessors'`.

## [2026-04-12 06:28] DONE — Surfaced stale-world failure detail into task triage and diagnostics
Closed the next runtime-disconnect visibility slice by carrying refresh-health detail into task-facing triage instead of collapsing everything to a generic `world=stale`. `RuntimeBridge` now passes the full `refresh_health()` snapshot into live task payload construction, `task_triage.py` enriches the stale branch with `world_sync_error`, `world_sync_failures`, and `world_sync_failure_threshold`, and `TaskTriageSnapshot` normalizes those fields for both live task payloads and replay `current_runtime` summaries. On the frontend, `TaskPanel` and `DiagPanel` now show `sync_fail=x/y` and the concrete refresh error next to stale tasks, and `DiagPanel` also mirrors the current `world_snapshot` stale banner so diagnostics mode no longer hides the only meaningful disconnect reason behind `OpsPanel`. Verification: `pytest tests/test_runtime_views.py tests/test_ws_and_review.py tests/test_game_control.py -q` (69 passed), `python3 -m py_compile main.py task_triage.py runtime_views.py tests/test_runtime_views.py tests/test_ws_and_review.py`, `npm test` (8 passed), and `npm run build` in `web-console-v2/`. Scope intentionally stayed out of coordinator wording and unit-pipeline blocker semantics; those remain follow-up slices, not part of this commit.

## [2026-04-12 05:14] DONE — Reset runtime stale/disconnect state on `session_clear` and runtime restart
Closed the highest-value slice from the runtime-disconnect audit by making session reset actually reset runtime health, not just logs and kernel state. `RuntimeBridge.on_session_clear()` now clears the `WorldModel` snapshot and `GameLoop` stale/disconnect bookkeeping before republishing runtime state, and `ApplicationRuntime.restart_game()` applies the same `GameLoop.reset_runtime_state()` hook before the forced world refresh. I added a small explicit `GameLoop.reset_runtime_state()` method instead of reusing broader lifecycle start/stop paths so the contract stays narrow: clear stale-notification / paused-for-recovery state that should never leak across a clear/restart, while leaving normal loop ownership untouched. Regression coverage now pins both backend reset hooks and the user-visible outcome: `tests/test_game_control.py` verifies `session_clear` resets publisher/kernel/world/loop state, and `tests/test_ws_and_review.py` now asserts the republished world snapshot comes back with `stale == False` after clear. Verification: `pytest tests/test_game_control.py -q` (17 passed), `pytest tests/test_ws_and_review.py -q` (44 passed), `pytest tests/test_game_loop.py -q` (13 passed), and `python3 -m py_compile main.py game_loop/loop.py tests/test_game_control.py tests/test_ws_and_review.py`.

Wrote the consolidated roadmap/history/deferred-work report and codified the recommended execution order: demo safety, capability consolidation, execution semantics, information plane, composite-task policy, then tactical substrate / commander targets. Also logged the documentation hygiene requirement that current execution docs and future-state design docs must be labeled separately to prevent backlog drift.
## [2026-04-09 02:27] DONE — Strengthened Adjutant battlefield snapshot and disposition routing
Added deterministic battlefield snapshot helpers inside `adjutant/adjutant.py`, threaded the snapshot into classification/query payloads, and made info/command disposition handling merge into the best live task when appropriate instead of always spawning a fresh task. Updated `tests/test_adjutant.py` to pin the snapshot disposition/focus, query prompt shaping, info merge routing, and command merge routing. Root cause: Adjutant only had raw world-summary context and treated player intel like a fresh command, so it lacked a concise top-level battlefield view and could not reliably attach updates to the right live task.

## [2026-04-09 03:16] DONE — Audited Slice 4 hard unit ownership and action-surface gap
Inspected `models/configs.py`, `experts/recon.py`, `experts/combat.py`, `experts/movement.py`, `task_agent/handlers.py`, `task_agent/tools.py`, `kernel/core.py`, and `openra_api/action/*`, then wrote the result to `docs/yu/archive/20260409/slice4_hard_unit_ownership_action_surface_audit_20260409.md`. Root finding: Slice 4 is not blocked on adding `actor_ids` — explicit ownership is already wired through config, tool schema, handler pass-through, expert `get_resource_needs()`, and kernel fallback inference. The remaining gap is continuity: tasks lack a stable kernel-owned mapping from task to its current actor set, so recon/combat can control explicit units but cannot yet preserve that force cleanly across a task lifecycle. The smallest viable path is therefore task-bound group continuity first, optional `group_handle` later, and tactical action-surface expansion only after ownership continuity lands.

## [2026-04-09 17:35] DONE — Audited future-unit request flow and minimal reservation landing slice
Traced the current request_units path through Kernel, EconomyExpert, QueueManager, task-agent handlers, models, runtime facts, and unit-request tests. Root finding: the repo already has reservation bookkeeping (`UnitReservation`, request→reservation mapping, runtime_state sync), but reservation state is still not the main control surface. `register_unit_request()` still routes through idle match → fast-path bootstrap → suspend/wait, bootstrap production still starts a direct `EconomyJob` on the requester task, and Capability mostly sees mirrored `unfulfilled_requests` rather than reservation contracts. Exact friction points identified: reservation creation depends on unit inference; cancellation does not currently unwind bootstrap job ownership; produced actors are attributed opportunistically on idle scan rather than on explicit reservation/job completion; QueueManager is queue-health only and not reservation-aware; and capability context omits the reservation list even though Kernel already syncs it. Safest next slice: keep the current E2E-preserving fast-path behavior, but make reservation metadata the visible contract first (Kernel return payloads + capability context + cancellation hygiene), before any allocator or queue-ownership rewrite.

## [2026-04-09 17:48] DONE — Audited smallest task-bound actor continuity slice after reservation visibility
Traced the current actor ownership path and found the key gap is not only missing group memory but also request-held bindings: fulfilled `request_units` actors are bound under `req:<request_id>` and resumed back to the task via `UNIT_ASSIGNED`, but there is no task-level registry and no transfer step from request binding into reusable task ownership. That means later movement/recon/combat jobs only keep continuity if the LLM manually echoes `actor_ids`; otherwise they reacquire generic units, and explicit `actor_ids` can also fail if the old `req:` binding is still occupying the actor. Safest minimal path: add a tiny Kernel-owned task actor registry, transfer fulfilled request-held actors into that registry when waking the task (and release the synthetic `req:` bindings), sync the registry into runtime state/context, then let handlers auto-fill `actor_ids` from the active group only when no actor-based job is already running for the same task. This preserves current E2E behavior, avoids allocator work, and reuses the existing explicit-actor support already present in Movement/Recon/Combat configs and Kernel resource inference.

## [2026-04-09 19:58] DONE — Implemented reservation-contract slice and task actor continuity handoff
Made unit reservations a visible contract for capability/runtime: `register_unit_request()` now returns reservation metadata (`reservation_id`, `unit_type`, `queue_type`, `bootstrap_job_id`, `reservation_status`, `remaining_count`), `cancel_unit_request()` aborts an active bootstrap job when present, kernel runtime sync now exposes `unit_reservations`, and capability context now renders a concise reservations block. While validating the slice, the wake path also exposed the next continuity step: fulfilled request-held actors are now transferred into a kernel-owned task actor group and surfaced as `active_actor_ids` / `active_group_size` in runtime state so later actor-based jobs can reuse the same task-owned units without allocator redesign. Focused tests passed after the fix.

## [2026-04-09 02:49] DONE — Reservation-contract slice tightened
Completed the smallest safe reservation-contract slice: `register_unit_request()` now returns richer reservation metadata for inferable requests (`reservation_id`, `unit_type`, `queue_type`, `bootstrap_job_id`, `reservation_status`, `remaining_count`, plus fulfillment counts), `cancel_unit_request()` aborts any active bootstrap job before marking the request cancelled, `cancel_task()` now routes pending/partial requests back through the request cancel path, and capability-facing context now shows concise `[预留]` reservations while ordinary task context redacts `unit_reservations`/`capability_status`. Focused regressions were added for fulfilled and bootstrap-backed request payloads, bootstrap-job cancellation, capability reservation rendering, and ordinary redaction. Root cause captured: the reservation contract had already existed structurally, but the return payload and context surface were still too thin for capability to reason about future-unit ownership safely.
## [2026-04-09 02:59] DONE — Audited logging/diagnostics/replay UX and defined the smallest next debugging slice
Re-audited the current logging + diagnostics + replay chain from the perspective of iterative E2E debugging and wrote the result to `docs/yu/archive/20260409/logging_diagnostics_next_slice_20260409.md`. Root finding: the main pain is no longer missing logs or missing transport; `logging_system` already persists per-session/task JSONL, `sync_request` already replays current live history, and Diagnostics already shows raw task traces. The remaining debugging cost comes from weak payload semantics: TaskPanel still infers waiting state by regexing job summaries, DiagPanel still shows a raw event timeline without a structured current-state summary, and replay still answers “what rows happened?” better than “what is this task doing right now and why?”. Recommended smallest next slice: add a deterministic `triage` snapshot to `task_list` / `task_update` in `main.py::_task_to_dict()`, then consume it in `TaskPanel.vue` and `DiagPanel.vue` before attempting bigger work such as disk-backed session browsing or offline replay. This keeps the next step narrow and directly improves day-to-day iteration without redesigning the logging substrate.
## [2026-04-09 23:10] DONE — Audited the next minimal tactical action-surface slice after actor continuity
Inspected `openra_api/action/*`, `openra_api/game_api.py`, `experts/*`, `task_agent/tools.py`, `task_agent/handlers.py`, and `kernel/core.py`, then wrote `docs/yu/archive/20260409/minimal_tactical_action_surface_audit_20260409.md`. Root finding: actor continuity is now good enough that the biggest remaining tactical control gap is not allocation or grouping but interruption. `GameAPI.stop(...)` already exists and Adjutant direct `stop_attack` already uses it, but the task/expert surface has no ownership-based `stop_units` primitive. Recommended next slice is therefore `stop_units + StopExpert + active-group reuse`, with regroup/retreat/hold wrappers explicitly deferred until after this interrupt primitive lands.

## [2026-04-09 23:59] DONE — Audited the next minimal capability truth slice
Inspected `experts/knowledge.py`, `openra_state/data/dataset.py`, `world_model/core.py`, `unit_registry.py`, and `task_agent/context.py`, then wrote `docs/yu/archive/20260409/capability_truth_next_slice_20260409.md`. Root finding: capability truth is currently split across three layers — raw YAML metadata in `UnitRegistry`, a hand-normalized prerequisite/buildability table in `dataset.py`, and additional hardcoded roster/prerequisite slices in `knowledge.py` and `context.py`. The most actionable drift points are `PROC/HARV/FTRK/MIG/E1/E3` between registry and dataset, `dome/v2rl/3tnk/4tnk` between knowledge and dataset, and the extra `has_construction_yard => implicit powr` assumption inside `WorldModel`. Recommended minimum path is not to force everything onto raw `UnitRegistry` yet; instead, keep `dataset.py` as the temporary normalized demo truth table, make `world_model` and `context` consume only that layer, and strip prerequisite truth back out of `knowledge.py` until a proper registry prerequisite normalizer exists.

## [2026-04-09 03:17] DONE — Audited the next minimal Adjutant coordinator slice without adding Commander
Inspected `adjutant/*`, `main.py`, `world_model/core.py`, `kernel/core.py`, and `task_agent/context.py`, then wrote `docs/yu/archive/20260409/adjutant_coordinator_next_slice_20260409.md`. Root finding: Adjutant is already the real top-level coordinator, but it still reasons from a much thinner view than the runtime already knows. `WorldModel.compute_runtime_facts()`, `Kernel._sync_world_runtime()`, and `main._build_task_triage()` already produce coordinator-useful state (`capability_status`, `unit_reservations`, `active_group_size`, `info_experts`, task triage), yet `Adjutant._battlefield_snapshot()` and `_build_context()` still only consume coarse `world_summary` plus label/raw_text/status task rows. The smallest next slice is therefore context plumbing plus a few deterministic disposition guards, not a new Commander layer or another planner brain. I also recorded this as a reusable knowledge-base rule in `docs/yu/agents.md`.

## [2026-04-09 16:06] DONE — Re-audited replay/triage UX after triage landed and defined the next smallest slice
Reviewed the live runtime payloads and current frontend again using `main.py`, `logging_system/core.py`, `ws_server/server.py`, `TaskPanel.vue`, `DiagPanel.vue`, and persisted sessions under `Logs/runtime/`. Root finding changed from the earlier audit: the structured triage snapshot slice has already landed (`main._build_task_triage()` plus TaskPanel/DiagPanel rendering), so the next bottleneck is no longer live-state ambiguity. The real remaining iteration pain is the split between live UI and historical evidence: once a session ends, hangs, or restarts, the useful evidence is already on disk in `Logs/runtime/session-*/tasks/<task_id>.jsonl`, but Diagnostics cannot replay it, so debugging falls back to scrolling raw JSONL in the shell. I wrote the updated recommendation to `docs/yu/archive/20260409/replay_triage_next_slice_20260409.md`: the smallest next material slice is disk-backed persisted task replay for the current/latest session, wired into `DiagPanel`, rather than more triage fields or a full session browser.

## [2026-04-09 16:34] DONE — Audited the next post-stop OpenRA action-surface slice
Compared the current Experts/Kernel/task-tool surface with the older direct-python/OpenRA_Copilot control surface and wrote `docs/yu/archive/20260409/openra_action_surface_next_slice_20260409.md`. Root finding: after `stop_units` and hard unit ownership, the next missing layer is not another broad tactical planner but a few narrow player-visible operations that already exist at `GameAPI` level and were used in direct control, yet still do not cross into Experts/Kernel/task tools. The concrete gaps are `repair_units`, `set_rally_point`, `move_units_by_path`, and later `occupy_units`; camera/select/group APIs remain lower priority because they do not materially strengthen the runtime’s control contract. Recommended next sequence is `repair_units` first, then capability-owned `set_rally_point`, then path movement, with engineer capture deferred until that gameplay lane becomes active again.

## [2026-04-09 16:48] DONE — Audited how E2E debugging can become task-centric instead of raw-log-centric
Re-audited the current iteration UX across `logging_system/core.py`, `main.py`, `ws_server/server.py`, `DiagPanel.vue`, and `TaskPanel.vue`, then wrote `docs/yu/archive/20260409/e2e_task_centric_iteration_ux_audit_20260409.md`. Root finding: the bottleneck is no longer missing logs, missing persisted sessions, or missing live triage — those pieces already exist. The remaining pain is that replay still returns rows rather than an answer, so developers still reconstruct blockers, decisive transitions, and player-visible consequences manually from long traces. The smallest next slice is therefore not “more logs” or a full session browser, but upgrading the existing `task_replay_request` path into a deterministic task debug bundle (`summary`, `last_transition`, `blockers`, `highlights`, `player_visible`, plus raw `entries`) and rendering that bundle above the raw trace in `DiagPanel`.
## [2026-04-09 23:25] DONE — Audited broader OpenRA action-surface gaps against direct Python control
Compared the current copilot surface against `GameAPI`, `openra_api/action/*`, `macro_actions`, and the older direct-control jobs, then wrote `docs/yu/archive/20260409/openra_action_surface_gap_audit_20260409.md`. Root finding: the runtime is no longer mainly missing generic actions; `move_units_by_path`, `stop_units`, `repair_units`, and `set_rally_point` already landed. The biggest remaining controllability gap is now precision: direct Python can use `GameAPI.attack_target(...)`, while the copilot still mostly attacks by area. Recommended next slice is therefore an ownership-based `attack_actor` / focus-fire primitive, with `occupy_target` and posture wrappers deferred behind it.

## [2026-04-10 00:24] DONE — Landed `min_start_package` wake semantics for unit requests
Finished the next reservation/request semantics slice in `models/core.py`, `kernel/core.py`, and `tests/test_unit_request.py`. Root cause: the kernel treated any non-fulfilled request as still blocking, so non-blocking reinforcement requests suspended agents unnecessarily and blocking requests ignored `min_start_package`, resuming only after full fulfillment. The fix split those semantics explicitly: non-blocking requests no longer suspend the agent, blocking requests wake as soon as their start package is satisfied, request/reservation payloads now surface `start_released`, and fulfilled request-held actor bindings are handed off into the task-owned actor group before the wake event is sent. Verification: `python3 tests/test_unit_request.py`, `python3 tests/test_kernel.py`, and `python3 -m py_compile models/core.py kernel/core.py tests/test_unit_request.py`. Commit: `0d09653`.

## [2026-04-10 00:31] DONE — Clarified capability-visible reservation lifecycle states
Extended the reservation/request runtime contract so Capability no longer sees all pending requests as one undifferentiated queue. Root cause: after the `min_start_package` slice, the kernel internally knew whether a request was still waiting for dispatch, bootstrapping, already start-released, or just reinforcement, but that state collapsed back into a flat `pending_request_count` plus blank `reason` strings in runtime facts. I updated `kernel/core.py` to derive explicit request reasons (`waiting_dispatch`, `bootstrap_in_progress`, `start_package_released`, and reinforcement variants), expose richer capability counts (`dispatch_request_count`, `bootstrapping_request_count`, `start_released_request_count`, `reinforcement_request_count`), and set a more truthful capability phase (`dispatch` / `bootstrapping` / `fulfilling` / `executing` / `idle`). I also updated `task_agent/context.py` so Capability sees human-readable lifecycle text instead of raw codes, and added focused regressions in `tests/test_kernel.py` and `tests/test_capability_task.py`. Verification: `python3 tests/test_kernel.py`, `python3 tests/test_capability_task.py`, and `python3 -m py_compile kernel/core.py task_agent/context.py tests/test_kernel.py tests/test_capability_task.py`. Commit: `6911ad1`.

## [2026-04-10 00:46] DONE — Finished the Adjutant capability-fulfilling coordinator slice
Completed the low-conflict audit follow-up in `adjutant/adjutant.py`. Root cause: `capability_status` had already grown new lifecycle truth (`task_label`, `start_released_request_count`, `reinforcement_request_count`, `phase=fulfilling`), but Adjutant consumed that state through three separate render paths. `_coordinator_snapshot()` and `_try_merge_to_capability()` had been partially updated, while `_derive_task_triage()` still hand-built stale capability status lines, so the top-level coordinator summary and the active task list disagreed. I fixed the remaining Adjutant path to surface `task_label` fallback, `ready=` / `reinforce=` counts, `phase=fulfilling`, and more truthful waiting reasons in task triage/status lines, and added a focused merge acknowledgement regression so fulfilling-capability feedback is locked at the chat layer too. Verification: `python3 tests/test_adjutant_coordinator.py`, `python3 tests/test_adjutant.py`, and `python3 -m py_compile adjutant/adjutant.py tests/test_adjutant.py tests/test_adjutant_coordinator.py`.

## [2026-04-11 03:55] DONE — Unified registry-backed unit lookup helpers
Root cause: unit/category/name lookup truth was split across `openra_api.production_names`, `world_model/core.py`, `openra_api/intel/service.py`, `openra_state/data/structure_data.py`, and `openra_state/data/combat_data.py`, so alias handling and category inference had to be maintained in several places. I introduced shared registry-backed helpers in `openra_api/production_names.py` (`production_name_entry`, `production_name_entries`, `production_name_unit_id`, `production_name_category`) and delegated the consumers to that path. `world_model` now resolves actor categories and structure counts through the helper instead of local alias sets, `IntelService` classifies buildings/vehicles/harvesters from the same lookup path, and `StructureData` / `CombatData` now fall back to the shared resolver for aliases. I added `tests/test_unit_lookup_unification.py` to cover the shared helper, data-layer delegation, world-model classification, and intel-service categorization. Verification: `python3 -m py_compile openra_api/production_names.py openra_api/intel/service.py world_model/core.py openra_state/data/structure_data.py openra_state/data/combat_data.py tests/test_unit_lookup_unification.py`, `python3 tests/test_unit_lookup_unification.py`, `python3 tests/test_world_model.py`, and `python3 tests/test_unit_registry.py`. Commit: `f488b66`.

## [2026-04-11 04:47] DONE — Audited current hard unit ownership support
Confirmed that hard ownership is already implemented across configs, handlers, experts, and runtime projection much further than the older roadmap implied: Recon/Combat/Movement/Stop/Repair/Occupy/Rally all accept explicit actor_ids, handlers default to task-owned active_actor_ids, and Kernel persists request-handoff groups into runtime_state. Root finding: the remaining gap is not missing actor_ids, but incomplete group continuity. Kernel only seeds _task_actor_groups from request fulfillment handoff, not from ordinary actor-job resource grants or explicit actor_ids start paths, so a battle task started directly via actor jobs can still lack persistent owned-group semantics. Recommended smallest next slice: update Kernel to seed/refresh task actor groups whenever actor-based jobs gain owned actor resources, without introducing a new group_handle layer yet.

## [2026-04-11 05:24] DONE — Completed read-only structural audit of adjutant/world_model/task_agent/kernel/openra_state data
Audited the current mainline after the recent slices with a code-quality/architecture lens only, no code changes. The top active structural issues are: (1) `WorldModel` still mixes observed game truth with kernel-fed runtime projection and presentation shaping, (2) `Adjutant` assembles coordinator context from multiple non-atomic queries and remains a god object around triage/snapshot/rendering, (3) the capability/request lifecycle contract is still a stringly-typed dict shared across `kernel`, `task_triage`, `adjutant`, and `task_agent/context`, (4) demo/faction truth is still repeated and partially inconsistent — notably `dataset.py` overwrites faction-specific rows like `E1/E3/E6`, while `WorldModel.compute_runtime_facts()` hardcodes `faction="soviet"` and `kernel/core.py` still carries separate request hint maps, (5) TaskAgent business rules are split between long prompt prose and code gates, making policy drift hard to detect, and (6) assembled runtime smoke coverage exists for expert actions but still does not pin the newer coordinator/capability snapshot contract end-to-end. Added the faction-truth overwrite pattern to `docs/yu/agents.md` because it will otherwise be rediscovered repeatedly.

## [2026-04-11 05:55] DONE — Audited post-slice structural drift around adjutant/world_model/kernel/task_agent
Ran a fresh read-only sidecar audit focused on current low-latency multi-agent blockers. Highest-signal findings: `WorldModel` still performs both observed-state derivation and kernel-fed runtime projection (`battlefield_snapshot()` calling `compute_runtime_facts()` plus `set_runtime_state()`), `Adjutant` still fan-outs multiple synchronous world/runtime queries per turn and duplicates capability/triage rendering logic, the capability lifecycle remains stringly-typed across `kernel/core.py`, `task_triage.py`, `adjutant/adjutant.py`, and `task_agent/context.py`, and TaskAgent still appends a full `[CONTEXT UPDATE]` JSON header into conversation every wake which guarantees prompt growth on long-lived tasks. Also reconfirmed that truth is still duplicated across `dataset.py`, `world_model/core.py`, and intel/category fallbacks, including the hardcoded `faction="soviet"` path and `DEFAULT_UNIT_CATEGORY_RULES` fallback. Recommended next slices: extract a pure runtime projection builder out of `WorldModel`, introduce a typed capability snapshot model shared by Kernel/Adjutant/TaskAgent, collapse Adjutant coordinator fan-out into one snapshot fetch, and replace repeated context-header accumulation with summarized state transitions for long-running tasks.

## [2026-04-11 06:03] DONE — Audited bridge/runtime projection access patterns across main/kernel/runtime_views/task_agent/ws
Read-only audit of `main.py`, `kernel/core.py`, `runtime_views.py`, `task_agent/context.py`, and WS flow confirmed that the worst direct kernel-private coupling is largely gone from the bridge layer, but runtime projection is still rebuilt in multiple places. `main.py` now uses public list/query APIs, yet it still assembles task payloads, triage, replay bundles, and dashboard state by combining `kernel.list_*`, `world_model.runtime_state()`, and local status helpers, so bridge truth remains non-atomic and partially duplicated. `runtime_views.py` only types `CapabilityStatusSnapshot`, leaving the rest of the coordinator/runtime projection as raw dicts. `WorldModel` still mixes observed state with kernel-fed runtime projection via `set_runtime_state()` and `compute_runtime_facts()`, and WS sync/replay publishes whatever those mixed layers return. Smallest next refactor slice: extract a typed `RuntimeSnapshot`/`CoordinatorSnapshot` builder used by both `main.py` and `Adjutant`, while keeping `WorldModel` as a consumer of runtime projection rather than the place that composes it.

## [2026-04-11 13:29] DONE — Completed read-only Adjutant coordinator slice audit
Audited `adjutant/adjutant.py` plus `task_triage.py`, `runtime_views.py`, `task_agent/context.py`, and current coordinator-focused tests to identify the highest-value low-risk strengthening slices without adding a Commander layer. The five best next cuts are: (1) make `_build_context()` / `_coordinator_snapshot()` consume one atomic snapshot instead of re-querying runtime/battlefield state multiple times, (2) derive task domain from live runtime (`active_expert` / capability / group state) instead of raw-text keywords alone, (3) centralize capability phase/blocker wording so Adjutant status/alerts/merge replies stop drifting, (4) make `_battlefield_snapshot()` fallback reuse existing runtime facts / info expert truth instead of dropping to `unknown/false/0`, and (5) strengthen deterministic short-follow-up hints using existing `battle_groups` / `recent_directives` / reservation state. Root cause across all five: Adjutant already has most of the right runtime truth available, but it still under-consumes it and re-renders overlapping coordinator state through several slightly different local paths.

## [2026-04-11 14:18] DONE — Read-only audit of `_build_context` atomic input collection
Audited `adjutant/adjutant.py` and the directly related coordinator/adjutant tests for the smallest safe “atomic once per `_build_context` cycle” refactor. Current code already avoids duplicate world queries inside the happy path by passing `collected_inputs` into `_coordinator_snapshot()`, and `tests/test_adjutant_coordinator.py` already pins one-call behavior for `runtime_state` / `battlefield_snapshot` / `compute_runtime_facts` / `refresh_health`. The remaining root issue is structural: `_build_context()` still assembles task/pending/message/job artifacts separately from the coordinator snapshot, and `_coordinator_snapshot()` still has an internal `self._collect_coordinator_inputs()` fallback, so future call sites can silently reintroduce non-atomic reads. Recommended next slice: introduce one build-snapshot helper that freezes coordinator inputs plus per-task kernel artifacts up front, then make `_coordinator_snapshot()` consume provided inputs only.

## [2026-04-11 14:44] DONE — Read-only audit of capability truth exposure chain
Audited the exact chain `WorldModel.compute_runtime_facts()` → `task_agent/context.py` capability rendering/filtering → `task_agent/policy.py` capability prompt, no code changes. Highest-risk truth drifts found: (1) `[可造]` currently means “prerequisites satisfied in the demo table”, but the prompt treats it as “can build now”, even though funds / low power / queue jams / ready-item blockers are not encoded there; (2) `world_sync_stale` / `world_sync_last_error` exist in runtime facts but are only buried in the JSON header and never promoted to a first-class capability blocker, so the capability agent can continue reasoning from stale snapshots; (3) the MCV-only state is represented as base progression `phase=deploy_mcv` but also `next_unit_type=fact`, which looks like a production target even though Capability only has `produce_units`; (4) `[基地状态]` only exposes structure counts, not online/disabled truth under low power, so the prompt overestimates available gateways; and (5) `context.py` intentionally filters non-demo queue/ready-item noise, which can make blocked production queues look idle to Capability. Added a follow-up queue item in `docs/yu/plan.md` so these truth-alignment fixes can be cut as a focused slice later.
## [2026-04-11 13:53] DONE — Respected disabled gateways in capability truth
Landed `cbaa394 fix: respect disabled gateways in capability truth` across `world_model/core.py`, `kernel/unit_request_runtime.py`, `kernel/runtime_projection.py`, `runtime_views.py`, `task_agent/context.py`, and focused tests. Root cause: capability/runtime truth still treated disabled prerequisite providers as if they continued to unlock downstream buildables, so the system collapsed “前置存在但离线/停用” into either `missing_prerequisite` or over-optimistic `[前置已满足]` output. The fix split the state explicitly: `production_readiness_for()` now distinguishes owned vs active prerequisites, reports `disabled_prerequisite` with concrete labels like `雷达站(powerdown)`, `runtime_facts_buildable()` / capability `buildable` now filter against active prerequisite providers, and capability blocker/request/context rendering now surface the specific disabled prerequisite chain instead of saying “缺少前置”. Verification: `pytest tests/test_world_model.py tests/test_kernel_runtime_projection.py tests/test_capability_task.py tests/test_runtime_views.py -q` (90 passed) and `pytest tests/test_adjutant.py tests/test_adjutant_coordinator.py tests/test_task_agent.py tests/test_unit_request.py tests/test_ws_and_review.py -q` (246 passed). Xi and Wang both audited and confirmed the slice.
## [2026-04-11 14:07] DONE — Froze Adjutant context artifacts once per build cycle
Landed `3c94a65 refactor: freeze adjutant context artifacts once` in `adjutant/adjutant.py`. Root cause: `_build_context()` had already stopped duplicating world queries in the happy path, but its coordinator inputs and per-task kernel artifacts were still assembled separately, and `_coordinator_snapshot()` still retained a silent self-collect fallback. That made future non-atomic context regressions easy to reintroduce. The fix added `_build_context_snapshot()` to freeze `tasks / pending_questions / task_messages / jobs_by_task / coordinator_inputs` once, changed `_build_context()` to consume only that frozen snapshot, and removed `_coordinator_snapshot()`’s internal fallback so it now always consumes provided inputs. Verification: `pytest tests/test_adjutant_coordinator.py tests/test_adjutant.py tests/test_ws_and_review.py -q` (120 passed). Xi and Wang both confirmed the slice as clean and behavior-preserving.

## [2026-04-11 15:22] DONE — Preserved hidden stash docs by restoring them into `docs/yu/archive`
After restoring the latest stash into the worktree, I found that it carried a batch of historical `docs/yu/*.md` deletions that would otherwise remain hidden inside stash state or disappear from the visible tree. Instead of re-promoting them to the top level, I restored those files from `HEAD` into `docs/yu/archive/` and added an archive README so the top-level `docs/yu/` can stay focused on active execution docs. This preserves old audits/live traces without letting stale reports masquerade as current runtime truth.

## [2026-04-11 15:29] DONE — Tightened stale planner-truth tests to match safe counter semantics
Validated the current `experts/knowledge.py` / `experts/planners.py` boundary and found that the stale assumption was in test expectations, not in the live code: faction-locked counters should not be recommended when side truth is unknown. I updated `tests/test_demo_capability_truth.py` so aircraft/vehicle-heavy cases now require an explicit `faction="soviet"` for `ftrk` / `v2rl`, while still keeping the neutral `e3` infantry counter expectation. Verification: `pytest tests/test_planners.py tests/test_demo_capability_truth.py -q` (28 passed) and `pytest tests/test_tool_handlers.py -q` (16 passed).

## [2026-04-11 15:46] DONE — Tightened `BattlefieldSnapshot` capability typing
Landed a small but important read-model cleanup in `runtime_views.py`: `BattlefieldSnapshot.capability_status` now uses the same `CapabilityStatusSnapshot` typed object as `RuntimeStateSnapshot`, instead of carrying a raw nested dict. Root cause: capability status had already been normalized for runtime-state surfaces, but battlefield/query surfaces still downgraded it back to an untyped mapping, which made the coordinator read side looser than necessary and invited future wording/shape drift. I updated `BattlefieldSnapshot.from_mapping()` / `to_dict()` / `build_battlefield_snapshot()` accordingly and extended `tests/test_runtime_views.py` to cover object input plus typed round-trip behavior. Verification: `pytest tests/test_runtime_views.py tests/test_world_model.py tests/test_adjutant_coordinator.py -q` (62 passed).

## [2026-04-11 15:58] DONE — Tightened stale policy/capability/planner test assertions
Used Laplace’s focused test-signal audit to tighten four stale assertion surfaces that could otherwise give misleading green results. In `tests/test_task_agent_policy.py` I changed the tool-surface check from loose spot checks to exact set equality for ordinary vs capability tool surfaces. In `tests/test_capability_task.py` I moved phase/blocker expectations onto the authoritative `runtime_facts["capability_status"]` snapshot instead of the legacy top-level fallback fields. In `tests/test_kernel.py` I made the dispatch-phase test assert consistency between `runtime_state` and `runtime_facts` capability snapshots rather than relying on one specific blocker value for a scenario that can legitimately require prerequisites. In `tests/test_planners.py` I strengthened the queue-blocked planner case so it now pins `recommended_expert=None` and `queue_scope="player_shared"`. Verification: `pytest tests/test_task_agent_policy.py tests/test_capability_task.py tests/test_kernel.py tests/test_planners.py -q` (91 passed).

## [2026-04-11 16:14] DONE — Made Adjutant battlefield fallback reuse richer runtime truth
Improved the `Adjutant._battlefield_snapshot()` fallback path used when `world_model.query("battlefield_snapshot")` is unavailable. Root cause: the fallback already reused runtime state for counts, but it still threw away coordinator-relevant truth that was present in `runtime_facts` and `capability_status` — notably request-driven recommended posture, threat direction/level, base health summary, and the nested capability snapshot itself. I aligned the fallback closer to the `WorldModel.battlefield_snapshot()` posture logic: it now treats pending requests/reservations as an economy focus and `satisfy_requests` posture, carries `queue_blocked_items` previews, propagates `info_experts` threat/base-state fields, marks `stale` from runtime facts, and embeds the typed `capability_status` payload into the returned snapshot. Verification: `pytest tests/test_adjutant_coordinator.py tests/test_adjutant.py -q` (87 passed).
## [2026-04-11 09:14] DONE — Completed read-only post-refactor cleanup audit for runtime diagnostics surfaces
Audited `main.py`, `task_replay.py`, `logging_system/`, `ws_server/`, and `web-console-v2/src/components/DiagPanel.vue` with a code-quality / immediate-cleanup lens only, no product-code edits. Highest-signal commit-sized findings: benchmark streaming currently rebroadcasts full history on each increment; `RuntimeBridge` task-update fingerprints omit rendered fields like `active_group_size`, so UI state can go stale silently; direct WS client sends lack the timeout protection used by broadcast sends; inbound WS payloads are routed with essentially no field validation; replay summarization dedupes by only `(label, message)`, which can hide repeated retries/blockers; tool-use counters in `task_replay.py` likely double count one tool round; the Vue diagnostics panel requests replay once and never refreshes it for active tasks; and benchmark aggregation semantics are split between “replace” and “append” expectations. Main structural hotspots remain `RuntimeBridge`, `build_task_replay_bundle()`, and the monolithic `DiagPanel` script block.
## [2026-04-11 12:03] DONE — Audited next safe extraction slice for current `kernel/core.py`
Read-only audit against latest main at `aa3fc80` found that the safest/highest-yield next extraction is no longer unit-request or allocator logic. After `task_coordination.py` and `task_questions.py` landed, the best P1 slice is the pure runtime projection assembly still embedded in `Kernel._sync_world_runtime()` (`job_stats`, `active_tasks`, `active_jobs`, final runtime payload shaping); it has a clear data boundary, low behavior risk, and can follow the same helper-module pattern as `aa3fc80`. P2 is the isolated `BASE_UNDER_ATTACK` emergency cluster (`_ensure_defend_base_task` / `_resolve_defend_base_target_position` / `_ensure_immediate_defend_base_job` / `_handle_base_under_attack_auto_response`) as a feature-island extraction. Explicit do-not-touch-first areas: the resource arbitration block (`_build_resource_needs` through `_notify_resource_loss`) because it owns allocation/preemption semantics, and the remaining unit-request orchestration glue because it still coordinates idle matching, bootstrap production, reservation state, and agent suspend/wake ordering.

## [2026-04-11 15:43] DONE — Normalized Adjutant battlefield/triage snapshot reads
Landed `1912ef8 refactor: normalize adjutant snapshot reads` across `adjutant/adjutant.py` and `tests/test_runtime_views.py`. Root cause: even after `BattlefieldSnapshot` and `TaskTriageSnapshot` existed, parts of Adjutant still consumed those coordinator inputs as raw dicts, which kept the semantic boundary soft and made field-shape drift easy to reintroduce. I switched `_infer_task_domain()` to use `TaskTriageSnapshot.from_mapping(...)`, tightened `_score_info_target()` to consume `BattlefieldSnapshot` instead of bare `.get(...)` access, and added a focused normalization regression for task triage snapshots. Verification: `pytest tests/test_runtime_views.py tests/test_adjutant_coordinator.py tests/test_adjutant.py -q` (91 passed) and `python3 -m py_compile adjutant/adjutant.py runtime_views.py tests/test_runtime_views.py`.

## [2026-04-11 15:58] DONE — Normalized runtime-state snapshot reads across Adjutant/task triage
Landed `9b8e114 refactor: normalize runtime state snapshot reads` across `runtime_views.py`, `task_triage.py`, `adjutant/adjutant.py`, and `tests/test_runtime_views.py`. Root cause: `RuntimeStateSnapshot` only had a `to_dict()` export path, so `task_triage` and Adjutant still re-shaped `runtime_state` by hand and repeatedly re-parsed `capability_status`. I added `RuntimeStateSnapshot.from_mapping(...)`, then updated the main coordinator/triage read paths to consume normalized runtime snapshots instead of local dict plumbing. This keeps `active_tasks`, `active_jobs`, `unit_reservations`, and `capability_status` on one typed boundary instead of four separate ad hoc readers. Verification: `pytest tests/test_runtime_views.py tests/test_adjutant_coordinator.py tests/test_adjutant.py tests/test_ws_and_review.py -q` (127 passed) and `python3 -m py_compile runtime_views.py task_triage.py adjutant/adjutant.py tests/test_runtime_views.py`.

## [2026-04-11 16:18] DONE — Archived completed `docs/yu` investigations and fixed active-path references
Trimmed the top-level `docs/yu` surface back to active execution references and moved completed audits/live traces into `docs/yu/archive/` and `docs/yu/archive/20260409/`. This included preserving the previously stash-hidden `agent_framework_maturity_audit_20260409.md` by restoring it into archive instead of letting it remain an implicit deletion. I then updated active docs such as `progress.md` and the roadmap/logging reports so they no longer point at removed top-level paths. The result is a much smaller top-level `docs/yu/` focused on `plan/agents/progress`, execution board, knowledge table, pending drift, and the Wang-facing correction report.

## [2026-04-11 16:42] DONE — Closed the next false-green slice with real bootstrap delivery coverage
Used Xi’s latest test-signal audit as the next cleanup target and closed two concrete gaps instead of just tightening more assertions. First, I fixed a real runtime hole in `game_loop/loop.py`: when `EconomyJob` detected completion via direct actor appearance instead of a `WorldModel` production event, pending unit requests were never re-fulfilled. The loop now synthesizes an internal `PRODUCTION_COMPLETE` route on `EconomyExpert` produced-count deltas, which reuses the existing Kernel fulfill path without touching arbitration logic. Second, I turned `tests/test_e2e_capability_bootstrap.py` into a deterministic smoke that drives the real world refresh + job tick path with forced refreshes and simulated time, so it now covers ordinary-task `request_units` → capability bootstrap → production → request fulfillment → actor handoff without depending on background-loop timing. In parallel, I hardened `tests/test_adjutant.py` mock contracts by adding explicit surface/schema guards, extending `MockKernel` to expose the minimal runtime/list/message interfaces Adjutant now expects, and normalizing nested runtime/battlefield capability snapshots. Verification: `pytest tests/test_game_loop.py tests/test_e2e_capability_bootstrap.py tests/test_adjutant.py -q` (83 passed via subprocess run, no hang).

## [2026-04-11 16:55] DONE — Removed one more raw capability-status read path from `task_agent/context.py`
Followed the same runtime/coordinator contract cleanup path with a very small, safe refactor in `task_agent/context.py`. Root cause: Capability prompt/context rendering still re-parsed `rf["capability_status"]` ad hoc in several nearby helpers, which kept the typed boundary soft and invited the next drift back to raw dict access. I introduced a shared `_capability_status_snapshot(...)` helper and switched the directive/phase/blocker/runtime-status builders onto that typed path. Verification: `pytest tests/test_capability_task.py tests/test_task_agent.py -q` (122 passed).
## [2026-04-11 19:37] DONE — Audited the residual Allied vehicle-counter blind spot in planner knowledge
Read-only audit against `experts/knowledge.py`, `experts/planners.py`, and the related planner/demo-truth tests found two remaining issues. First, the actual blind spot is still a code gap in `counter_recommendation()`: the vehicle rule only offers `v2rl` and `3tnk`, both Soviet-locked, so `counter_recommendation(vehicle_heavy, faction="allied")` returns `None`. Second, `ProductionAdvisor` has a separate correctness bug that can hide that blind spot by producing the wrong Soviet answer for an Allied base: `_planner_faction_hint()` resolves own actors through `demo_capability_unit_type_for()`, which drops non-demo Allied ids like `tent` / `2tnk`, then falls back to ambiguous localized names such as `兵营` and infers `barr` → `soviet`. Reproduction in-shell showed `counter_allied=None`, `_planner_faction_hint(...) == "soviet"` for an Allied `tent`/`2tnk` actor set, and `ProductionAdvisor` then recommended `v2rl`. Test root cause: `tests/test_planners.py` and `tests/test_demo_capability_truth.py` only cover the `None` and `soviet` vehicle-heavy branches, so `pytest tests/test_planners.py tests/test_demo_capability_truth.py -q` stayed green (`28 passed`) while both Allied paths remained untested.

## [2026-04-12 04:45] DONE — Audited and tightened the deploy short-circuit truth boundary
Audited `Adjutant`'s `rule_deploy_already_deployed` path against the current world/runtime surfaces and the latest live session. Exact short-circuit trigger before the fix was: deploy-like, non-query, non-complex text containing `基地车`; `refresh_health().stale == False`; no `my_actors(category=mcv)` result; and at least one `my_actors(type=建造厂)` result, which returned `rule_deploy_already_deployed` without creating a task. Root cause of the truth gap: the deploy/no-deploy decision was being made off direct actor queries alone, while the same runtime already exposes normalized deploy truth through `compute_runtime_facts()` (`mcv_count`, `has_construction_yard`). I added a shared `_deploy_truth_snapshot()` helper so both feedback short-circuiting and direct NLU deploy resolution consume the same truth source, prefer runtime facts for `has_construction_yard`, and only mark the state ambiguous when runtime facts say an undeployed MCV exists but actor queries cannot yield a concrete MCV actor id. I explicitly did not treat construction-yard mismatch alone as ambiguous, because that would over-block safe `missing_mcv` / `already_deployed` replies and create a new false-negative class. Regression coverage now includes the runtime-facts-over-query construction-yard case and the true ambiguous-MCV case, and I patched the file’s `__main__` runner so `python tests/test_adjutant.py` no longer skips those new tests. Verification: `pytest tests/test_adjutant.py -q` (71 passed), `python tests/test_adjutant.py` (manual runner passed), and `pytest tests/test_world_model.py -k 'actor_queries_honor_type_aliases or tech_level_zero_when_only_mcv_present or production_readiness_marks_deploy_required_when_only_mcv_exists' -q` (2 passed).
## [2026-04-12 04:53] DONE — Audited remaining yellow-zone runtime/diagnostics risks before next E2E
Read-only audit across `adjutant/`, `kernel/`, `world_model/`, `web-console-v2/`, and the runtime/diagnostics-facing tests found that the main residual risk is no longer startup boot. The remaining yellow areas cluster around post-start visibility and trust: most automated E2E slices still bypass the WS/runtime-diagnostics path entirely (`enable_ws=False` or direct `Kernel` wiring), the only live WS runner still ignores `task_update`/`task_message`/`task_replay`/`benchmark`/`error` traffic and therefore can wait on stale state, and the Vue diagnostics/ops surfaces still have operator-facing gaps (unsupported `game_start`/`game_stop` controls, replay cache that can leave the “Current Runtime” block stale, and stale/disconnect health collapsed to a single boolean). Root-cause pattern: backend runtime facts and diagnostics payloads are now materially richer than the current E2E/frontend consumers, so post-start regressions can still hide behind green backend-only slices.

## [2026-04-12 04:51] DONE — Completed a read-only false-green audit of the current test stack
Audited `tests/`, `test_backend.sh`, `pytest.ini`, runtime smoke layering, and the direct `main.py` startup path without making code changes. Current state is better than the earlier single-smoke gate, but still extremely sparse at the layer boundaries that matter most for runtime truth: 62 test files / 719 test functions, yet only 1 `startup_smoke`, 4 `contract`, 2 `runtime_invariants`, 1 `mock_integration`, and 0 `live` markers. Root causes of the remaining false-green risk are structural: no test executes `run_runtime()` / `main.main()` with parsed CLI flags; startup smoke still hardcodes `enable_voice=False` and mock-injected providers/world source; most files remain unclassified unit-style tests; files named `test_e2e_*` are still mostly mock-driven; and 38 files still expose `if __name__ == "__main__"` runners that can drift from pytest discovery. I also traced why direct startup failures like `python main.py --enable-voice` can still escape: the true entrypoint path (`parse_args` → `run_runtime` → `ApplicationRuntime.start`) is not under test, the voice-enabled path is only unit-tested at `WSServer` level with `_check_voice_availability` mocked out, and even the specific `RuntimeBridge._handle_published_task_message` contract lives outside startup smoke. I am returning the actionable report and minimal closure slices in the user-facing response instead of patching code in this task.
