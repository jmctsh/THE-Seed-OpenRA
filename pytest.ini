[pytest]
markers =
    startup_smoke: real runtime startup and websocket smoke checks
    contract: module boundary and wiring contract tests
    runtime_invariants: runtime behavior and health invariant checks
    mock_integration: deterministic integration tests using mocked world/api surfaces
    live: manual or game-in-loop checks that are not part of the default green signal
